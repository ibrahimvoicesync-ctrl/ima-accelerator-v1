---
milestone: v1.7
milestone_name: Student Referral Links (Rebrandly Integration)
audited: 2026-04-16
status: passed
scores:
  requirements: 20/20
  phases: 3/3
  integration: complete
  flows: 2/2
phases:
  - id: 58-schema-backfill
    verification: passed
    requirements_claimed: [DB-01, DB-02, DB-03, CFG-01, CFG-02]
  - id: 59-referral-api-rebrandly
    verification: human_needed
    note: "Code-verified; 6 runtime checks need live dev server (UAT batched to milestone-end)"
    requirements_claimed: [API-01, API-02, API-03, API-04, API-05, API-06, API-07, API-08, CFG-02]
  - id: 60-referralcard-ui-dashboard-integration
    verification: human_needed
    note: "Code-verified 5/6; 5 UAT items need human testing (UAT batched to milestone-end)"
    requirements_claimed: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, INT-01, INT-02, CFG-02]
integration:
  orphaned_exports: 0
  missing_connections: 0
  broken_flows: 0
  unprotected_routes: 0
tech_debt:
  carried_in:
    - "No Nyquist VALIDATION.md for v1.5 phases 44-52 (tracked since v1.6)"
    - "student_activity_status('active') branch lacks direct test coverage"
    - "Per-edit change-log for deal updates deferred (v1.5 D-17)"
    - "Full email notifications pipeline (Resend) still out-of-scope"
  new_in_v17:
    - phase: 60
      items:
        - "IN-01 (code review): empty-name greeting renders 'Good morning, !' — pre-existing dashboard bug, not introduced by v1.7, not in fix scope"
        - "IN-02 (code review): Number(d.revenue/profit) produces literal 'NaN' string on non-numeric columns — pre-existing dashboard bug, not introduced by v1.7"
        - "UI-REVIEW accessibility enhancements (non-blocking, score was 24/24): add min-w-0 on URL span, aria-live on READY transition, aria-live on Copied status"
pending_uat:
  phase_59:
    - "POST /api/referral-link against live dev server (auth, role gates, env guard, Rebrandly call, idempotency, CAS, error shapes — 6 runtime scenarios)"
  phase_60:
    - "Student /student — card visible at bottom with correct visual styling"
    - "Click flow: spinner → READY state with URL + Copy + Share (mobile)"
    - "Copy toggle: 2-second Copied! revert"
    - "Error path: network failure shows toast + button re-enables"
    - "Repeat all on /student_diy"
  policy: "Batch UAT to milestone-end per user preference (captured in Claude memory)."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 3  # 58, 59, 60 — project has no test framework, gate is lint+tsc+build
  overall: "Project-wide architectural decision: no test framework; validation = lint+tsc+build. All three phases passed the CFG-02 build gate."
---

# v1.7 Milestone Audit — Student Referral Links (Rebrandly Integration)

**Date:** 2026-04-16
**Milestone owner policy:** Batch UAT to milestone-end (don't pause for per-phase manual UAT).
**Project validation model:** No test framework installed; verification = `npm run lint && npx tsc --noEmit && npm run build` exits 0 (CFG-02 gate at every phase boundary).

## Requirements Coverage — 20/20

All 20 v1.7 requirements (DB-01..03, CFG-01..02, API-01..08, UI-01..06, INT-01..02) are wired end-to-end with confirmed touchpoints at each hop of the 3-phase data flow (Schema → API → UI). See integration checker report below.

## Phases — 3/3 complete

| Phase | Status | Code-level verification | Notes |
|-------|--------|------------------------|-------|
| 58 — Schema & Backfill | Complete | PASSED (code-verified) | Migration 00031 applied; backfill complete; `referral_code` unique index created |
| 59 — Referral API + Rebrandly | Complete | HUMAN-NEEDED | Code-verified; 6 runtime checks batched for milestone UAT |
| 60 — ReferralCard UI & Dashboard Integration | Complete | HUMAN-NEEDED | Code-verified 5/6; 5 UAT items batched for milestone UAT |

## Cross-Phase Integration — WIRED

- **No orphaned exports** — every type export, API route, and component is consumed downstream.
- **No missing connections** — Schema columns → types.ts → API reads/writes → UI renders.
- **No broken flows** — both E2E flows (generate link, idempotent second call) are intact in code.
- **No unprotected routes** — API gates on session + role; dashboard pages gate via `requireRole()`; proxy guards path prefixes.

### E2E Flow: Student generates referral link
Schema column → API 8-step pipeline (auth, role, env guard, code CAS, Rebrandly call, URL CAS persist, response) → UI fetch with `response.ok` guard → render short URL with Copy + Share. **WIRED.**

### E2E Flow: Idempotency (second POST returns cached URL)
API cache-hit at STEP 2 short-circuits before Rebrandly is called, returning the stored `referral_short_url`. **WIRED.**

## Tech Debt (new in v1.7)

All non-blocking, tracked for backlog:

- **IN-01 (code review):** `user.name.split(" ")[0]` renders "Good morning, !" for empty names in `student/page.tsx` and `student_diy/page.tsx`. Pre-existing dashboard bug; surfaced but not in Phase 60 fix scope (Info severity).
- **IN-02 (code review):** `Number(d.revenue)` / `Number(d.profit)` produces literal `"NaN"` in Total Revenue/Profit cards on non-numeric columns. Pre-existing dashboard bug; Info severity.
- **UI accessibility enhancements** (Phase 60 UI-REVIEW scored 24/24 — these are *beyond* spec compliance):
  - Add `min-w-0` on URL span for reliable truncate in extreme narrow widths.
  - Add `aria-live="polite"` on the READY state transition to announce URL appearance.
  - Add a visually-hidden `aria-live` span announcing "Copied to clipboard" on success.

## Tech Debt (carried in from v1.6 and earlier)

Unchanged — v1.7 did not close any of these:

- No Nyquist VALIDATION.md for v1.5 phases 44-52.
- `student_activity_status('active')` branch lacks direct test coverage.
- Per-edit change-log for deal updates deferred (v1.5 D-17).
- Full email notifications pipeline (Resend) still out-of-scope.

## Pending UAT (batch at milestone close)

### Phase 59 — API runtime checks
Needs live dev server (`npm run dev`) + real Supabase auth session:
1. Unauthenticated POST → 401
2. Authenticated owner role → 403
3. Missing `REBRANDLY_API_KEY` env → 500 with clear `console.error`
4. First student POST → 200 with valid `{ shortUrl, referralCode }`, Rebrandly called exactly once
5. Second student POST → 200 returns cached values, Rebrandly NOT called
6. Rebrandly 5xx simulated → 502 with rollback behavior

### Phase 60 — ReferralCard UI checks
1. Student `/student` — card visible at bottom with correct visual styling.
2. Click flow: spinner → READY state with URL + Copy + Share (mobile viewport where `navigator.share` is available).
3. Copy toggle: 2-second "Copied!" revert.
4. Error path: network failure shows toast + button re-enables.
5. Repeat all on `/student_diy`.

## Status: PASSED

All 20 requirements met at the code level. Cross-phase integration is clean (0 orphans, 0 missing, 0 broken flows). Code reviews closed (1 warning fixed, 2 info items deferred as pre-existing bugs). UI review scored 24/24. Pending items are human UAT batched per explicit user policy — no gaps_found, no critical blockers.

Proceed to `/gsd-complete-milestone v1.7`.
