---
phase: 58-schema-backfill
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified: []
autonomous: true
requirements: [DB-01, DB-02, DB-03, CFG-02]

must_haves:
  truths:
    - "Migration 00031_referral_links.sql is applied to the live Supabase DB (local or remote, whichever the repo is pointed at)"
    - "Every ASSERT in the migration's DO $phase58_assert$ block passed (no exception raised — implied by successful push)"
    - "Every existing public.users row with role IN ('student','student_diy') now has a non-null referral_code"
    - "Every public.users row with role IN ('owner','coach') still has referral_code IS NULL"
    - "The partial UNIQUE index idx_users_referral_code exists in pg_indexes"
    - "Post-phase build gate passes: `npm run lint && npx tsc --noEmit && npm run build` exits 0"
  artifacts:
    - path: ".planning/phases/58-schema-backfill/58-02-SUMMARY.md"
      provides: "Evidence of migration application + build-gate pass"
      min_lines: 20
  key_links:
    - from: "supabase/migrations/00031_referral_links.sql"
      to: "live Postgres schema"
      via: "supabase db push"
      pattern: "referral_code\\|referral_short_url"
    - from: "CFG-02 build gate"
      to: "Next.js production bundle"
      via: "`npm run lint && npx tsc --noEmit && npm run build`"
      pattern: "exit\\s+code\\s+0"
---

<objective>
Apply the migration written in Plan 01 to the live database, verify the schema + backfill invariants against real data, and run the CFG-02 post-phase build gate. This plan is [BLOCKING] — Phase 58 cannot close without the `supabase db push` step, because build + type-check will pass WITHOUT the push (types come from config/hand-edit, not from live DB introspection), creating a false-positive verification state.

Purpose: Close DB-01, DB-02, DB-03 at runtime (the migration's embedded ASSERTs prove each criterion in-database) and close CFG-02 (the phase-close build gate).
Output: 58-02-SUMMARY.md with push output, post-push query evidence (row counts, index existence), and build-gate exit code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-schema-backfill/58-CONTEXT.md
@.planning/phases/58-schema-backfill/58-RESEARCH.md
@.planning/phases/58-schema-backfill/58-01-SUMMARY.md
@CLAUDE.md

@supabase/migrations/00031_referral_links.sql
@package.json

<interfaces>
<!-- Commands and env vars the executor needs. -->

Supabase CLI (from package.json devDependencies: supabase ^2.78.1):
- `npx supabase db push` — applies pending migrations to the linked project. In non-TTY environments, set `SUPABASE_ACCESS_TOKEN` env var to skip the login prompt.
- `npx supabase db reset` — local-dev alternative that rebuilds the local DB from scratch and re-applies all migrations (including 00031). Use this only if the repo is pointed at a local Supabase instance (`supabase start` was run).
- `npx supabase migration list` — shows which migrations are registered locally vs. applied remotely.

Build gate (CLAUDE.md "Commands" + REQUIREMENTS CFG-02):
- `npm run lint && npx tsc --noEmit && npm run build` — MUST exit 0.

From CLAUDE.md Hard Rule 4: "Admin client only in server code — never import in client components". Applies only to code changes; no code changes in Plan 02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Apply migration 00031 via supabase db push</name>
  <files></files>
  <read_first>
    - supabase/migrations/00031_referral_links.sql (verify the file from Plan 01 is present before pushing)
    - .planning/phases/58-schema-backfill/58-01-SUMMARY.md (confirm Plan 01 completed successfully and files are on disk)
    - package.json (confirm `supabase` is in devDependencies at the version researched in RESEARCH.md — ^2.78.1)
  </read_first>
  <action>
Apply the migration to the live Supabase database. This is the **[BLOCKING]** step — Phase 58 verification is a false-positive without it because `tsc --noEmit` and `npm run build` both pass against the hand-edited `types.ts` regardless of whether the live schema actually has the columns.

**Non-TTY workaround:** if `SUPABASE_ACCESS_TOKEN` is not set and the push command prompts for login, export it from the user's Supabase account access token first: `export SUPABASE_ACCESS_TOKEN=<token>`. If the executor cannot obtain the token non-interactively, set this task to `autonomous: false` and surface a decision-checkpoint to the user (but attempt the autonomous path first).

Execution order (stop on first non-zero exit):

1. Verify the migration file is present:
   ```bash
   test -f supabase/migrations/00031_referral_links.sql
   ```

2. Check which local/remote Supabase target the repo is pointed at:
   ```bash
   npx supabase status || true    # shows local URL if `supabase start` was run
   npx supabase migration list    # shows registered vs applied migrations
   ```

3. Apply the migration. Prefer `npx supabase db push` (pushes pending migrations to the linked project). If that requires interactive login and `SUPABASE_ACCESS_TOKEN` is unset, fall back to `npx supabase db reset` (safe local-dev alternative that rebuilds from scratch and re-runs all migrations — only use if `supabase status` shows a local stack is running):
   ```bash
   # Primary path (production-ish / linked remote):
   npx supabase db push
   # Fallback (local only):
   # npx supabase db reset
   ```
   Capture full stdout + stderr into a variable for the SUMMARY.md. A non-zero exit here means either the migration SQL had a syntax error (edit `00031_referral_links.sql` and re-run) or an ASSERT failed (edit the migration, confirm data contract, re-run).

4. Verify success post-push with three runtime queries (use `npx supabase db execute` or any SQL runner pointed at the same DB):
   ```sql
   -- Q1: ASSERT 1a/1b — columns exist and are nullable
   SELECT column_name, data_type, character_maximum_length, is_nullable
     FROM information_schema.columns
    WHERE table_schema='public' AND table_name='users'
      AND column_name IN ('referral_code', 'referral_short_url')
    ORDER BY column_name;
   -- Expected: 2 rows; both is_nullable='YES'; referral_code has character_maximum_length=12.

   -- Q2: ASSERT 2a/2b — backfill correctness
   SELECT role, count(*) AS total,
          count(*) FILTER (WHERE referral_code IS NULL) AS null_codes,
          count(*) FILTER (WHERE referral_code IS NOT NULL) AS with_codes
     FROM public.users
    GROUP BY role
    ORDER BY role;
   -- Expected: student + student_diy rows have null_codes = 0; owner + coach rows have with_codes = 0.

   -- Q3: ASSERT 3 — partial UNIQUE index exists
   SELECT indexname, indexdef
     FROM pg_indexes
    WHERE schemaname='public'
      AND tablename='users'
      AND indexname='idx_users_referral_code';
   -- Expected: 1 row; indexdef contains "UNIQUE" and "WHERE (referral_code IS NOT NULL)".
   ```

5. If any query returns unexpected results, STOP. Record the exact output in SUMMARY.md and flag the phase as failed — do not proceed to Task 2 (build gate). Debug the migration, re-run push, re-run queries.

Do NOT: edit the migration file from Plan 01 unless a query in step 4 reveals a real data bug — the migration is the spec. Do NOT add a new migration number to "fix" Phase 58 unless 00031 was already applied to a production-like DB that cannot be reset.
  </action>
  <verify>
    <automated>bash -c "npx supabase migration list 2>&1 | grep -q '00031' && test -f supabase/migrations/00031_referral_links.sql"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase migration list` output shows `00031` (or `00031_referral_links`) in the "applied" / "remote" column — depending on CLI version, the marker may be `●` or `applied` or a date; the row exists.
    - Query Q1 returns exactly 2 rows (`referral_code`, `referral_short_url`), both with `is_nullable='YES'`, `referral_code` has `character_maximum_length=12`.
    - Query Q2 shows: for every role in ('student','student_diy'), `null_codes = 0`; for every role in ('owner','coach'), `with_codes = 0`.
    - Query Q3 returns exactly 1 row, and the `indexdef` string contains both `UNIQUE` (case-insensitive) and `WHERE (referral_code IS NOT NULL)` (accept equivalent formatting like `WHERE referral_code IS NOT NULL`).
    - `supabase db push` stdout contains the migration filename OR the output contains "Finished" / "Applied" / equivalent success marker and exit code is 0.
    - The migration's `DO $phase58_assert$` block DID NOT raise an exception (implied by the push succeeding — any ASSERT failure aborts the transaction, aborts the push, non-zero exit).
  </acceptance_criteria>
  <done>
    Migration is applied to the live DB, all three post-push queries return the expected shape, and the CLI reports success. Evidence captured in 58-02-SUMMARY.md ready for Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run CFG-02 post-phase build gate</name>
  <files></files>
  <read_first>
    - CLAUDE.md (Commands section — confirms `npm run lint`, `npx tsc --noEmit`, `npm run build` are the three gates)
    - .planning/REQUIREMENTS.md (CFG-02 — "Post-phase build gate passes with zero errors for every phase in v1.7")
    - .planning/phases/58-schema-backfill/58-RESEARCH.md (Example 5 — verbatim gate command)
  </read_first>
  <action>
Run the CFG-02 phase-close build gate exactly as specified in REQUIREMENTS:

```bash
npm run lint && npx tsc --noEmit && npm run build
```

Order matters — `&&` short-circuits on first non-zero. The full command must exit 0. No step may be skipped or replaced (do NOT substitute `npm run build` with `next build` or a partial check).

Capture:
- The full stdout of each step (lint warnings list, tsc errors if any, build output including route sizes and build duration).
- The final exit code (must be 0).
- Any ESLint warnings — "zero errors" is the CFG-02 bar, but record warnings so they don't accumulate silently.

If lint fails: the failure is almost certainly unrelated to Phase 58 (no new component/API files were added) — but if it IS related (e.g. a stale import referring to a non-existent type), fix the offending file and re-run from the top.

If `tsc --noEmit` fails: the most likely cause is a consumer in Phase 59/60 work (uncommitted, not yet planned) referencing `referral_code` before the types.ts edit landed — should not happen in Phase 58 scope. If it does, the Plan 01 types.ts edit is probably malformed; re-read `src/lib/types.ts` around the edit site and compare against RESEARCH.md Example 2.

If `npm run build` fails: same analysis — either pre-existing drift (fix before re-running) or a typo introduced during Plan 01 (correct and re-run).

Record build gate output in SUMMARY.md: per-step duration, whether warnings were emitted, final exit code.
  </action>
  <verify>
    <automated>bash -c "npm run lint && npx tsc --noEmit && npm run build"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0.
    - Combined command `npm run lint && npx tsc --noEmit && npm run build` exits 0.
    - No new ESLint errors introduced vs. the last v1.6 closure. Warnings (if any) are documented in SUMMARY.md but do not block.
    - `npm run build` output contains evidence of a successful Next.js build: "Compiled successfully" / "Creating an optimized production build" / route size table present.
  </acceptance_criteria>
  <done>
    Build gate completed with exit 0. Output captured in SUMMARY.md. Phase 58 is ready to close.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev environment → Supabase project (local or linked remote) | Migration writes to live DB. A bad ASSERT would corrupt the transaction (auto-rolled back, but the push attempt is visible in CLI history). |
| `SUPABASE_ACCESS_TOKEN` env var → Supabase auth | If exposed in shell history or CI logs, grants push rights to the linked project. Must live in `.env.local` or session-only export. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-06 | Availability | `supabase db push` against remote project | mitigate | `BEGIN;...COMMIT;` envelope in 00031 guarantees atomic rollback on ASSERT failure. Post-push verification queries (Q1/Q2/Q3) catch any silent drift. |
| T-58-07 | Elevation of Privilege | `SUPABASE_ACCESS_TOKEN` leak | mitigate | Token is a shell export only, never committed. If CI needs it, use the CI's secret store. Plan does NOT write the token to any file. |
| T-58-08 | Tampering | False-positive build gate (build passes but DB schema differs from types.ts) | mitigate | This is exactly why Task 1 is BLOCKING and runs BEFORE Task 2. Without the push, types.ts declares fields the DB doesn't have — build passes, runtime fails on first Phase 59 call. Blocking the gate behind `db push` closes the gap. |
</threat_model>

<verification>
- [ ] `supabase db push` exited 0, migration 00031 is visible in `supabase migration list` as applied.
- [ ] Post-push Q1/Q2/Q3 return expected shape (columns nullable, backfill correct, partial UNIQUE index present).
- [ ] `npm run lint && npx tsc --noEmit && npm run build` exited 0.
- [ ] No real Rebrandly API key committed to git in Phase 58 (re-verify by `git log -p -- .env.local.example | grep -E '^\+REBRANDLY_API_KEY=.+' | wc -l` outputs `0`).
</verification>

<success_criteria>
- Live Postgres schema has `public.users.referral_code (varchar(12), nullable)` and `public.users.referral_short_url (text, nullable)`.
- Every pre-existing `student` / `student_diy` row has a non-null `referral_code` matching `upper(substr(md5(id::text), 1, 8))`.
- Every `owner` / `coach` row still has `referral_code IS NULL`.
- `idx_users_referral_code` partial UNIQUE index exists on `public.users(referral_code) WHERE referral_code IS NOT NULL`.
- CFG-02 build gate exits 0 after migration + types.ts + env-example edits.
- Phase 58 closed; Phase 59 can proceed against a fully-typed and fully-migrated baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/58-schema-backfill/58-02-SUMMARY.md` describing:
- `supabase db push` output (command used, any stderr, exit code, CLI version).
- Results of Q1/Q2/Q3 verification queries with actual row counts per role.
- Build gate timing + exit code + any warnings (not errors) emitted.
- Final phase-close status: all of DB-01, DB-02, DB-03, CFG-01, CFG-02 satisfied.
</output>
