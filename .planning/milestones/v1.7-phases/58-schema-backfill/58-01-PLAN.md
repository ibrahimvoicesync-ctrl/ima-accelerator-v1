---
phase: 58-schema-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00031_referral_links.sql
  - src/lib/types.ts
  - .env.local.example
autonomous: true
requirements: [DB-01, DB-02, DB-03, CFG-01]

must_haves:
  truths:
    - "File supabase/migrations/00031_referral_links.sql exists and contains a single BEGIN;...COMMIT; transaction"
    - "Migration adds public.users.referral_code (varchar(12), nullable) and public.users.referral_short_url (text, nullable)"
    - "Migration backfills referral_code = upper(substr(md5(id::text), 1, 8)) for every existing role IN ('student','student_diy') row, owners/coaches untouched"
    - "Migration creates partial UNIQUE index idx_users_referral_code ON public.users(referral_code) WHERE referral_code IS NOT NULL"
    - "src/lib/types.ts users Row/Insert/Update blocks contain referral_code: string | null and referral_short_url: string | null (optional on Insert/Update)"
    - ".env.local.example contains a REBRANDLY_API_KEY= line with empty value under a labelled Rebrandly section"
  artifacts:
    - path: "supabase/migrations/00031_referral_links.sql"
      provides: "Schema migration: 2 nullable columns + backfill + partial UNIQUE index + ASSERT block"
      min_lines: 80
      contains: "BEGIN;"
    - path: "src/lib/types.ts"
      provides: "TypeScript types for users table including new referral columns"
      contains: "referral_code"
    - path: ".env.local.example"
      provides: "Dev onboarding documentation for REBRANDLY_API_KEY"
      contains: "REBRANDLY_API_KEY="
  key_links:
    - from: "supabase/migrations/00031_referral_links.sql"
      to: "public.users table"
      via: "ALTER TABLE ... ADD COLUMN + UPDATE + CREATE UNIQUE INDEX"
      pattern: "ALTER TABLE public\\.users"
    - from: "src/lib/types.ts"
      to: "Database['public']['Tables']['users']"
      via: "hand-edit of Row/Insert/Update blocks"
      pattern: "referral_code:\\s*string\\s*\\|\\s*null"
---

<objective>
Create the Phase 58 artifacts that land the referral-link schema and dev onboarding contract: a single-transaction Postgres migration (`00031_referral_links.sql`), a hand-edit of `src/lib/types.ts` that surfaces the two new columns on the `users` Row/Insert/Update blocks, and an append to `.env.local.example` that documents `REBRANDLY_API_KEY=` for onboarding devs.

Purpose: Close DB-01, DB-02, DB-03, CFG-01 — the entire file-editing surface of Phase 58. The actual DB write (applying the migration) happens in Plan 02 so this plan is pure file authoring and can be re-run / re-diffed safely.
Output: Three modified/created files ready for the Plan 02 `supabase db push` step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-schema-backfill/58-CONTEXT.md
@.planning/phases/58-schema-backfill/58-RESEARCH.md
@CLAUDE.md

@supabase/migrations/00022_deals_logged_by.sql
@supabase/migrations/00030_roadmap_step_8_insertion.sql
@supabase/migrations/00015_v1_4_schema.sql
@src/lib/types.ts
@.env.local.example
@.gitignore

<interfaces>
<!-- Contracts the executor MUST respect. Extracted from repo (2026-04-16). -->

From src/lib/types.ts (current users block — HAND-EDIT this block, do NOT regen):
```typescript
users: {
  Row: {
    auth_id: string | null
    coach_id: string | null
    created_at: string
    email: string
    id: string
    joined_at: string
    last_active_at: string | null
    name: string
    niche: string | null
    // HAND-EDIT: narrow from DB CHECK constraint — reapply after regen.
    role: "owner" | "coach" | "student" | "student_diy"
    status: "active" | "inactive" | "suspended"
    streak_count: number
    updated_at: string
  }
  Insert: { /* same fields, most optional */ }
  Update: { /* same fields, all optional */ }
  Relationships: [ /* coach_id FK to users.id */ ]
}
```

Two new columns to insert alphabetically after `niche`:
- `referral_code: string | null` (Row), `referral_code?: string | null` (Insert/Update)
- `referral_short_url: string | null` (Row), `referral_short_url?: string | null` (Insert/Update)

From supabase/migrations/00022_deals_logged_by.sql — the structural template for additive migrations:
```sql
-- ALTER TABLE ... ADD COLUMN IF NOT EXISTS <name> <type>;
-- UPDATE ... SET <col> = <expr> WHERE <guard>;
-- DO $$ ... ASSERT ... $$; -- verification block
```

From supabase/migrations/00015_v1_4_schema.sql line 62 — partial unique index precedent:
```sql
CREATE UNIQUE INDEX idx_messages_recipient_read
  ON public.messages (recipient_id)
  WHERE read_at IS NULL;
```

From .env.local.example (current file, 6 lines + trailing blank):
```
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Discord WidgetBot (Resources > Community tab)
NEXT_PUBLIC_DISCORD_GUILD_ID=
NEXT_PUBLIC_DISCORD_CHANNEL_ID=
```

From .gitignore — confirms `.env.local.example` is TRACKED (`.env*.local` ignored, `.example` suffix NOT ignored).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 00031_referral_links.sql migration</name>
  <files>supabase/migrations/00031_referral_links.sql</files>
  <read_first>
    - supabase/migrations/00022_deals_logged_by.sql (canonical additive + backfill + ASSERT template)
    - supabase/migrations/00030_roadmap_step_8_insertion.sql (most recent migration — confirms BEGIN/COMMIT + DO $$ ASSERT $$ envelope is current style)
    - supabase/migrations/00015_v1_4_schema.sql (partial UNIQUE index precedent at line 62: idx_messages_recipient_read ... WHERE read_at IS NULL)
    - .planning/phases/58-schema-backfill/58-RESEARCH.md (Example 1 — the full migration skeleton to copy verbatim)
    - .planning/REQUIREMENTS.md (DB-01/02/03 — exact success-criteria wording must match)
  </read_first>
  <action>
Create `supabase/migrations/00031_referral_links.sql` as a single `BEGIN;...COMMIT;` transaction. Copy the structure from RESEARCH.md Example 1 verbatim; do NOT paraphrase or re-order the sections. Concrete requirements:

1. **Header comment block** — file-level summary naming the phase ("Phase 58: Schema & Backfill"), the closed requirements ("DB-01, DB-02, DB-03"), and the idempotency/transactional guarantees.

2. **Section 1 — ADD COLUMN** (uses `IF NOT EXISTS` for rerun-safety, both columns in a single `ALTER TABLE` statement):
   ```sql
   ALTER TABLE public.users
     ADD COLUMN IF NOT EXISTS referral_code      varchar(12),
     ADD COLUMN IF NOT EXISTS referral_short_url text;
   ```
   Also add two `COMMENT ON COLUMN` statements documenting the purpose (text from RESEARCH.md Example 1).

3. **Section 2 — BACKFILL** with idempotency guard (`AND referral_code IS NULL`):
   ```sql
   UPDATE public.users
      SET referral_code = upper(substr(md5(id::text), 1, 8))
    WHERE role IN ('student', 'student_diy')
      AND referral_code IS NULL;
   ```

4. **Section 3 — partial UNIQUE index** (`IF NOT EXISTS` for rerun-safety):
   ```sql
   CREATE UNIQUE INDEX IF NOT EXISTS idx_users_referral_code
     ON public.users (referral_code)
     WHERE referral_code IS NOT NULL;
   ```

5. **Section 4 — embedded ASSERT block** wrapped in `DO $phase58_assert$ ... $phase58_assert$;`. MUST verify all four invariants (copy ASSERT bodies VERBATIM from RESEARCH.md Example 1):
   - ASSERT 1a: `referral_code` column exists, is_nullable='YES', character_maximum_length=12
   - ASSERT 1b: `referral_short_url` column exists, is_nullable='YES'
   - ASSERT 2a: COUNT where role IN ('student','student_diy') AND referral_code IS NULL = 0 (every target row backfilled)
   - ASSERT 2b: COUNT where role IN ('owner','coach') AND referral_code IS NOT NULL = 0 (no pollution)
   - ASSERT 2c: COUNT where backfilled rows' codes ≠ `upper(substr(md5(id::text), 1, 8))` = 0 (deterministic shape check)
   - ASSERT 3: partial UNIQUE index `idx_users_referral_code` exists in `pg_indexes`

   Every ASSERT must include a descriptive failure message (use `format('...%s...', var)` for dynamic values, static strings otherwise).

6. Envelope the five sections in a single `BEGIN;` / `COMMIT;` pair — NOT nested, NOT per-section. Any ASSERT failure must roll back the entire migration.

Do NOT: add RLS policy changes (users RLS is untouched — Hard Rule 4 means admin client reads/writes bypass RLS). Do NOT use `CREATE TABLE` (we're altering, not creating). Do NOT drop the `AND referral_code IS NULL` guard (non-idempotent without it — Pitfall 4 in RESEARCH.md). Do NOT lowercase the md5 output (must be `upper(substr(md5(id::text), 1, 8))` verbatim — Pitfall 1).
  </action>
  <verify>
    <automated>bash -c "test -f supabase/migrations/00031_referral_links.sql && grep -q 'BEGIN;' supabase/migrations/00031_referral_links.sql && grep -q 'COMMIT;' supabase/migrations/00031_referral_links.sql && grep -q 'referral_code.*varchar(12)' supabase/migrations/00031_referral_links.sql && grep -q 'referral_short_url.*text' supabase/migrations/00031_referral_links.sql && grep -q \"upper(substr(md5(id::text), 1, 8))\" supabase/migrations/00031_referral_links.sql && grep -q 'idx_users_referral_code' supabase/migrations/00031_referral_links.sql && grep -q 'WHERE referral_code IS NOT NULL' supabase/migrations/00031_referral_links.sql && grep -q 'DO \\$phase58_assert\\$' supabase/migrations/00031_referral_links.sql"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/00031_referral_links.sql` exists.
    - File contains exactly one `BEGIN;` and exactly one `COMMIT;` at file scope: `grep -c '^BEGIN;' supabase/migrations/00031_referral_links.sql` outputs `1`; same for `^COMMIT;`.
    - File contains `ALTER TABLE public.users` and `ADD COLUMN IF NOT EXISTS referral_code      varchar(12)` (12 literal).
    - File contains `ADD COLUMN IF NOT EXISTS referral_short_url text`.
    - File contains the EXACT string `upper(substr(md5(id::text), 1, 8))` (case-sensitive match).
    - File contains `WHERE role IN ('student', 'student_diy')` AND `AND referral_code IS NULL` in the backfill UPDATE.
    - File contains `CREATE UNIQUE INDEX IF NOT EXISTS idx_users_referral_code` AND `WHERE referral_code IS NOT NULL`.
    - File contains `DO $phase58_assert$` and the matching close `$phase58_assert$`.
    - File contains 6 distinct `ASSERT` keywords (ASSERT 1a nullable, 1a varchar(12), 1b nullable, 2a backfill, 2b no owner/coach pollution, 2c shape match, 3 index exists — actually 7 asserts per Example 1; verify ≥6): `grep -c '^\s*ASSERT\|^\s\+ASSERT' supabase/migrations/00031_referral_links.sql` ≥ 6.
    - File does NOT contain `CREATE TABLE` (we're altering, not creating).
    - File does NOT contain `DROP` of any kind (pure additive migration).
  </acceptance_criteria>
  <done>
    Migration file exists at the specified path, matches the skeleton in RESEARCH.md Example 1 section-for-section, and all grep acceptance criteria pass. File content is ready to be applied by Plan 02's `supabase db push` step.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hand-edit src/lib/types.ts to surface referral columns on users Row/Insert/Update</name>
  <files>src/lib/types.ts</files>
  <read_first>
    - src/lib/types.ts (read lines 760–825 to see the exact current users Row/Insert/Update shape and the HAND-EDIT marker at line 777)
    - .planning/phases/58-schema-backfill/58-RESEARCH.md (Example 2 — the exact insertion shape, Pitfall 7 — why NOT to run codegen)
  </read_first>
  <action>
Hand-edit `src/lib/types.ts` to add two new fields to the `users` table's Row, Insert, and Update type blocks. Do NOT run `npx supabase gen types typescript` — doing so would clobber the existing `// HAND-EDIT: narrow from DB CHECK constraint — reapply after regen.` markers at lines 777 (role) and 640 (status).

Alphabetical insertion point: directly after `niche` and before `role` in all three blocks (so the field order stays alphabetical: …, niche, referral_code, referral_short_url, role, …).

Concrete diff to apply (insert exactly these lines, preserving surrounding indentation):

In the `users.Row` block (around line 770–782), after `niche: string | null` add:
```
          referral_code: string | null
          referral_short_url: string | null
```

In the `users.Insert` block (around line 783–797), after `niche?: string | null` add:
```
          referral_code?: string | null
          referral_short_url?: string | null
```

In the `users.Update` block (around line 798–812), after `niche?: string | null` add:
```
          referral_code?: string | null
          referral_short_url?: string | null
```

Do NOT touch the `role`, `status`, or any other field. Do NOT touch the `Relationships` array. Do NOT add to any other table's block (this is a `public.users` change only). Do NOT change indentation style (the file uses 10-space indentation for Row field lines — match it exactly).

After editing, run `npx tsc --noEmit` locally to confirm the delta compiles. It should exit 0 because no code yet references the new fields (Phase 59 is the first consumer).
  </action>
  <verify>
    <automated>bash -c "grep -c 'referral_code: string | null' src/lib/types.ts | grep -q '^1$' && grep -c 'referral_short_url: string | null' src/lib/types.ts | grep -q '^1$' && grep -c 'referral_code?: string | null' src/lib/types.ts | grep -q '^2$' && grep -c 'referral_short_url?: string | null' src/lib/types.ts | grep -q '^2$' && npx tsc --noEmit"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/types.ts` contains EXACTLY 1 occurrence of `referral_code: string | null` (the Row block, no `?`): `grep -c 'referral_code: string | null' src/lib/types.ts` outputs `1`.
    - `src/lib/types.ts` contains EXACTLY 1 occurrence of `referral_short_url: string | null` (the Row block): `grep -c 'referral_short_url: string | null' src/lib/types.ts` outputs `1`.
    - `src/lib/types.ts` contains EXACTLY 2 occurrences of `referral_code?: string | null` (Insert + Update): `grep -c 'referral_code?: string | null' src/lib/types.ts` outputs `2`.
    - `src/lib/types.ts` contains EXACTLY 2 occurrences of `referral_short_url?: string | null` (Insert + Update): `grep -c 'referral_short_url?: string | null' src/lib/types.ts` outputs `2`.
    - The HAND-EDIT marker at around line 777 (`// HAND-EDIT: narrow from DB CHECK constraint — reapply after regen.`) is still present: `grep -c 'HAND-EDIT: narrow from DB CHECK' src/lib/types.ts` outputs `2` (status + role markers preserved).
    - `role: "owner" | "coach" | "student" | "student_diy"` is still present in the users Row block (narrowing preserved): `grep -c '"owner" | "coach" | "student" | "student_diy"' src/lib/types.ts` outputs `3` (Row + Insert + Update).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `src/lib/types.ts` has the two new fields in all three users blocks, the HAND-EDIT narrowings for `role` and `status` are untouched, and `tsc --noEmit` passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append REBRANDLY_API_KEY block to .env.local.example</name>
  <files>.env.local.example</files>
  <read_first>
    - .env.local.example (current 7-line file — match the commented-group style already used for Discord)
    - .gitignore (confirm `.env.local.example` is NOT ignored — `.env*.local` pattern only catches `.env.local`, NOT `.env.local.example`)
    - .planning/phases/58-schema-backfill/58-RESEARCH.md (Example 3 — the exact append text to use; Pitfall 6 — confirm file is tracked)
  </read_first>
  <action>
Append a labelled Rebrandly section to `.env.local.example` matching the existing Discord comment-group style. Do NOT replace the file; preserve the existing 3 Supabase lines + blank + Discord block + Discord vars exactly.

After the existing `NEXT_PUBLIC_DISCORD_CHANNEL_ID=` line, append (with a preceding blank line separator):

```

# Rebrandly (student referral short-link generation — v1.7)
# Required by /api/referral-link (Phase 59). Get a key at https://app.rebrandly.com/account/api
# When unset, the referral endpoint returns HTTP 500 with a clear console.error; the dashboard continues to load.
REBRANDLY_API_KEY=
```

The value after the `=` MUST be empty — no key, no placeholder like `your-rebrandly-key-here`. A real key belongs only in `.env.local` (gitignored) or in hosting env vars. Committing a real key to the example file is a security incident (Pitfall 6 / Security Domain in RESEARCH.md).

Do NOT: reorder existing lines, change existing values, add other env vars, or group Rebrandly under Discord.
  </action>
  <verify>
    <automated>bash -c "grep -q '^REBRANDLY_API_KEY=$' .env.local.example && grep -q '^# Rebrandly' .env.local.example && grep -q '^NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321$' .env.local.example && grep -q '^NEXT_PUBLIC_DISCORD_CHANNEL_ID=$' .env.local.example && ! grep -qiE 'REBRANDLY_API_KEY=.+' .env.local.example"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.local.example` contains a line matching EXACTLY `^REBRANDLY_API_KEY=$` (empty value, no trailing chars): `grep -c '^REBRANDLY_API_KEY=$' .env.local.example` outputs `1`.
    - `.env.local.example` does NOT contain `REBRANDLY_API_KEY=` followed by any non-empty value (guard against accidental real-key commit): `grep -cE '^REBRANDLY_API_KEY=.+$' .env.local.example` outputs `0`.
    - `.env.local.example` contains the comment header `# Rebrandly (student referral short-link generation — v1.7)`: `grep -c '^# Rebrandly' .env.local.example` outputs `1`.
    - Existing lines preserved: `grep -c '^NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321$' .env.local.example` outputs `1`, `grep -c '^NEXT_PUBLIC_DISCORD_CHANNEL_ID=$' .env.local.example` outputs `1`.
    - File is tracked by git (not ignored): `git check-ignore .env.local.example` exits non-zero (no output).
  </acceptance_criteria>
  <done>
    `.env.local.example` has the Rebrandly block appended, empty value, comment header present, existing Supabase + Discord sections untouched, and file is git-tracked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git working tree → committed repo | Secret leakage risk if a real REBRANDLY_API_KEY lands in `.env.local.example`. |
| pre-migration DB → post-migration DB | Partial migration state if `BEGIN;...COMMIT;` envelope is missing. |
| types.ts hand-edit → TypeScript compile | Type drift breaks Phase 59 consumers if new columns are mis-typed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Information Disclosure | `.env.local.example` | mitigate | Value after `REBRANDLY_API_KEY=` must be empty. Acceptance criterion: `grep -cE '^REBRANDLY_API_KEY=.+$' .env.local.example` = 0. Reviewer greps diff before commit. |
| T-58-02 | Tampering (type-level) | `src/lib/types.ts` | mitigate | Hand-edit preserves existing HAND-EDIT narrowings for `role` and `status`. `tsc --noEmit` must exit 0 after edit. CFG-02 build gate in Plan 02 is final catch. |
| T-58-03 | Availability | `supabase/migrations/00031_referral_links.sql` | mitigate | Entire migration wrapped in `BEGIN;...COMMIT;`. Any ASSERT failure rolls back. Fix-before-proceed rather than accept partial state. |
| T-58-04 | Information Disclosure (weak) | `referral_code` derivation via `md5(id::text)` | accept | `md5` here is a deterministic address-space reducer for marketing codes, NOT a security primitive. 4.3B-slot space at 100k users → ~10⁻⁴ collision risk, surfaced via UNIQUE index. Custom domain + signed params explicitly out of scope per STATE.md. |
| T-58-05 | Tampering | Concurrent INSERT of new student during backfill window | accept | New student rows get `referral_code = NULL`; Phase 59 API-03 handles NULL-code case by generating at request time. No data loss, no locking needed. |
</threat_model>

<verification>
- [ ] `supabase/migrations/00031_referral_links.sql` exists and matches RESEARCH.md Example 1 structure.
- [ ] `src/lib/types.ts` has two new fields in Row/Insert/Update of the users block, HAND-EDIT markers intact.
- [ ] `.env.local.example` has `REBRANDLY_API_KEY=` (empty) under a labelled comment block.
- [ ] `npx tsc --noEmit` exits 0 after Task 2.
- [ ] No real Rebrandly API key is present in `.env.local.example` (empty value enforced).
</verification>

<success_criteria>
- DB-01, DB-02, DB-03 captured in migration SQL (not yet applied — that is Plan 02's job).
- CFG-01 satisfied by `.env.local.example` edit.
- All three files committed cleanly, no extraneous changes, no file-wide regen of `types.ts`.
- Plan 02 can immediately run `supabase db push` against the written migration file without any additional file edits.
</success_criteria>

<output>
After completion, create `.planning/phases/58-schema-backfill/58-01-SUMMARY.md` describing:
- Migration file path, line count, section structure.
- Exact diff applied to `src/lib/types.ts` (the 6 new lines).
- Exact diff applied to `.env.local.example` (the 5 new lines including blank separator).
- Confirmation `tsc --noEmit` passed post-edit.
- Status: files ready for Plan 02 `supabase db push`.
</output>
