# Milestone v1.8: Analytics Expansion, Notification Pruning & DIY Parity

**Status:** SHIPPED 2026-04-17
**Phases:** 61-65
**Total Plans:** 14
**Commits:** 39 (since v1.7)
**Files Modified:** 70
**LOC Delta:** +10,274 / -2,322
**Timeline:** 2 days (2026-04-16 → 2026-04-17)
**Requirements:** 53/53 satisfied (audit status: `tech_debt` — 13 deferred UAT items batched to end-of-milestone per `feedback_batch_uat_end_of_milestone` policy)
**Migrations:** 00033 - 00036

## Overview

Six surgical improvements across student analytics, owner analytics, owner alerts, coach alerts config, and the owner's student detail view. Delivered 53 requirements across 5 standalone/bundled phases:

- F1 (Phase 61): Student analytics outreach KPI re-split (brand / influencer independent aggregates, fixing the double-count `total_emails` bug)
- F5 (Phase 62): Coach `tech_setup` alert activation at Step 4 ("Set Up Your Agency")
- F6 (Phase 63): DIY students viewable on owner detail page with inline role-based UI gating
- F2+F3 (Phase 64): Owner analytics — 3 coach leaderboards + per-leaderboard Weekly/Monthly/Yearly/All Time window selectors (6 leaderboards × 4 windows = 24 pre-computed slots in one RPC call, zero client re-fetch on toggle)
- F4 (Phase 65): Owner alerts pruned to `deal_closed` only — 4 legacy alert types silently removed, replaced by one info alert per closed deal

Closed the v1.6-deferred `ownerAnalyticsTag()` invariant by wiring `/api/reports` into the invalidation graph.

## Phases

### Phase 61: Student Analytics Re-split (F1)

**Goal:** Student analytics KPI cards correctly labeled and re-split so brand outreach and influencer outreach are tracked as separate totals, replacing the combined `total_emails = SUM(brands + influencers)` bug with two independent aggregates via a breaking `get_student_analytics` RPC change.
**Depends on:** Phase 60 (v1.7 baseline)
**Plans:** 4 plans
**Requirements:** SA-01 - SA-09 (9/9)

Plans:

- [x] 61-01: Migration 00033 — defensive DROP + `CREATE OR REPLACE get_student_analytics` with renamed totals jsonb keys + post-assert
- [x] 61-02: Rename `StudentAnalyticsTotals` fields (`total_emails` → `total_brand_outreach`, `total_influencers` → `total_influencer_outreach`)
- [x] 61-03: Bump `unstable_cache` keys on both analytics pages to `student-analytics-v2`; rewrite AnalyticsClient KPI strip (renamed cards + DIY hide-guard removed + grid simplified to `lg:grid-cols-6`)
- [x] 61-04: Post-phase build gate + shape-assert

**Details:**
- Migration 00033 applied cleanly on top of 00032; PGRST203 prevention via defensive DROP pattern.
- Totals jsonb now returns `total_brand_outreach` + `total_influencer_outreach`; old `total_emails` / `total_influencers` keys removed (breaking change — all consumers updated in same milestone).
- SA-07 DIY KPI visibility: resolved as "show" (default) — DIY hide-guard removed from AnalyticsClient.tsx.
- Outreach trend chart + daily report form unchanged.
- VERIFICATION status: `human_needed` (9/9 code must-haves PASS; 5 deferred UAT items).

### Phase 62: Coach Alert `tech_setup` Activation — "Set Up Your Agency" at Step 4 (F5)

**Goal:** Coaches receive a "Set Up Your Agency" alert on their `/coach/alerts` feed whenever an assigned student completes roadmap Step 4 post-deploy, with zero retroactive flood — achieved by flipping `techSetupEnabled=true` + `techSetupStep=4`, rewriting the RPC CTE, and backfilling `alert_dismissals` for every historical completion.
**Depends on:** Phase 61
**Plans:** 1 plan
**Requirements:** CA-01 - CA-07 (7/7)

Plans:

- [x] 62-01: Migration 00034 + config flips + relabel + cache-key bump (single atomic commit)

**Details:**
- `MILESTONE_CONFIG.techSetupStep: 4`, `MILESTONE_FEATURE_FLAGS.techSetupEnabled: true`, `MILESTONE_META["tech_setup"].label: "Set Up Your Agency"`.
- Internal type key `tech_setup` PRESERVED (not renamed) — ripples through RPC, dismissal keys `milestone_tech_setup:%`, and in-flight dismissals untouched.
- Migration 00034 rewrites `get_coach_milestones` CTE from placeholder `step_number=0` to `step_number=4`; backfills `alert_dismissals` for all historical Step-4 completions (ASSERT B verified zero leakage per coach).
- `coach-milestones-v2` cache key bumped in same commit.
- VERIFICATION status: `human_needed` (7/7 PASS; 2 deferred UAT items).

### Phase 63: DIY Owner Detail Page (F6)

**Goal:** Owner navigating to `/owner/students/[studentId]` for a `student_diy` user lands on a working detail page instead of 404 — achieved by role-filter broadening on the detail route and list page, with daily-report-derived UI suppressed for DIY users.
**Depends on:** Phase 62
**Plans:** 1 plan
**Requirements:** DIY-01 - DIY-10 (10/10)

Plans:

- [x] 63-01: Role filter broadening + `role` prop thread + inline conditional rendering + DIY badge on list page

**Details:**
- Both `/owner/students/[studentId]/page.tsx` and `/owner/students/page.tsx` broadened from `.eq("role", "student")` to `.in("role", ["student", "student_diy"])`.
- `role` prop threaded through `OwnerStudentDetailClient` → `StudentDetailTabs` → `CalendarTab` + `StudentKpiSummary`.
- DIY Badge (ima-info variant) rendered on DIY rows in owner student list (no layout shift).
- CalendarTab for DIY renders hours-worked activity only; report dots + day-panel report card suppressed.
- `get_student_detail` RPC confirmed role-agnostic — zero migrations in this phase.
- Coach route `/coach/students/[studentId]` unchanged (owner-only scope).
- No parallel `/owner/students_diy/...` route tree, no `OwnerStudentDetailClientDIY.tsx`.
- VERIFICATION status: `human_needed` (10/10 PASS; 2 deferred UAT items).

### Phase 64: Owner Analytics Expansion — Coach Leaderboards + Per-Leaderboard Window Selectors (F2 + F3)

**Goal:** Owner analytics gains 3 coach leaderboards (revenue, avg total outreach per student per day, deals) beneath existing student leaderboards, and every one of the 6 leaderboards carries an independent Weekly / Monthly / Yearly / All Time toggle — powered by a single `get_owner_analytics` RPC that pre-computes all 24 slots and delivers via SSR with zero client re-fetch on toggle.
**Depends on:** Phase 63
**Plans:** 6 plans
**Requirements:** OA-01 - OA-08 + WS-01 - WS-10 (18/18)

Plans:

- [x] 64-01: Migration 00035 — expand `get_owner_analytics` to 24 slots + cache-key bump to `owner-analytics-v2`
- [x] 64-02: New `SegmentedControl` UI primitive (radiogroup + radio + arrow-key nav + 44px + ima-* tokens)
- [x] 64-03: `LeaderboardCard` extended with `linkRows?: boolean` (non-linked rows for coach leaderboards)
- [x] 64-04: `OwnerAnalyticsClient` — SSR payload + 6 independent window `useState` toggles, zero client re-fetch
- [x] 64-05: Wire `ownerAnalyticsTag()` into `/api/reports` (closes v1.6 deferred invariant on both insert + update branches)
- [x] 64-06: Build gate + shape-assert

**Details:**
- WS-02 ambiguity resolved: trailing N days (7/30/365), NOT calendar periods.
- Coach eligibility: `role='coach' AND status='active' AND EXISTS (active assigned student)`. Coaches with ≥1 active student but zero metric appear with 0 (no HAVING filter).
- Tiebreaker `ORDER BY metric DESC, LOWER(name) ASC, id::text ASC` applied across all 24 ranked CTEs.
- avg_total_outreach formula: `SUM(brands+influencers) / (COUNT(DISTINCT students) × window_days)` documented in SQL comment.
- Cache TAG unchanged (`owner-analytics`) — all existing `revalidateTag(ownerAnalyticsTag())` call sites keep working.
- `OwnerAnalyticsTeaser` untouched (stays student-only).
- VERIFICATION status: `passed` (9/9 must-haves + 18/18 reqs; fully automated, no UAT defers).

### Phase 65: Owner Alerts Prune to `deal_closed` Only (F4)

**Goal:** `/owner/alerts` stops generating `student_inactive`, `student_dropoff`, `unreviewed_reports`, and `coach_underperforming` entirely (silent, no tombstone) and replaces them with one `deal_closed` alert per closed deal; the owner sidebar badge count matches the pruned feed.
**Depends on:** Phase 64
**Plans:** 2 plans
**Requirements:** OAL-01 - OAL-09 (9/9)

Plans:

- [x] 65-01: Migration 00036 — rewrite OWNER branch of `get_sidebar_badges` + `sidebar-badges-v2` cache-key bump
- [x] 65-02: `/owner/alerts` feed rewrite — `AlertItem.type` narrowed to `"deal_closed"`; `TYPE_CONFIG` simplified; 4 legacy fetch branches fully removed

**Details:**
- Each alert: `type: "deal_closed"`, `title: student.name`, `message: "Closed a $X,XXX deal"` (revenue via `toLocaleString`), `subjectId: student_id`, `key: "deal_closed:${deal_id}"`, links to `/owner/students/${student_id}`, icon DollarSign with ima-success tokens.
- OAL-09 TTL resolved: 30-day trailing filter applied consistently in feed (`gte(created_at, thirtyDaysAgoIso)`) and RPC (`>= NOW() - INTERVAL '30 days'`).
- `/api/alerts/dismiss`, `alert_dismissals` table, dismissal uniqueness contract UNCHANGED — dismissal infrastructure reused verbatim.
- Orphaned rows from 4 removed alert types preserved for forensic history.
- COACH + STUDENT branches of `get_sidebar_badges` preserved byte-for-byte — coach alerts feed unaffected.
- VERIFICATION status: `passed` (9/9 must-haves; human UAT deferred per lean-context policy).

---

## Milestone Summary

**Key Decisions:**

- **SA-07 DIY KPI visibility** — show renamed Brand/Influencer Outreach cards on `/student_diy/analytics` (DIY hide-guard removed)
- **WS-02 window semantics** — trailing N days (7/30/365), not calendar periods (matches migration 00023:71 precedent)
- **OAL-09 feed TTL** — 30-day trailing filter (both feed and RPC use identical window)
- **`tech_setup` internal key preservation** — only the UI label changes; internal type key + dismissal-key prefix preserved to avoid RPC / dismissal-key ripple
- **Zero parallel route trees** — DIY handled via inline conditional rendering + role prop threading (DIY-05/DIY-09)
- **SSR-only window toggles** — all 24 slots pre-computed in one RPC call; toggle flips which slice renders (no fetch, no useEffect)

**Issues Resolved:**

- v1.6 deferred `ownerAnalyticsTag()` invariant closed — `/api/reports` now invalidates owner analytics on both insert and update branches
- `total_emails` double-count bug fixed — renamed + re-split RPC returns two independent aggregates
- Placeholder `step_number=0` in `get_coach_milestones` tech_setup CTE replaced with `step_number=4`
- Legacy 4 owner alert types (`student_inactive`, `student_dropoff`, `unreviewed_reports`, `coach_underperforming`) silently removed; badge count realigned to pruned feed

**Issues Deferred (tech debt, not blockers):**

- 13 live-environment UAT smoke checks across phases 61, 62, 63, 65 (policy-accepted via `feedback_batch_uat_end_of_milestone`)
- Nyquist VALIDATION.md backfill — 0 compliant, 1 partial (phase 61 draft), 4 missing (62, 63, 64, 65). Carry-over track.
- NOTIF-01 Tech/Email Setup activation is NOW shipped in Phase 62 (the v1.5 carry-over is effectively closed)
- Nyquist v1.5 backfill (phases 44-52) still carry-over to v1.9+

**Technical Debt Incurred:**

- Four pre-existing lint warnings remain across the codebase (not introduced in v1.8): `student/loading.tsx` unused import, `CalendarTab.tsx` unused modifiers, `WorkTrackerClient.tsx` useCallback deps, `Modal.tsx` useEffect dep
- No runtime smoke tests executed against deployed build for the 13 deferred UAT items (batched)

**Anti-Pattern Scan (clean):**

- Zero new TODO/FIXME/XXX/HACK/PLACEHOLDER across v1.8 phases
- No `as any` / `as unknown as` escape hatches near renamed types
- No empty catch blocks
- No hardcoded hex/gray tokens introduced
- New `SegmentedControl` primitive meets `min-h-[44px]`, `role="radiogroup"/radio"`, `aria-checked`, `motion-safe:transition-colors`

---

_For current project status, see .planning/ROADMAP.md_
