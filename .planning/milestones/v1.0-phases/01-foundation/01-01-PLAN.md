---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - tailwind.config.ts
  - eslint.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/utils.ts
  - .env.local.example
  - .gitignore
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "npm run dev starts without errors and the app loads at localhost:3000"
    - "TypeScript strict mode is enabled and npx tsc --noEmit passes"
    - "Tailwind v4 with ima-* design tokens is configured and usable in components"
    - "Inter font is loaded and applied to body"
  artifacts:
    - path: "package.json"
      provides: "All V1 dependencies at correct versions"
      contains: "next"
    - path: "tailwind.config.ts"
      provides: "ima-* color tokens (V1 only, no tier/brand/warm)"
      contains: "ima-primary"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 import with @config directive"
      contains: "@config"
    - path: "src/lib/utils.ts"
      provides: "cn() utility function"
      exports: ["cn"]
    - path: "CLAUDE.md"
      provides: "Project instructions for Claude Code"
      contains: "proxy.ts"
  key_links:
    - from: "src/app/globals.css"
      to: "tailwind.config.ts"
      via: "@config directive"
      pattern: '@config "../../tailwind.config.ts"'
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: './globals.css'
---

<objective>
Scaffold the Next.js 16 project with TypeScript strict, Tailwind v4, ima-* design tokens, ESLint, and all V1 dependencies installed. Creates the foundational project structure that all other plans build upon.

Purpose: No feature work can begin without a compiling project with correct dependencies and configuration.
Output: A working Next.js 16 app at localhost:3000 with Tailwind v4 + ima-* tokens, Inter font, and all V1 dependencies.
</objective>

<execution_context>
@C:/Users/ibrah/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ibrah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 16 project and install all V1 dependencies</name>
  <read_first>
    reference-old/package.json
  </read_first>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, eslint.config.mjs, .gitignore, .env.local.example</files>
  <action>
1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir` in the project root (C:/Users/ibrah/ima-accelerator-v1). If prompted about existing files, allow overwrite. The project root already has `reference-old/`, `supabase/`, `.planning/`, and `IMA-V1-REBUILD-PLAN.md` — these must NOT be deleted.

2. After scaffold completes, pin Next.js to 16.1.6 in package.json. Open package.json and ensure `"next": "16.1.6"` (not whatever @latest installed).

3. Install production dependencies:
```bash
npm install @supabase/ssr@^0.9.0 @supabase/supabase-js@^2.98.0 server-only@^0.0.1 class-variance-authority@^0.7.1 clsx@^2.1.1 tailwind-merge@^3.5.0 lucide-react@^0.576.0 motion@^12.35.2 zod@^4.3.6 date-fns@^4.1.0
```

4. Install dev dependencies:
```bash
npm install -D supabase@^2.76.16 @tailwindcss/postcss@^4
```

5. Ensure package.json scripts section contains:
```json
{
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "eslint"
}
```
Note: do NOT use `-p 3001` like the reference — V1 uses the default port 3000.

6. Ensure tsconfig.json has `"strict": true` and the `@/*` path alias pointing to `./src/*`:
```json
{
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

7. Create `.env.local.example` with the required env vars (NO actual secrets):
```
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
```

8. Ensure `.gitignore` includes `.env.local`, `.env*.local`, `node_modules/`, `.next/`, and does NOT ignore `reference-old/` or `.planning/`.

9. Ensure `postcss.config.mjs` uses the Tailwind v4 PostCSS plugin:
```js
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
export default config;
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"next": "16.1.6"`
    - package.json contains `"@supabase/ssr":`
    - package.json contains `"@supabase/supabase-js":`
    - package.json contains `"server-only":`
    - package.json contains `"class-variance-authority":`
    - package.json contains `"zod":`
    - package.json contains `"motion":`
    - package.json contains `"lucide-react":`
    - package.json contains `"date-fns":`
    - package.json contains `"supabase":` (devDependencies)
    - tsconfig.json contains `"strict": true`
    - tsconfig.json contains `"@/*"`
    - postcss.config.mjs contains `@tailwindcss/postcss`
    - .env.local.example contains `NEXT_PUBLIC_SUPABASE_URL`
    - .env.local.example contains `SUPABASE_SERVICE_ROLE_KEY`
    - .gitignore contains `.env.local`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Next.js 16.1.6 project compiles with all V1 dependencies installed, TypeScript strict enabled, and path aliases configured.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind v4 with ima-* tokens, Inter font, globals.css, utils.ts, root layout, and CLAUDE.md</name>
  <read_first>
    reference-old/tailwind.config.ts
    reference-old/src/app/globals.css
    reference-old/src/app/layout.tsx
    reference-old/CLAUDE.md
  </read_first>
  <files>tailwind.config.ts, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/lib/utils.ts, CLAUDE.md</files>
  <action>
1. **tailwind.config.ts** — Overwrite with V1-only ima-* tokens. Include ONLY these colors (no tier-*, brand-*, warm-* tokens):
```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        ima: {
          primary: "#2563EB",
          "primary-hover": "#1D4ED8",
          secondary: "#1E40AF",
          accent: "#3B82F6",
          success: "#10B981",
          warning: "#F59E0B",
          error: "#EF4444",
          info: "#3B82F6",
          bg: "#F8FAFC",
          surface: "#FFFFFF",
          "surface-light": "#F1F5F9",
          "surface-accent": "#EFF6FF",
          border: "#E2E8F0",
          text: "#1E293B",
          "text-secondary": "#64748B",
          "text-muted": "#94A3B8",
          overlay: "rgba(0, 0, 0, 0.5)",
        },
      },
      boxShadow: {
        "card-hover": "0 8px 25px -5px rgba(0,0,0,0.1)",
      },
      keyframes: {
        fadeIn: { "0%": { opacity: "0" }, "100%": { opacity: "1" } },
        scaleIn: {
          "0%": { opacity: "0", transform: "scale(0.95)" },
          "100%": { opacity: "1", transform: "scale(1)" },
        },
        slideUp: {
          "0%": { opacity: "0", transform: "translateY(16px)" },
          "100%": { opacity: "1", transform: "translateY(0)" },
        },
      },
      animation: {
        fadeIn: "fadeIn 300ms ease-out",
        scaleIn: "scaleIn 300ms ease-out",
        slideUp: "slideUp 400ms ease-out forwards",
      },
    },
  },
  plugins: [],
};

export default config;
```

2. **src/app/globals.css** — Overwrite with exactly:
```css
@import "tailwindcss";
@config "../../tailwind.config.ts";
```
Nothing else. No custom utility classes yet.

3. **src/lib/utils.ts** — Create with cn() utility:
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

4. **src/app/layout.tsx** — Overwrite with V1 root layout (no ToastProvider yet — that's a Phase 10 concern):
```typescript
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "IMA Accelerator",
  description: "Student performance & coaching management platform for Abu Lahya's halal influencer marketing mentorship program.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-ima-bg text-ima-text antialiased`}>
        {children}
      </body>
    </html>
  );
}
```

5. **src/app/page.tsx** — Replace with a minimal placeholder page that confirms the app works:
```typescript
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <div className="text-center">
        <h1 className="text-3xl font-bold text-ima-primary">IMA Accelerator</h1>
        <p className="mt-2 text-ima-text-secondary">Foundation setup complete</p>
      </div>
    </main>
  );
}
```

6. **CLAUDE.md** — Create V1-specific project instructions at project root:
```markdown
# IMA Accelerator V1

Student performance & coaching platform for Abu Lahya's halal influencer marketing mentorship.

## Stack

- Next.js 16 (App Router, proxy.ts NOT middleware.ts)
- React 19, TypeScript strict
- Supabase (auth + Postgres + RLS)
- Tailwind CSS 4 with ima-* design tokens
- Google OAuth only, invite-only registration

## Commands

```
npm run dev          # Dev server on port 3000
npm run build        # Production build
npm run lint         # ESLint
npx tsc --noEmit     # Type check (no emit)
```

## Architecture

```
src/app/(auth)/       # Login, register, no-access — no sidebar
src/app/(dashboard)/  # Owner, coach, student views — shares Sidebar layout
src/app/api/          # Route handlers (mutations only)
src/components/ui/    # CVA-based primitives
src/components/       # Feature components
src/lib/config.ts     # Single source of truth (roles, nav, roadmap)
src/lib/supabase/     # Client, server, admin clients
src/proxy.ts          # Route guard — NOT middleware.ts
```

## Database

6 tables: users, invites, magic_links, work_sessions, roadmap_progress, daily_reports. All have RLS. Roles: owner > coach > student.

## Critical Rules

1. **Config is truth** — import from src/lib/config.ts, never hardcode roles/nav/roadmap
2. **Admin client only in server code** — never import in client components
3. **Proxy not middleware** — Next.js 16 uses src/proxy.ts
4. **Google OAuth only** — no password flows exist
5. **Light theme with blue accents** — all UI uses ima-* tokens

## Hard Rules (enforce during EVERY build)

1. **motion-safe:** — every `animate-*` class MUST use `motion-safe:animate-*`
2. **44px touch targets** — every interactive element needs `min-h-[44px]`
3. **Accessible labels** — every input needs `aria-label` or `<label>` with `htmlFor`+`id`
4. **Admin client in API routes** — every `.from()` query in route handlers uses the admin client
5. **Never swallow errors** — every `catch` block must toast or `console.error`, never empty
6. **Check response.ok** — every `fetch()` must check `response.ok` before parsing JSON
7. **Zod import** — `import { z } from "zod"`, never `"zod/v4"`
8. **ima-* tokens only** — all colors use ima-* design tokens, never hardcoded hex/gray

## Code Quality

- ima-* color tokens everywhere (text-white only on colored backgrounds like buttons/avatars)
- 44px min touch targets (h-11, min-h-[44px] min-w-[44px])
- ARIA on dynamic content (role="progressbar", role="timer", role="alert")
- aria-hidden="true" on decorative icons
- Zod safeParse on all API inputs, try-catch on request.json()
- Auth + role check before validation on every API route
- Filter by user ID in queries, never rely on RLS alone
- px-4 on all page wrappers for mobile
- Stable useCallback deps — use refs for toast/router
```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.ts contains `"ima-primary": "#2563EB"`
    - tailwind.config.ts contains `"ima-bg": "#F8FAFC"`
    - tailwind.config.ts contains `"ima-surface-accent": "#EFF6FF"`
    - tailwind.config.ts does NOT contain `tier-bronze`
    - tailwind.config.ts does NOT contain `brand-gold`
    - tailwind.config.ts does NOT contain `warm-50`
    - src/app/globals.css contains `@import "tailwindcss"`
    - src/app/globals.css contains `@config "../../tailwind.config.ts"`
    - src/lib/utils.ts contains `export function cn(`
    - src/lib/utils.ts contains `twMerge`
    - src/app/layout.tsx contains `Inter`
    - src/app/layout.tsx contains `bg-ima-bg`
    - src/app/layout.tsx contains `IMA Accelerator`
    - src/app/page.tsx contains `text-ima-primary`
    - CLAUDE.md contains `proxy.ts NOT middleware.ts`
    - CLAUDE.md contains `motion-safe:`
    - CLAUDE.md contains `44px touch targets`
    - `npx tsc --noEmit` exits with code 0
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Tailwind v4 configured with V1 ima-* tokens only, Inter font loaded, cn() utility created, root layout renders, CLAUDE.md provides project instructions. App compiles and builds successfully.</done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors and the app loads at localhost:3000
- `npx tsc --noEmit` passes with no type errors
- `npm run build` completes successfully
- tailwind.config.ts has all 17 ima-* color tokens and no V2 tokens
- Inter font renders in the browser
</verification>

<success_criteria>
- Next.js 16.1.6 project with all V1 dependencies installed at correct versions
- TypeScript strict mode enabled, zero type errors
- Tailwind v4 with ima-* tokens configured via @config directive
- cn() utility in src/lib/utils.ts
- Root layout with Inter font and ima-bg background
- CLAUDE.md with hard rules and project instructions
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
