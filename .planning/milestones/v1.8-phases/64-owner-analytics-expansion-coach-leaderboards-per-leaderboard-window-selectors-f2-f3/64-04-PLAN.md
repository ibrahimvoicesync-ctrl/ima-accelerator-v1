---
phase: 64
plan: "04"
title: "Owner analytics page refactor — SSR payload + 6 independent window toggles (zero client re-fetch)"
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - src/app/(dashboard)/owner/analytics/page.tsx
  - src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx
requirements: [OA-01, OA-02, OA-06, OA-08, WS-01, WS-05, WS-06, WS-07, WS-08]
autonomous: true
must_haves:
  - /owner/analytics renders exactly 6 LeaderboardCards — 3 students + 3 coaches
  - Each of the 6 cards carries its own SegmentedControl (Weekly / Monthly / Yearly / All Time)
  - Server fetches full 24-slot payload ONCE via getOwnerAnalyticsCached()
  - Client receives all 24 slots as a prop and swaps the rendered slice on toggle — zero fetch(), zero useEffect re-fetch
  - `rg -n "fetch\\(" src/app/\\(dashboard\\)/owner/analytics/` returns 0 hits (excluding next/cache internals)
  - Default window on initial render = "alltime" for every leaderboard
  - Student rows link to /owner/students/[id]; coach rows are non-linked (linkRows=false)
---

<objective>
Replace the current 3-leaderboard server component with a server page that fetches the full 24-slot payload and passes it to a new client component `<OwnerAnalyticsClient>`. The client holds 6 independent window states and swaps the pre-computed slice per card. No client re-fetch on toggle.
</objective>

<read_first>
- src/app/(dashboard)/owner/analytics/page.tsx (current 3-card implementation)
- src/lib/rpc/owner-analytics.ts (after Plan 01 — new v2 shape)
- src/lib/rpc/owner-analytics-types.ts (after Plan 01 — new types)
- src/components/analytics/LeaderboardCard.tsx (after Plan 03 — linkRows prop)
- src/components/ui/SegmentedControl.tsx (from Plan 02)
- src/components/ui/Card.tsx
- src/components/ui/EmptyState.tsx
</read_first>

<tasks>
<task id="t1">
<action>
Create `src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx` (client component):

```tsx
"use client";

import { useState } from "react";
import { BarChart3 } from "lucide-react";
import { Card, CardContent } from "@/components/ui/Card";
import { EmptyState } from "@/components/ui/EmptyState";
import {
  LeaderboardCard,
  type LeaderboardRow,
} from "@/components/analytics/LeaderboardCard";
import { SegmentedControl } from "@/components/ui/SegmentedControl";
import type {
  OwnerAnalyticsPayload,
  OwnerAnalyticsWindow,
} from "@/lib/rpc/owner-analytics-types";

const WINDOW_OPTIONS: { value: OwnerAnalyticsWindow; label: string }[] = [
  { value: "weekly",  label: "Weekly" },
  { value: "monthly", label: "Monthly" },
  { value: "yearly",  label: "Yearly" },
  { value: "alltime", label: "All Time" },
];

function mapStudentRows(
  rows: { rank: number; student_id: string; student_name: string; metric_display: string }[],
): LeaderboardRow[] {
  return rows.map((r) => ({
    rank: r.rank,
    student_id: r.student_id,
    student_name: r.student_name,
    metric_display: r.metric_display,
  }));
}

function mapCoachRows(
  rows: { rank: number; coach_id: string; coach_name: string; metric_display: string }[],
): LeaderboardRow[] {
  // LeaderboardRow has historical student_* field names — coach id/name pass through.
  return rows.map((r) => ({
    rank: r.rank,
    student_id: r.coach_id,
    student_name: r.coach_name,
    metric_display: r.metric_display,
  }));
}

interface Props {
  payload: OwnerAnalyticsPayload;
}

export function OwnerAnalyticsClient({ payload }: Props) {
  // Six independent window states — one per leaderboard.
  const [studentHoursWin,   setStudentHoursWin]   = useState<OwnerAnalyticsWindow>("alltime");
  const [studentProfitWin,  setStudentProfitWin]  = useState<OwnerAnalyticsWindow>("alltime");
  const [studentDealsWin,   setStudentDealsWin]   = useState<OwnerAnalyticsWindow>("alltime");
  const [coachRevenueWin,   setCoachRevenueWin]   = useState<OwnerAnalyticsWindow>("alltime");
  const [coachOutreachWin,  setCoachOutreachWin]  = useState<OwnerAnalyticsWindow>("alltime");
  const [coachDealsWin,     setCoachDealsWin]     = useState<OwnerAnalyticsWindow>("alltime");

  const studentHours   = mapStudentRows(payload.leaderboards.students.hours[studentHoursWin]);
  const studentProfit  = mapStudentRows(payload.leaderboards.students.profit[studentProfitWin]);
  const studentDeals   = mapStudentRows(payload.leaderboards.students.deals[studentDealsWin]);
  const coachRevenue   = mapCoachRows(payload.leaderboards.coaches.revenue[coachRevenueWin]);
  const coachOutreach  = mapCoachRows(payload.leaderboards.coaches.avg_total_outreach[coachOutreachWin]);
  const coachDeals     = mapCoachRows(payload.leaderboards.coaches.deals[coachDealsWin]);

  // Empty-state check across ALL 24 slots (not just current window) — if no window
  // has any data across any leaderboard, show the global empty state.
  const anyData = Object.values(payload.leaderboards.students).some((m) =>
    Object.values(m).some((arr) => (arr as unknown[]).length > 0),
  ) || Object.values(payload.leaderboards.coaches).some((m) =>
    Object.values(m).some((arr) => (arr as unknown[]).length > 0),
  );

  if (!anyData) {
    return (
      <div className="mt-6">
        <Card>
          <CardContent className="p-6">
            <EmptyState
              icon={<BarChart3 className="h-6 w-6" aria-hidden="true" />}
              title="No activity yet"
              description="Leaderboards will appear once students log hours, close deals, or earn profit."
            />
          </CardContent>
        </Card>
      </div>
    );
  }

  return (
    <div className="mt-6 space-y-8">
      {/* Students section */}
      <section aria-labelledby="owner-lb-students-h2">
        <h2
          id="owner-lb-students-h2"
          className="text-lg font-semibold text-ima-text mb-3"
        >
          Students
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
          <LeaderboardWithToggle
            headingId="owner-lb-hours"
            heading="Top 3 Students by Hours Worked"
            subheading="Completed work sessions"
            rows={studentHours}
            value={studentHoursWin}
            onChange={setStudentHoursWin}
            controlLabel="Hours time range"
            emptyHeading="No hours logged yet"
            emptyBody="Students appear here once they complete a work session."
            hrefPrefix="/owner/students/"
          />
          <LeaderboardWithToggle
            headingId="owner-lb-profit"
            heading="Top 3 Students by Profit Earned"
            subheading="All closed deals"
            rows={studentProfit}
            value={studentProfitWin}
            onChange={setStudentProfitWin}
            controlLabel="Profit time range"
            emptyHeading="No profit recorded yet"
            emptyBody="Students appear here once they log a deal with profit."
            hrefPrefix="/owner/students/"
          />
          <LeaderboardWithToggle
            headingId="owner-lb-deals"
            heading="Top 3 Students by Deals Closed"
            subheading="Count of closed deals"
            rows={studentDeals}
            value={studentDealsWin}
            onChange={setStudentDealsWin}
            controlLabel="Deals time range"
            emptyHeading="No deals closed yet"
            emptyBody="Students appear here once they log a deal."
            hrefPrefix="/owner/students/"
          />
        </div>
      </section>

      {/* Coaches section */}
      <section aria-labelledby="owner-lb-coaches-h2">
        <h2
          id="owner-lb-coaches-h2"
          className="text-lg font-semibold text-ima-text mb-3"
        >
          Coaches
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
          <LeaderboardWithToggle
            headingId="owner-lb-coach-revenue"
            heading="Top 3 Coaches by Revenue"
            subheading="Sum of deal profit from assigned students"
            rows={coachRevenue}
            value={coachRevenueWin}
            onChange={setCoachRevenueWin}
            controlLabel="Coach revenue time range"
            emptyHeading="No coach revenue yet"
            emptyBody="Coaches appear here once their assigned students close deals."
            linkRows={false}
          />
          <LeaderboardWithToggle
            headingId="owner-lb-coach-outreach"
            heading="Top 3 Coaches by Avg Outreach"
            subheading="Brands + influencers per student per day"
            rows={coachOutreach}
            value={coachOutreachWin}
            onChange={setCoachOutreachWin}
            controlLabel="Coach outreach time range"
            emptyHeading="No outreach data yet"
            emptyBody="Coaches appear here once their assigned students log reports."
            linkRows={false}
          />
          <LeaderboardWithToggle
            headingId="owner-lb-coach-deals"
            heading="Top 3 Coaches by Deals"
            subheading="Total deals across assigned students"
            rows={coachDeals}
            value={coachDealsWin}
            onChange={setCoachDealsWin}
            controlLabel="Coach deals time range"
            emptyHeading="No coach deals yet"
            emptyBody="Coaches appear here once their assigned students close deals."
            linkRows={false}
          />
        </div>
      </section>
    </div>
  );
}

interface LeaderboardWithToggleProps {
  headingId: string;
  heading: string;
  subheading: string;
  rows: LeaderboardRow[];
  value: OwnerAnalyticsWindow;
  onChange: (v: OwnerAnalyticsWindow) => void;
  controlLabel: string;
  emptyHeading: string;
  emptyBody: string;
  hrefPrefix?: string;
  linkRows?: boolean;
}

function LeaderboardWithToggle({
  headingId, heading, subheading, rows, value, onChange, controlLabel,
  emptyHeading, emptyBody, hrefPrefix, linkRows,
}: LeaderboardWithToggleProps) {
  return (
    <div className="space-y-2">
      <SegmentedControl<OwnerAnalyticsWindow>
        options={WINDOW_OPTIONS}
        value={value}
        onChange={onChange}
        ariaLabel={controlLabel}
      />
      <LeaderboardCard
        headingId={headingId}
        heading={heading}
        subheading={subheading}
        rows={rows}
        emptyHeading={emptyHeading}
        emptyBody={emptyBody}
        hrefPrefix={hrefPrefix}
        linkRows={linkRows}
      />
    </div>
  );
}
```

Hard Rules verification:
- No `fetch(` calls anywhere in this file.
- No `useEffect` — state changes trigger re-render from already-delivered props only.
- `min-h-[44px]`, `motion-safe:`, aria labels all inherited from SegmentedControl + LeaderboardCard primitives.
- ima-* tokens only (`text-ima-text`).
</action>
<read_first>
- src/lib/rpc/owner-analytics-types.ts
- src/components/ui/SegmentedControl.tsx
- src/components/analytics/LeaderboardCard.tsx
</read_first>
<acceptance_criteria>
- File `src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx` exists
- `grep -c "\"use client\"" src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx` returns 1
- `grep -c "fetch(" src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx` returns 0
- `grep -c "useEffect" src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx` returns 0
- `grep -c "SegmentedControl" src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx` returns at least 2
- `grep -c "LeaderboardCard" src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx` returns at least 2
- `grep -c "useState" src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx` returns at least 1
- `grep -c "linkRows={false}" src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx` returns 3 (three coach cards)
- `grep -c "\"alltime\"" src/app/(dashboard)/owner/analytics/OwnerAnalyticsClient.tsx` returns at least 6 (default for each of 6 window states)
</acceptance_criteria>
</task>

<task id="t2">
<action>
Rewrite `src/app/(dashboard)/owner/analytics/page.tsx` to be a thin server component that fetches the payload once and delegates to the client:

```tsx
/**
 * Phase 64: Owner Analytics page — server fetches full 24-slot payload from the
 * expanded get_owner_analytics RPC (migration 00035) and hands it to the client
 * component which owns 6 independent Weekly/Monthly/Yearly/All Time window
 * toggles. Client swaps pre-computed slices — NO client re-fetch on toggle.
 *
 * Cache: unstable_cache(60s, key=["owner-analytics-v2"], tag=ownerAnalyticsTag()).
 * Cache invalidated by mutations in /api/deals, /api/deals/[id],
 * /api/work-sessions/[id], and /api/reports (wired in Plan 05 — closes v1.6
 * ownerAnalyticsTag deferred item).
 */

import { requireRole } from "@/lib/session";
import { getOwnerAnalyticsCached } from "@/lib/rpc/owner-analytics";
import { OwnerAnalyticsClient } from "./OwnerAnalyticsClient";

// Align route-level revalidation with the RPC cache TTL.
export const revalidate = 60;

export default async function OwnerAnalyticsPage() {
  await requireRole("owner");

  const payload = await getOwnerAnalyticsCached();

  return (
    <section
      aria-labelledby="owner-analytics-h1"
      className="px-4 py-6 max-w-7xl mx-auto"
    >
      <h1
        id="owner-analytics-h1"
        className="text-2xl font-bold text-ima-text"
      >
        Owner Analytics
      </h1>
      <p className="mt-1 text-sm text-ima-text-secondary">
        Leaderboards across students and coaches — toggle each card independently.
      </p>

      <OwnerAnalyticsClient payload={payload} />
    </section>
  );
}
```
</action>
<read_first>
- src/app/(dashboard)/owner/analytics/page.tsx (current version)
- src/lib/rpc/owner-analytics.ts
</read_first>
<acceptance_criteria>
- `grep -c "OwnerAnalyticsClient" src/app/(dashboard)/owner/analytics/page.tsx` returns at least 2 (import + usage)
- `grep -c "getOwnerAnalyticsCached" src/app/(dashboard)/owner/analytics/page.tsx` returns at least 2
- `grep -c "requireRole(\"owner\")" src/app/(dashboard)/owner/analytics/page.tsx` returns 1
- `grep -c "fetch(" src/app/(dashboard)/owner/analytics/page.tsx` returns 0
- `grep -c "use client" src/app/(dashboard)/owner/analytics/page.tsx` returns 0 (still server component)
- `grep -c "export const revalidate = 60" src/app/(dashboard)/owner/analytics/page.tsx` returns 1
</acceptance_criteria>
</task>
</tasks>

<verification>
- Visual: /owner/analytics shows 6 cards in 2 sections (students on top, coaches below).
- Each card has its own SegmentedControl above it.
- Clicking a segment instantly swaps which slice renders; no network request fires.
- Zero `fetch(` hits in the analytics directory.
- Coach rows are non-linked (no `<a>` tags in those `<li>`).
</verification>
