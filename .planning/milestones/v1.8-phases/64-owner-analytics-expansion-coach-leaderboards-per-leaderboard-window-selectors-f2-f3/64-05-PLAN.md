---
phase: 64
plan: "05"
title: "Close v1.6 defer — wire ownerAnalyticsTag() into /api/reports"
wave: 1
depends_on: []
files_modified:
  - src/app/api/reports/route.ts
requirements: [OA-04]
autonomous: true
must_haves:
  - revalidateTag(ownerAnalyticsTag(), "default") is called on BOTH branches (update-existing-row AND insert-new-row)
  - Import of ownerAnalyticsTag is added from @/lib/rpc/owner-analytics-types
  - Each revalidateTag call is wrapped in try/catch with console.error (never swallow errors)
  - Existing invalidations for badges, student analytics, coach dashboard/analytics/milestones remain intact
---

<objective>
Close the v1.6 deferred "ownerAnalyticsTag invariant" by wiring `revalidateTag(ownerAnalyticsTag(), "default")` into both branches of `POST /api/reports`. This ensures the coach avg-total-outreach leaderboard never goes stale after a daily report mutation.
</objective>

<read_first>
- src/app/api/reports/route.ts (full file — identify both mutation branches)
- src/lib/rpc/owner-analytics-types.ts (after Plan 01 — export of ownerAnalyticsTag)
- src/app/api/deals/route.ts (reference: how ownerAnalyticsTag is invoked elsewhere)
- .planning/v16-AUTONOMOUS-CHECKPOINT.md (v1.6 defer context)
</read_first>

<tasks>
<task id="t1">
<action>
Modify `src/app/api/reports/route.ts`:

1. Add import near the other `@/lib/rpc/*` imports (top of file):

```ts
import { ownerAnalyticsTag } from "@/lib/rpc/owner-analytics-types";
```

2. In the **update-existing-row branch** (after the existing `revalidateTag(coachMilestonesTag(...))` block, still inside the `if (studentRow?.coach_id)` parent try/catch is fine OR add a separate try/catch — prefer a separate try/catch so owner invalidation does NOT depend on the coach-id lookup succeeding):

Place this block AFTER the existing `try { revalidateTag(studentAnalyticsTag(profile.id), ...) } catch {...}` block but BEFORE the coach-dashboard lookup:

```ts
try {
  revalidateTag(ownerAnalyticsTag(), "default");
} catch (e) {
  console.error("[revalidate-tag owner-analytics]", e);
}
```

3. Repeat the exact same block in the **insert-new-row branch** — after the `try { revalidateTag(studentAnalyticsTag(...)) } catch {...}` block and before the coach-id lookup.

Both branches must have the call. The owner analytics leaderboards are a single global cache — they invalidate on ANY student's report mutation regardless of coach assignment.

Keep all existing invalidations (`badges`, `studentAnalyticsTag`, `coachDashboardTag`, `coachAnalyticsTag`, `coachMilestonesTag`) untouched.
</action>
<read_first>
- src/app/api/reports/route.ts
- src/lib/rpc/owner-analytics-types.ts
</read_first>
<acceptance_criteria>
- `grep -c "ownerAnalyticsTag" src/app/api/reports/route.ts` returns at least 3 (1 import + 2 call sites)
- `grep -c "revalidateTag(ownerAnalyticsTag()" src/app/api/reports/route.ts` returns 2
- `grep -c "from \"@/lib/rpc/owner-analytics-types\"" src/app/api/reports/route.ts` returns 1
- `grep -c "\\[revalidate-tag owner-analytics\\]" src/app/api/reports/route.ts` returns 2
- `grep -c "studentAnalyticsTag" src/app/api/reports/route.ts` returns at least 2 (existing, not removed)
- `grep -c "coachDashboardTag" src/app/api/reports/route.ts` returns at least 2 (existing, not removed)
</acceptance_criteria>
</task>
</tasks>

<verification>
- The ownerAnalyticsTag invariant flagged by v1.6 audit is now closed.
- A student submitting/editing a daily report triggers revalidation of the owner analytics cache, so coach avg-outreach leaderboards stay fresh within the 60s TTL.
- Error handling honors Hard Rule #5 (console.error, never silent).
</verification>
