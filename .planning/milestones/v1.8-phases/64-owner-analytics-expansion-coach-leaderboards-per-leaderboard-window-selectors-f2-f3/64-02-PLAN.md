---
phase: 64
plan: "02"
title: "SegmentedControl UI primitive"
wave: 1
depends_on: []
files_modified:
  - src/components/ui/SegmentedControl.tsx
requirements: [WS-05, WS-06, WS-07, WS-08]
autonomous: true
must_haves:
  - New file src/components/ui/SegmentedControl.tsx exports a client component
  - role="radiogroup" + role="radio" + aria-checked per segment
  - Arrow-key navigation (ArrowLeft/ArrowRight cycles focused segment and fires onChange)
  - Every segment has min-h-[44px]
  - Uses ima-* tokens only, motion-safe transitions
  - Controlled via value/onChange props
---

<objective>
Create the `SegmentedControl` primitive that powers the six Weekly/Monthly/Yearly/All Time window toggles on the owner analytics page. Must meet Hard Rule 2 (44px touch targets), be keyboard-navigable (arrow keys), and use ima-* tokens only.
</objective>

<read_first>
- src/components/ui/Button.tsx (CVA pattern, 44px sizing, focus-ring, ima-* tokens)
- src/components/ui/Card.tsx (CVA pattern reference)
- src/lib/utils.ts (cn helper)
- .planning/phases/64-*/64-RESEARCH.md (section E)
</read_first>

<tasks>
<task id="t1">
<action>
Create `src/components/ui/SegmentedControl.tsx`:

```tsx
"use client";

import { useCallback, useRef, type KeyboardEvent } from "react";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "@/lib/utils";

const segmentVariants = cva(
  "inline-flex items-center justify-center px-3 text-xs font-medium min-h-[44px] rounded-md motion-safe:transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ima-primary focus-visible:ring-offset-1",
  {
    variants: {
      active: {
        true: "bg-ima-primary text-white",
        false: "bg-transparent text-ima-text-secondary hover:bg-ima-surface-light hover:text-ima-text",
      },
    },
    defaultVariants: { active: false },
  },
);

export type SegmentedControlOption<T extends string = string> = {
  value: T;
  label: string;
};

export interface SegmentedControlProps<T extends string = string>
  extends VariantProps<typeof segmentVariants> {
  options: SegmentedControlOption<T>[];
  value: T;
  onChange: (next: T) => void;
  ariaLabel: string;
  className?: string;
}

export function SegmentedControl<T extends string = string>({
  options,
  value,
  onChange,
  ariaLabel,
  className,
}: SegmentedControlProps<T>) {
  const listRef = useRef<HTMLDivElement>(null);

  const handleKeyDown = useCallback(
    (event: KeyboardEvent<HTMLButtonElement>, index: number) => {
      if (event.key !== "ArrowLeft" && event.key !== "ArrowRight") return;
      event.preventDefault();
      const delta = event.key === "ArrowRight" ? 1 : -1;
      const nextIndex = (index + delta + options.length) % options.length;
      const nextOpt = options[nextIndex]!;
      onChange(nextOpt.value);
      // Move focus to the new active segment so keyboard nav feels native.
      const buttons =
        listRef.current?.querySelectorAll<HTMLButtonElement>(
          'button[role="radio"]',
        );
      buttons?.[nextIndex]?.focus();
    },
    [options, onChange],
  );

  return (
    <div
      ref={listRef}
      role="radiogroup"
      aria-label={ariaLabel}
      className={cn(
        "inline-flex items-center gap-1 p-1 rounded-lg border border-ima-border bg-ima-surface",
        className,
      )}
    >
      {options.map((opt, idx) => {
        const isActive = opt.value === value;
        return (
          <button
            key={opt.value}
            type="button"
            role="radio"
            aria-checked={isActive}
            tabIndex={isActive ? 0 : -1}
            onClick={() => onChange(opt.value)}
            onKeyDown={(e) => handleKeyDown(e, idx)}
            className={cn(segmentVariants({ active: isActive }))}
          >
            {opt.label}
          </button>
        );
      })}
    </div>
  );
}
```

Key decisions:
- `"use client"` directive — this is a client primitive (handles onChange).
- Outer container `role="radiogroup"` with `aria-label`.
- Each segment is a `<button role="radio" aria-checked>` with `tabIndex={isActive ? 0 : -1}` — standard radiogroup focus pattern.
- Arrow keys cycle through options and fire onChange.
- `min-h-[44px]` on every segment (Hard Rule 2).
- `motion-safe:transition-colors` (Hard Rule 1).
- All colors are ima-* tokens: `ima-primary`, `ima-border`, `ima-surface`, `ima-surface-light`, `ima-text`, `ima-text-secondary`. `text-white` on active only (colored bg — allowed per CLAUDE.md).
</action>
<read_first>
- src/components/ui/Button.tsx
- src/lib/utils.ts
</read_first>
<acceptance_criteria>
- File `src/components/ui/SegmentedControl.tsx` exists
- `grep -c "\"use client\"" src/components/ui/SegmentedControl.tsx` returns 1
- `grep -c "role=\"radiogroup\"" src/components/ui/SegmentedControl.tsx` returns 1
- `grep -c "role=\"radio\"" src/components/ui/SegmentedControl.tsx` returns 1
- `grep -c "aria-checked" src/components/ui/SegmentedControl.tsx` returns at least 1
- `grep -c "min-h-\\[44px\\]" src/components/ui/SegmentedControl.tsx` returns at least 1
- `grep -c "motion-safe:" src/components/ui/SegmentedControl.tsx` returns at least 1
- `grep -c "ArrowLeft\\|ArrowRight" src/components/ui/SegmentedControl.tsx` returns at least 1
- `grep -c "ima-primary\\|ima-border\\|ima-surface\\|ima-text" src/components/ui/SegmentedControl.tsx` returns at least 4
- No hardcoded hex colors: `grep -Ec "#[0-9a-fA-F]{3,6}" src/components/ui/SegmentedControl.tsx` returns 0
</acceptance_criteria>
</task>
</tasks>

<verification>
- File exists, type-checks, exports `SegmentedControl` + `SegmentedControlOption<T>`.
- Accessibility: radiogroup + radio + aria-checked + arrow-key nav.
- 44px touch targets + ima-* tokens + motion-safe.
</verification>
