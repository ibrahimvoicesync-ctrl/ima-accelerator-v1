# Milestone v1.6 - Integration Check

Scope: Phases 54, 55, 56, 57
Date: 2026-04-15
Checker: integration-checker sub-agent

## Summary

Score: 7/7 critical cross-phase contracts PASS

All wiring between Phase 54 (Owner Analytics), Phase 55 (Chat Removal + Announcements Migration), Phase 56 (Announcements CRUD), and Phase 57 (Roadmap Step 8 Insertion) is intact. No broken E2E flows. One cosmetic stale comment (non-functional) noted under Minor Findings.

---

## Contract 1 - Phase 55 migration 00029 to Phase 56 API routes [PASS]

- 00029_chat_removal_announcements.sql creates public.announcements with columns id, author_id, content, created_at, updated_at; FK announcements_author_id_fkey to users.id; BEFORE UPDATE trigger announcements_set_updated_at; 4 RLS policies; index on created_at DESC.
- src/app/api/announcements/route.ts (POST, GET) and src/app/api/announcements/[id]/route.ts (PATCH, DELETE) all query admin.from announcements table and use the joined author alias users!announcements_author_id_fkey exactly matching the FK name.
- src/components/announcements/announcement-types.ts Announcement shape matches toAnnouncementPayload in both route files.
- Admin client used exclusively in API route handlers (CLAUDE.md Hard Rule 4).

Requirements wired: ANNOUNCE-01 through 07, 10, 11, 12.

## Contract 2 - Phase 55 nav removal to Phase 56 Announcements nav + Sidebar icon map [PASS]

- src/lib/config.ts NAVIGATION has Announcements entries with icon Megaphone for all four roles: owner (line 294), coach (305), student (314), student_diy (323). No Chat nav items remain.
- src/components/layout/Sidebar.tsx ICON_MAP (lines 34-54) includes Megaphone (import line 25, map line 53).
- No /api/chat routes, no /[role]/chat pages, no .from messages queries anywhere in src.
- Legacy src/lib/chat-utils.ts is intentionally retained and reused by AnnouncementCard for formatRelativeTime (documented in file header).

Requirements wired: CHAT-REM-01 through CHAT-REM-08, ANNOUNCE-08.

## Contract 3 - Phase 54 ownerAnalyticsTag cache invalidation unchanged by Phase 57 [PASS]

revalidateTag(ownerAnalyticsTag(), default) calls survive intact in:
- src/app/api/work-sessions/[id]/route.ts:168
- src/app/api/deals/route.ts:207, 242
- src/app/api/deals/[id]/route.ts:131, 245

Phase 57 modified src/app/api/roadmap/route.ts (cache tag calls for student analytics, coach dashboard, coach analytics, coach milestones) but did not touch owner-analytics wiring. No regression.

Requirements wired: OA-01 through OA-06, PERF-04.

## Contract 4 - Phase 57 step literals agree across config.ts + migration 00030 [PASS]

- src/lib/config.ts MILESTONE_CONFIG.influencersClosedStep = 12 (line 396), brandResponseStep = 14 (line 402).
- supabase/migrations/00030_roadmap_step_8_insertion.sql get_coach_milestones CTE filters rp.step_number = 12 (line 123) for 5_influencers and rp.step_number = 14 (line 138) for brand_response, both with SYNC comments pointing back to config.
- ROADMAP_STEPS[11].step === 12 (Close 5 Influencers) and ROADMAP_STEPS[13].step === 14 (Get Brand Response) verified in config.

Requirements wired: ROADMAP-02, ROADMAP-03, ROADMAP-06, ROADMAP-07.

## Contract 5 - ROADMAP_STEPS.length (=16) used everywhere [PASS]

Zero hardcoded /15 or of-15 denominators found in src. Every progress-bar denominator, aria-valuemax, Zod max(...), skeleton loader length, and completion check uses ROADMAP_STEPS.length:
- src/components/coach/StudentCard.tsx:80
- src/components/coach/RoadmapTab.tsx:79, 94, 101, 102
- src/app/api/roadmap/route.ts:15, 103
- src/app/api/roadmap/undo/route.ts:11, 108
- src/app/(dashboard)/student/page.tsx:109, 110, 280, 297, 308
- src/app/(dashboard)/student/roadmap/page.tsx:39, 116, 117, 124, 138, 146, 147, 159
- src/app/(dashboard)/student/roadmap/loading.tsx:30
- src/app/(dashboard)/student_diy/page.tsx:81, 86, 136, 153, 164
- src/app/(dashboard)/student_diy/roadmap/page.tsx:39, 116, 117, 124, 138, 146, 147, 159

Matches for /15 that did appear in grep were Tailwind opacity modifiers (bg-ima-primary/15) - false positive, not denominators.

Requirements wired: ROADMAP-05, ROADMAP-06.

## Contract 6 - E2E flow: announcements CRUD [COMPLETE]

1. Owner opens /owner/announcements: src/app/(dashboard)/owner/announcements/page.tsx renders AnnouncementsPage with role=owner.
2. AnnouncementsPage (server) calls requireRole(owner), admin-fetches first 25 rows joined with author, hands to AnnouncementsFeed with currentUserId, role, initialItems, initialHasMore.
3. Owner clicks New Announcement: AnnouncementForm mode=create POSTs /api/announcements with content; 201 response updates feed via handleCreateSuccess.
4. All four role pages (owner, coach, student, student_diy) mount AnnouncementsPage and see the same data via admin client + same sort. Role gating (canAuthor, canMutate) read from prop.
5. Coach clicks pencil: AnnouncementForm mode=edit PATCHes /api/announcements/[id]; server updates row; BEFORE UPDATE trigger bumps updated_at; toAnnouncementPayload computes is_edited with 2000ms skew tolerance; client shows edited indicator via AnnouncementCard.
6. Owner clicks trash: DeleteAnnouncementDialog DELETEs /api/announcements/[id]; row removed from state via handleDeleted.

All 4 role pages load AnnouncementsPage with correct role string. Form + delete dialog are wired to correct API verbs and paths. No dangling references.

Requirements wired: ANNOUNCE-02 through ANNOUNCE-12.

## Contract 7 - E2E flow: roadmap Step 8 insertion [COMPLETE]

1. Migration 00030 drops roadmap_progress_step_number_check, two-pass renumbers 8-15 to 9-16 (via 108-115 parking range to dodge the UNIQUE(student_id, step_number) collision), auto-completes new Step 8 for every student who completed old Step 7, re-adds CHECK BETWEEN 1 AND 16, asserts MAX=16 and zero duplicates.
2. ROADMAP_STEPS[7] in config is now step 8, stage 1, title Join at least one Influencer Q-and-A session (CPM + pricing), target_days 5 - matching the migration INSERT verbatim.
3. Student /student/roadmap/page.tsx backfills missing rows up to ROADMAP_STEPS.length (now 16) and renders the full list including the new Step 8.
4. src/app/api/roadmap/route.ts PATCH accepts step_number in [1, ROADMAP_STEPS.length] = [1, 16], correctly unlocks step 9 when step 8 is completed.
5. get_coach_milestones RPC matches config influencersClosedStep=12 and brandResponseStep=14 for coach milestone alerts.

Requirements wired: ROADMAP-01 through ROADMAP-09.

---

## Requirements Integration Map

| Requirement | Integration Path | Status |
|-------------|-----------------|--------|
| OA-01..OA-06, PERF-04 | Phase 54 ownerAnalyticsTag wired into deals + work-session API revalidation; Phase 57 leaves intact | WIRED |
| CHAT-REM-01..08 | Phase 55 config.ts nav scrub + migration 00029 DROP messages; no residuals | WIRED |
| ANNOUNCE-01 | Phase 55 migration 00029 CREATE announcements + RLS | WIRED |
| ANNOUNCE-02..04 | API routes POST/PATCH/DELETE with owner+coach role check, NO ownership filter on edit/delete | WIRED |
| ANNOUNCE-05..07 | GET pagination (page=N, size=25), author join, edit-aware is_edited with 2000ms tolerance | WIRED |
| ANNOUNCE-08 | NAVIGATION entries + Sidebar ICON_MAP Megaphone for all four roles | WIRED |
| ANNOUNCE-09..10 | AnnouncementsPage (SSR first page) + AnnouncementsFeed (paginated loadMore via GET) | WIRED |
| ANNOUNCE-11 | checkRateLimit on POST/PATCH/DELETE; 429 with Retry-After header | WIRED |
| ANNOUNCE-12 | Zod content min 1 max 2000 on both POST and PATCH schemas | WIRED |
| ROADMAP-01 | Migration 00030 two-pass renumber + INSERT new Step 8 | WIRED |
| ROADMAP-02..03 | MILESTONE_CONFIG 12/14 SYNC with get_coach_milestones RPC CTEs | WIRED |
| ROADMAP-04 | /api/roadmap PATCH accepts any step in [1..16]; unlock-next-step logic uses ROADMAP_STEPS.length | WIRED |
| ROADMAP-05..06 | Progress UI uses ROADMAP_STEPS.length everywhere; zero /15 denominators remain | WIRED |
| ROADMAP-07 | Migration 00030 auto-completes Step 8 for existing step-7 completers; ON CONFLICT DO NOTHING idempotent | WIRED |
| ROADMAP-08..09 | CHECK constraint swap 1..15 to 1..16; embedded ASSERT block guards MAX and duplicates | WIRED |

No requirement is left UNWIRED or PARTIAL.

---

## Minor Findings (non-blocking)

1. Stale comment in src/app/api/roadmap/route.ts:120 reads: Phase 51: a Step-11 or Step-13 completion for an assigned student produces a coach milestone notification. After Phase 57 shift, those are now Step-12 and Step-14. Comment is non-functional (code reads MILESTONE_CONFIG via the RPC, not these literals), but worth refreshing in a docs-only follow-up.

## Orphaned Code

None detected. All exports used, all API routes consumed, all pages routed.

## Unprotected Routes

None. All four /[role]/announcements pages call requireRole(role); all mutating API routes check auth, then profile, then role in [owner, coach] before validating body.
