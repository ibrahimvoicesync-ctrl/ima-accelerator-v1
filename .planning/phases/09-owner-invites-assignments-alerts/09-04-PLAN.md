---
phase: 09-owner-invites-assignments-alerts
plan: 04
type: execute
wave: 1
depends_on: ["09-02"]
files_modified:
  - src/app/(dashboard)/owner/assignments/page.tsx
  - src/components/owner/OwnerAssignmentsClient.tsx
autonomous: true
gap_closure: true
requirements: ["OWNER-07"]

must_haves:
  truths:
    - "Owner can navigate to /owner/assignments and see a dedicated page (not a redirect)"
    - "Owner can see all students listed with their current coach assignment"
    - "Owner can change any student's coach assignment from the assignments page"
    - "Owner can filter students by coach or see unassigned students"
    - "Owner can see coach capacity cards showing student load per coach"
  artifacts:
    - path: "src/app/(dashboard)/owner/assignments/page.tsx"
      provides: "Dedicated assignments page with server-side data fetch"
      contains: "requireRole"
    - path: "src/components/owner/OwnerAssignmentsClient.tsx"
      provides: "Client component with filter tabs, student rows with coach dropdowns, coach capacity cards"
      exports: ["OwnerAssignmentsClient"]
  key_links:
    - from: "src/components/owner/OwnerAssignmentsClient.tsx"
      to: "/api/assignments"
      via: "fetch PATCH on coach dropdown change"
      pattern: "fetch.*api/assignments"
    - from: "src/app/(dashboard)/owner/assignments/page.tsx"
      to: "supabase admin"
      via: "parallel Promise.all queries for students, coaches, student counts"
      pattern: "Promise\\.all"
---

<objective>
Replace the /owner/assignments redirect with a dedicated assignments management page.

Purpose: User reported (UAT test 8) that redirecting /owner/assignments to /owner/students "makes no sense" and wants a dedicated page for managing coach-student assignments. This overrides the original locked decision from 09-CONTEXT.md.

Output: A fully functional /owner/assignments page with coach capacity overview, student list with inline coach assignment dropdowns, filter by coach/unassigned, and search.
</objective>

<execution_context>
@C:/Users/ibrah/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ibrah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-owner-invites-assignments-alerts/09-02-SUMMARY.md

<interfaces>
<!-- Existing PATCH /api/assignments endpoint from 09-02 -->
From src/app/api/assignments/route.ts:
```typescript
// PATCH /api/assignments?studentId={uuid}
// Body: { coach_id: string | null }
// Returns: { data: { id: string, coach_id: string | null } }
// Owner-only, validates student exists, validates coach exists if non-null
```

From src/lib/config.ts:
```typescript
export const COACH_CONFIG = {
  atRiskInactiveDays: 3,
  atRiskRatingThreshold: 2,
  maxStudentsPerCoach: 50,
  reportInboxDays: 7,
} as const;
```

From src/components/ui/Card.tsx:
```typescript
export function Card({ children, interactive, variant, className }: CardProps)
export function CardContent({ children, className }: CardContentProps)
```

From src/components/ui/Badge.tsx:
```typescript
export function Badge({ variant, size, className, children }: BadgeProps)
// variants: "default" | "success" | "warning" | "error"
```

From src/components/ui/Input.tsx:
```typescript
export function Input({ label, ...props }: InputProps)
```

From src/components/ui/Button.tsx:
```typescript
export function Button({ variant, size, loading, disabled, ...props }: ButtonProps)
// variants: "primary" | "secondary" | "ghost" | "danger"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build OwnerAssignmentsClient component</name>
  <files>src/components/owner/OwnerAssignmentsClient.tsx</files>
  <action>
Create a "use client" component `OwnerAssignmentsClient` that receives props:
- `students: { id: string; name: string; email: string; status: string; coach_id: string | null }[]`
- `coaches: { id: string; name: string; studentCount: number }[]`

The component must include:

**1. Coach capacity cards section** (top of page):
- Heading "Coach Capacity" in xs uppercase tracking-wide text-ima-text-secondary
- Grid: `grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-3`
- Each coach card (use Card component): coach name (truncated), "N / 50 students" text, progress bar using `role="progressbar"` with aria-valuenow/min/max/label
- Progress bar fill color: `bg-ima-primary` normally, `bg-ima-warning` at >=80%, `bg-ima-error` at >95%
- Use `COACH_CONFIG.maxStudentsPerCoach` from config (import it)
- Student counts must be LIVE — recalculated based on `localAssignments` overrides (same pattern as reference-old)

**2. Filter tabs** below capacity cards:
- "All" | "Assigned" | "Unassigned" filter buttons
- Each button: `min-h-[44px]`, `motion-safe:transition-colors`
- Active tab: `bg-ima-primary text-white`, inactive: `bg-ima-surface text-ima-text-secondary border border-ima-border`
- Show count badge on each tab: All (total), Assigned (with coach), Unassigned (no coach)

**3. Search input** below filters:
- Use `<Input>` component with label="Search students", placeholder="Search by name or email..."
- Client-side filter on student name and email (case-insensitive includes)

**4. Student assignment rows**:
- Use Card-based layout (NO Table component — V1 doesn't have one)
- Each student row: Card with `<CardContent className="p-4 flex flex-col sm:flex-row sm:items-center gap-3">`
- Left side: avatar circle (initials, bg-ima-primary text-white, w-10 h-10), name (font-semibold truncate), email (text-xs text-ima-text-secondary truncate)
- Right side: `<select>` dropdown for coach assignment with `min-h-[44px]`, `aria-label="Assign {name} to coach"`, options: "Unassigned" + all coaches with "(N students)" count
- On select change: immediately call `PATCH /api/assignments?studentId={id}` with `{ coach_id: value || null }`
- Check `response.ok` before parsing JSON
- On success: toast "Student assigned to coach" or "Student unassigned", call `router.refresh()`
- On error: toast error message, revert select to previous value
- Use stable `routerRef` and `toastRef` refs pattern (same as OwnerStudentDetailClient)
- While saving, show "Saving..." text and disable the select (`disabled={isSaving}`)
- If the dropdown value differs from the server value (has local pending change), add `border-ima-primary ring-1 ring-ima-primary/30` to highlight

**5. Empty state** when no students match filter/search:
- Card with centered content: Users icon (aria-hidden), "No students found" heading, contextual subtitle

All interactive elements: min-h-[44px]. All icons: aria-hidden="true". All colors: ima-* tokens only.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>OwnerAssignmentsClient component exists with coach capacity cards, filter tabs, search, and student rows with inline coach assignment dropdowns. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Replace assignments redirect with dedicated page</name>
  <files>src/app/(dashboard)/owner/assignments/page.tsx</files>
  <action>
Replace the current redirect-only `/owner/assignments/page.tsx` with a full server component page:

1. Import `requireRole` from `@/lib/session`, `createAdminClient` from `@/lib/supabase/admin`, `OwnerAssignmentsClient` from `@/components/owner/OwnerAssignmentsClient`
2. `requireRole("owner")` at the top
3. Parallel `Promise.all` with 2 queries via admin client:
   - All students: `admin.from("users").select("id, name, email, status, coach_id").eq("role", "student").eq("status", "active").order("name")`
   - All active coaches: `admin.from("users").select("id, name").eq("role", "coach").eq("status", "active").order("name")`
4. After both resolve, compute coach student counts from the students data:
   - Build a `Record<string, number>` mapping coach_id to count of students with that coach_id
   - Map coaches to `{ id, name, studentCount }` format
5. Console.error on any query errors (never swallow)
6. Render page layout:
   - `<div className="space-y-6 px-4">`
   - Page header with ArrowLeftRight icon (lucide-react, aria-hidden), h1 "Assignments" in text-2xl font-bold text-ima-text, subtitle "Manage coach-student assignments across the platform." in text-sm text-ima-text-secondary
   - Stat row: 3 mini stat cards in `grid grid-cols-3 gap-4` — Total Students (count), Assigned (students with coach_id), Unassigned (students without coach_id). Each stat card: Card with CardContent p-4, number in text-2xl font-bold text-ima-text, label in text-xs text-ima-text-secondary
   - `<OwnerAssignmentsClient students={students} coaches={coachOptions} />`

Do NOT import `redirect` from next/navigation — the page is no longer a redirect.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>/owner/assignments renders a dedicated page with stat cards, coach capacity overview, and student assignment management. Build succeeds with the route listed in the route table.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npm run lint` exits 0
3. `npm run build` succeeds and `/owner/assignments` appears in route table as a dynamic route (not a redirect)
4. Both new files exist and export the expected components
</verification>

<success_criteria>
- /owner/assignments is a full page, not a redirect
- Page shows coach capacity cards with progress bars
- Page shows all students with inline coach dropdown per row
- Filter tabs work (All / Assigned / Unassigned)
- Search filters students by name/email
- Coach assignment change fires PATCH /api/assignments and shows toast
- All ima-* tokens, 44px touch targets, aria labels present
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-owner-invites-assignments-alerts/09-04-SUMMARY.md`
</output>
