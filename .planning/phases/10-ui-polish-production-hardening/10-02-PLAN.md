---
phase: 10-ui-polish-production-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/student/loading.tsx
  - src/app/(dashboard)/student/work/loading.tsx
  - src/app/(dashboard)/student/roadmap/loading.tsx
  - src/app/(dashboard)/student/report/loading.tsx
  - src/app/(dashboard)/student/report/history/loading.tsx
  - src/app/(dashboard)/student/ask/loading.tsx
  - src/app/(dashboard)/coach/loading.tsx
  - src/app/(dashboard)/coach/reports/loading.tsx
  - src/app/(dashboard)/coach/analytics/loading.tsx
  - src/app/(dashboard)/coach/invites/loading.tsx
  - src/app/(dashboard)/coach/students/loading.tsx
  - src/app/(dashboard)/coach/students/[studentId]/loading.tsx
  - src/app/(dashboard)/owner/loading.tsx
  - src/app/(dashboard)/owner/students/loading.tsx
  - src/app/(dashboard)/owner/students/[studentId]/loading.tsx
  - src/app/(dashboard)/owner/coaches/loading.tsx
  - src/app/(dashboard)/owner/coaches/[coachId]/loading.tsx
  - src/app/(dashboard)/owner/invites/loading.tsx
  - src/app/(dashboard)/owner/assignments/loading.tsx
  - src/app/(dashboard)/owner/alerts/loading.tsx
autonomous: true
requirements:
  - UI-01
  - UI-06

must_haves:
  truths:
    - "Every page shows a skeleton loading state matching the page layout while data is fetching"
    - "Skeletons use ima-* tokens and motion-safe:animate-pulse"
    - "No layout shift when real content replaces skeleton"
  artifacts:
    - path: "src/app/(dashboard)/student/loading.tsx"
      provides: "Student dashboard skeleton (heading + work card + 2-col grid)"
      contains: "Skeleton"
    - path: "src/app/(dashboard)/coach/loading.tsx"
      provides: "Coach dashboard skeleton (heading + stat cards + student card grid)"
      contains: "SkeletonCard"
    - path: "src/app/(dashboard)/owner/loading.tsx"
      provides: "Owner dashboard skeleton (heading + 4-col stat grid)"
      contains: "Skeleton"
    - path: "src/app/(dashboard)/owner/students/loading.tsx"
      provides: "Owner students list skeleton (heading + search bar + card grid)"
      contains: "SkeletonCard"
  key_links:
    - from: "all loading.tsx files"
      to: "src/components/ui/Skeleton.tsx"
      via: "import Skeleton, SkeletonCard"
      pattern: "import.*Skeleton.*from.*@/components/ui"
---

<objective>
Create page-specific loading.tsx skeleton files for all 20 dashboard routes, matching each page's actual layout shape to prevent jarring layout shifts.

Purpose: Next.js automatically wraps route segments in Suspense boundaries when loading.tsx is present. Each skeleton matches the real page structure (stat grids, card lists, form layouts, tab views) so content loads smoothly.

Output: 20 loading.tsx files, one per dashboard route.
</objective>

<execution_context>
@C:/Users/ibrah/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ibrah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-polish-production-hardening/10-CONTEXT.md
@.planning/phases/10-ui-polish-production-hardening/10-RESEARCH.md

<interfaces>
<!-- Skeleton primitives to use -->

From src/components/ui/Skeleton.tsx:
```typescript
export function Skeleton({ className }: { className?: string }) {
  // renders: <div aria-hidden="true" className="rounded-lg bg-ima-border motion-safe:animate-pulse {className}" />
}

export function SkeletonCard({ className }: { className?: string }) {
  // renders: card shell with 3 skeleton lines (h-4 w-3/4, h-4 w-1/2, h-8 w-full)
  // uses: bg-ima-surface border border-ima-border rounded-xl shadow-sm p-4 space-y-3
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx skeletons for student and coach routes (12 files)</name>
  <files>
    src/app/(dashboard)/student/loading.tsx,
    src/app/(dashboard)/student/work/loading.tsx,
    src/app/(dashboard)/student/roadmap/loading.tsx,
    src/app/(dashboard)/student/report/loading.tsx,
    src/app/(dashboard)/student/report/history/loading.tsx,
    src/app/(dashboard)/student/ask/loading.tsx,
    src/app/(dashboard)/coach/loading.tsx,
    src/app/(dashboard)/coach/reports/loading.tsx,
    src/app/(dashboard)/coach/analytics/loading.tsx,
    src/app/(dashboard)/coach/invites/loading.tsx,
    src/app/(dashboard)/coach/students/loading.tsx,
    src/app/(dashboard)/coach/students/[studentId]/loading.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/student/page.tsx
    - src/app/(dashboard)/student/work/page.tsx
    - src/app/(dashboard)/student/roadmap/page.tsx
    - src/app/(dashboard)/student/report/page.tsx
    - src/app/(dashboard)/student/report/history/page.tsx
    - src/app/(dashboard)/student/ask/page.tsx
    - src/app/(dashboard)/coach/page.tsx
    - src/app/(dashboard)/coach/reports/page.tsx
    - src/app/(dashboard)/coach/analytics/page.tsx
    - src/app/(dashboard)/coach/invites/page.tsx
    - src/app/(dashboard)/coach/students/page.tsx
    - src/app/(dashboard)/coach/students/[studentId]/page.tsx
    - src/components/ui/Skeleton.tsx
  </read_first>
  <action>
Create 12 loading.tsx files. Each MUST import from `@/components/ui/Skeleton` and wrap content in `<div className="px-4">`. Every skeleton shape MUST match the actual page layout.

**Page layout patterns to match (read each page.tsx to verify exact structure):**

1. **`/student` (dashboard):** heading (h-8 w-56) + subtitle (h-4 w-48) + work progress card (rounded-xl border p-6 with 4 skeleton lines + h-11 button) + 2-col grid with 2 placeholder cards (each: rounded-xl border p-6, 3 skeleton lines + h-11 button)

2. **`/student/work`:** heading (h-8 w-48) + subtitle (h-4 w-32) + timer card (rounded-xl border p-6, centered circle skeleton h-48 w-48 rounded-full, then h-11 button below) + cycle cards grid (grid-cols-2 sm:grid-cols-4 gap-4, 4 small cards each h-20)

3. **`/student/roadmap`:** heading (h-8 w-48) + subtitle (h-4 w-64) + vertical list of 10 step skeletons (each: flex items-center gap-4, h-10 w-10 rounded-full + flex-1 space-y-2 with h-4 w-3/4 and h-3 w-1/2)

4. **`/student/report`:** heading (h-8 w-48) + subtitle (h-4 w-64) + form card (rounded-xl border p-6, 4 form field skeletons each h-11 w-full with h-4 w-20 label above, then h-11 submit button)

5. **`/student/report/history`:** heading (h-8 w-48) + subtitle (h-4 w-40) + vertical list of 5 report card skeletons (each: SkeletonCard)

6. **`/student/ask`:** heading (h-8 w-56) + subtitle (h-4 w-64) + large iframe placeholder (h-[600px] w-full rounded-xl bg-ima-border motion-safe:animate-pulse)

7. **`/coach` (dashboard):** heading (h-8 w-48) + subtitle (h-4 w-32) + 3 stat cards row (grid-cols-3 gap-4, each: rounded-xl border p-4 with h-7 w-12 number + h-3 w-20 label) + student card grid (grid-cols-1 md:grid-cols-2 gap-4, 4x SkeletonCard)

8. **`/coach/reports`:** heading (h-8 w-48) + subtitle (h-4 w-40) + 3 stat cards row (same as coach dashboard) + filter tabs skeleton (flex gap-2, 3x h-11 w-24 rounded-lg) + vertical list of 5 report row skeletons (each: rounded-xl border p-4 flex items-center gap-4, h-4 w-32 + h-4 w-20 + h-11 w-24)

9. **`/coach/analytics`:** heading (h-8 w-48) + subtitle (h-4 w-40) + 4 stat cards (grid-cols-2 lg:grid-cols-4 gap-4, each: rounded-xl border p-4, h-10 w-10 icon + h-7 w-12 + h-3 w-20) + student breakdown section (h-6 w-32 section heading + grid-cols-1 md:grid-cols-2 gap-4, 4x SkeletonCard)

10. **`/coach/invites`:** heading (h-8 w-48) + subtitle (h-4 w-40) + 2 tab buttons (flex gap-2, 2x h-11 w-32 rounded-lg) + form area skeleton (rounded-xl border p-6, h-11 w-full input + h-11 w-32 button) + invite list (4x rounded-xl border p-4 flex items-center gap-4, h-4 w-40 + h-6 w-16 badge + h-11 w-20 button)

11. **`/coach/students`:** heading (h-8 w-48) + subtitle (h-4 w-32) + student card grid (grid-cols-1 md:grid-cols-2 gap-4, 4x SkeletonCard)

12. **`/coach/students/[studentId]`:** back link skeleton (h-4 w-20) + header (flex items-center gap-4, h-12 w-12 rounded-full avatar + h-6 w-40 name + h-4 w-48 subtitle) + tab bar (flex gap-4 border-b, 3x h-11 w-20) + tab content (SkeletonCard + SkeletonCard)

Every loading.tsx MUST:
- `export default function Loading()` (default export, no props)
- Import `Skeleton` and/or `SkeletonCard` from `@/components/ui/Skeleton`
- Wrap in `<div className="px-4">` for mobile padding consistency
- Use `bg-ima-surface`, `border-ima-border`, `bg-ima-border` tokens only (no gray/hardcoded)
- Use `motion-safe:animate-pulse` only on custom skeleton elements (the Skeleton component already has it built in)
- Use `aria-hidden="true"` only on custom skeleton divs that are NOT using the Skeleton component (the Skeleton component already has it built in)
  </action>
  <verify>
    <automated>cd C:/Users/ibrah/ima-accelerator-v1 && npx tsc --noEmit 2>&1 | head -20 && find src/app/\(dashboard\)/student -name "loading.tsx" | wc -l && find src/app/\(dashboard\)/coach -name "loading.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find src/app/(dashboard)/student -name "loading.tsx" | wc -l` returns 6
    - `find src/app/(dashboard)/coach -name "loading.tsx" | wc -l` returns 6
    - Every loading.tsx contains `import { Skeleton` or `import { SkeletonCard` from `@/components/ui/Skeleton`
    - Every loading.tsx contains `export default function Loading()`
    - Every loading.tsx contains `className="px-4"` on the outer wrapper div
    - No loading.tsx contains `animate-pulse` without `motion-safe:` prefix (check custom divs only -- Skeleton component is already correct)
    - No loading.tsx contains `text-gray` or `bg-gray` or `bg-[#` (no hardcoded colors)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>12 loading.tsx files for student + coach routes, each matching the actual page layout shape</done>
</task>

<task type="auto">
  <name>Task 2: Create loading.tsx skeletons for owner routes (8 files)</name>
  <files>
    src/app/(dashboard)/owner/loading.tsx,
    src/app/(dashboard)/owner/students/loading.tsx,
    src/app/(dashboard)/owner/students/[studentId]/loading.tsx,
    src/app/(dashboard)/owner/coaches/loading.tsx,
    src/app/(dashboard)/owner/coaches/[coachId]/loading.tsx,
    src/app/(dashboard)/owner/invites/loading.tsx,
    src/app/(dashboard)/owner/assignments/loading.tsx,
    src/app/(dashboard)/owner/alerts/loading.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/owner/page.tsx
    - src/app/(dashboard)/owner/students/page.tsx
    - src/app/(dashboard)/owner/students/[studentId]/page.tsx
    - src/app/(dashboard)/owner/coaches/page.tsx
    - src/app/(dashboard)/owner/coaches/[coachId]/page.tsx
    - src/app/(dashboard)/owner/invites/page.tsx
    - src/app/(dashboard)/owner/assignments/page.tsx
    - src/app/(dashboard)/owner/alerts/page.tsx
    - src/components/ui/Skeleton.tsx
  </read_first>
  <action>
Create 8 loading.tsx files for all owner routes. Each MUST import from `@/components/ui/Skeleton` and wrap content in `<div className="px-4">`. Match the actual page layout shape.

**Page layout patterns to match (read each page.tsx to verify):**

1. **`/owner` (dashboard):** heading (h-8 w-48) + subtitle (h-4 w-32) + 4 stat cards (grid grid-cols-2 lg:grid-cols-4 gap-4, each: bg-ima-surface border border-ima-border rounded-xl p-4 flex items-center gap-4, with h-10 w-10 rounded-lg icon skeleton + h-7 w-12 number + h-3 w-20 label)

2. **`/owner/students`:** heading (h-8 w-48) + subtitle (h-4 w-40) + search bar skeleton (h-11 w-full rounded-lg) + student card grid (grid-cols-1 md:grid-cols-2 gap-4, 6x SkeletonCard)

3. **`/owner/students/[studentId]`:** back link (h-4 w-24) + header (flex items-center gap-4, h-12 w-12 rounded-full + h-6 w-40 + h-4 w-48) + assignment dropdown skeleton (h-11 w-48 rounded-lg) + tab bar (flex gap-4 border-b, 3x h-11 w-20) + tab content (2x SkeletonCard)

4. **`/owner/coaches`:** heading (h-8 w-48) + subtitle (h-4 w-32) + coach card grid (grid-cols-1 md:grid-cols-2 gap-4, 4x SkeletonCard)

5. **`/owner/coaches/[coachId]`:** back link (h-4 w-24) + header (flex items-center gap-4, h-12 w-12 rounded-full + h-6 w-40 + h-4 w-48) + 4 stat cards (grid grid-cols-2 lg:grid-cols-4 gap-4, each: rounded-xl border p-4 + h-7 w-12 + h-3 w-20) + student section heading (h-6 w-32) + grid-cols-1 md:grid-cols-2 gap-4, 4x SkeletonCard

6. **`/owner/invites`:** heading (h-8 w-48) + subtitle (h-4 w-40) + 2 tab buttons (flex gap-2, 2x h-11 w-32) + form skeleton (rounded-xl border p-6, h-11 w-full input + h-11 w-48 role select + h-11 w-32 button) + invite list (4x rounded-xl border p-4 flex items-center gap-4, h-4 w-40 + h-6 w-16 + h-11 w-20)

7. **`/owner/assignments`:** heading (h-8 w-48) + subtitle (h-4 w-40) + filter bar (flex gap-4, h-11 w-48 select + h-11 w-32 button) + coach capacity cards (grid grid-cols-2 lg:grid-cols-4 gap-4, 4x small cards each rounded-xl border p-4 with h-4 w-24 + h-6 w-8) + student assignment list (6x rounded-xl border p-4 flex items-center gap-4, h-4 w-32 name + h-11 w-48 select)

8. **`/owner/alerts`:** heading (h-8 w-48) + subtitle (h-4 w-40) + filter tabs (flex gap-2, 5x h-11 w-24 rounded-lg) + alert cards (4x SkeletonCard)

Every loading.tsx MUST:
- `export default function Loading()` (default export)
- Import from `@/components/ui/Skeleton`
- Wrap in `<div className="px-4">`
- Use ima-* tokens only
- No hardcoded gray/hex colors
  </action>
  <verify>
    <automated>cd C:/Users/ibrah/ima-accelerator-v1 && npx tsc --noEmit 2>&1 | head -20 && find src/app/\(dashboard\)/owner -name "loading.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find src/app/(dashboard)/owner -name "loading.tsx" | wc -l` returns 8
    - Every loading.tsx contains `import { Skeleton` or `import { SkeletonCard` from `@/components/ui/Skeleton`
    - Every loading.tsx contains `export default function Loading()`
    - Every loading.tsx contains `className="px-4"` on the outer wrapper div
    - No loading.tsx contains `text-gray` or `bg-gray` or `bg-[#` (no hardcoded colors)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>8 loading.tsx files for all owner routes, each matching the actual page layout shape</done>
</task>

</tasks>

<verification>
```bash
# All 20 loading.tsx files exist
find src/app/(dashboard) -name "loading.tsx" | wc -l
# Should be 20

# Type check passes
npx tsc --noEmit

# No hardcoded colors
grep -rn "text-gray\|bg-gray\|bg-\[#" src/app/(dashboard)/*/loading.tsx src/app/(dashboard)/*/*/loading.tsx 2>/dev/null
# Should return nothing

# All use Skeleton imports
grep -rL "Skeleton" src/app/(dashboard)/*/loading.tsx src/app/(dashboard)/*/*/loading.tsx 2>/dev/null
# Should return nothing
```
</verification>

<success_criteria>
- 20 loading.tsx files exist across all dashboard routes
- Each skeleton matches the actual page layout to prevent layout shifts
- All use Skeleton/SkeletonCard from ui primitives
- `npx tsc --noEmit` passes
- `npm run lint` passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish-production-hardening/10-02-SUMMARY.md`
</output>
