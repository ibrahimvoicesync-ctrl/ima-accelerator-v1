---
phase: 24-infrastructure-validation
plan: "05"
type: execute
wave: 2
depends_on:
  - "24-04"
files_modified:
  - load-tests/CAPACITY.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "A staging Supabase project is provisioned with the same compute tier and region as production"
    - "k6 load tests have been executed against staging with real 5,000-student seed data"
    - "CAPACITY.md contains measured P50/P95/P99 latencies, connection counts, and rate limiter trigger counts — no (projected) values remain"
    - "PROJECT.md Key Decisions Phase 24 entry reflects actual test results, not projections"
    - "INFRA-01/02/03 are re-checked in REQUIREMENTS.md based on real evidence"
  artifacts:
    - path: "load-tests/CAPACITY.md"
      provides: "Completed capacity report with measured values"
      contains: "STATUS: MEASURED"
    - path: ".planning/PROJECT.md"
      provides: "Evidence-based compute sizing decision"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Evidence-backed INFRA requirement completion"
  key_links:
    - from: "k6 test output"
      to: "load-tests/CAPACITY.md"
      via: "Human copies P50/P95/P99 from k6 summary output"
      pattern: "measured"
    - from: "load-tests/CAPACITY.md"
      to: ".planning/PROJECT.md"
      via: "Compute decision updated with actual numbers"
      pattern: "Phase 24"
---

<objective>
Provision staging infrastructure, execute k6 load tests, and replace all projected values with measured data.

Purpose: The phase goal — "the platform is validated under realistic 5,000-student load" — requires actual test execution. Plans 24-01 through 24-03 built all the tooling. Plan 24-04 fixed the owner token bug. This plan closes the validation loop by running the tests and recording evidence.

Output: CAPACITY.md with real measured values, PROJECT.md with evidence-based compute decision, REQUIREMENTS.md with INFRA-01/02/03 re-checked based on actual results.
</objective>

<execution_context>
@C:/Users/ibrah/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ibrah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-infrastructure-validation/24-CONTEXT.md
@.planning/phases/24-infrastructure-validation/24-VERIFICATION.md
@.planning/phases/24-infrastructure-validation/24-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision staging Supabase and execute load tests</name>
  <read_first>
    - load-tests/CAPACITY.md (the "How to Execute Actual Tests" section has exact commands)
    - load-tests/scripts/gen-tokens.js (verify the array fix from Plan 04 is in place before generating tokens)
    - .planning/phases/24-infrastructure-validation/24-CONTEXT.md (D-01 through D-03 for staging requirements)
  </read_first>
  <action>
Human must complete these steps in order. Claude cannot do this — it requires Supabase dashboard access, billing, and live infrastructure.

**Step 1: Provision staging Supabase project**
- Create a new Supabase project in the same region as production
- Use the same compute add-on tier (Pro Small)
- Note the project URL, anon key, and JWT secret
- Per D-03: Keep this project after Phase 24 for v1.3 regression testing

**Step 2: Apply all migrations**
- Link to the staging project: `npx supabase link --project-ref <staging-ref>`
- Push migrations: `npx supabase db push`
- Verify all 12 migration files applied successfully

**Step 3: Seed the database**
- Run: `npx supabase db execute --file load-tests/seed/00001_staging_seed.sql`
- This creates 5,000 students, ~450k daily_reports, proportional work_sessions and roadmap_progress rows
- Verify with: `SELECT count(*) FROM daily_reports;` (expect ~450,000)

**Step 4: Generate JWT tokens**
- Run: `STAGING_JWT_SECRET=<your-staging-jwt-secret> node load-tests/scripts/gen-tokens.js`
- Verify owner_token.json is a JSON array: `node -e "const t = JSON.parse(require('fs').readFileSync('load-tests/tokens/owner_token.json','utf8')); console.log(Array.isArray(t), t[0].substring(0,10))"`
- Expected output: `true eyJhbGciO` (array=true, starts with JWT header)

**Step 5: Run k6 scenarios**
Run all three scenarios. Replace `<url>` and `<key>` with staging credentials.

```bash
# Scenario 1: Write spike (student report submission burst)
"/c/Program Files/k6/k6.exe" run \
  -e APP_URL=https://<staging>.supabase.co \
  -e SUPABASE_URL=https://<staging>.supabase.co \
  -e SUPABASE_ANON_KEY=<staging-anon-key> \
  load-tests/scenarios/write-spike.js

# Scenario 2: Read mix (owner dashboard browsing)
"/c/Program Files/k6/k6.exe" run \
  -e APP_URL=https://<staging>.supabase.co \
  -e SUPABASE_URL=https://<staging>.supabase.co \
  -e SUPABASE_ANON_KEY=<staging-anon-key> \
  load-tests/scenarios/read-mix.js

# Scenario 3: Combined (simultaneous read + write)
"/c/Program Files/k6/k6.exe" run \
  -e APP_URL=https://<staging>.supabase.co \
  -e SUPABASE_URL=https://<staging>.supabase.co \
  -e SUPABASE_ANON_KEY=<staging-anon-key> \
  load-tests/scenarios/combined.js
```

**Step 6: Capture connection data during write spike**
- During write-spike.js execution (the 5-minute sustain phase), check Supabase dashboard
- Navigate to Reports > Database > Connections
- Record: current connections, max_connections for the compute tier, percentage used
- Also run in SQL editor during spike: `SELECT count(*) FROM pg_stat_activity WHERE state = 'active';`

**Step 7: Capture rate limiter data after tests complete**
- Run in SQL editor: `SELECT count(*) FROM rate_limit_log;`
- Run: `SELECT endpoint, count(*) FROM rate_limit_log GROUP BY endpoint ORDER BY count DESC LIMIT 10;`

**Step 8: Save k6 output**
- Save the k6 terminal output for each scenario (contains P50/P95/P99 automatically)
- Or redirect output: append `--out csv=load-tests/results/<scenario>.csv` to each k6 command
  </action>
  <acceptance_criteria>
    - Staging Supabase project exists and is accessible
    - All 12 migrations applied successfully
    - daily_reports table has approximately 450,000 rows
    - All three k6 scenarios complete without k6-level errors
    - k6 output shows P50/P95/P99 latency values for each scenario
    - Connection count captured from Supabase dashboard during write spike
    - Rate limiter log count captured after test completion
  </acceptance_criteria>
  <resume-signal>Provide the k6 output summary for all 3 scenarios, the connection count during spike, and the rate_limit_log count. Claude will use these to update CAPACITY.md, PROJECT.md, and REQUIREMENTS.md.</resume-signal>
  <done>All three k6 scenarios have been executed against a staging environment with 5,000 seeded students; P50/P95/P99 latencies, connection counts, and rate limiter data are captured</done>
</task>

<task type="auto">
  <name>Task 2: Update capacity docs and requirements with measured data</name>
  <files>load-tests/CAPACITY.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - load-tests/CAPACITY.md (current projected values to be replaced)
    - .planning/PROJECT.md (Key Decisions table, Phase 24 entry)
    - .planning/REQUIREMENTS.md (INFRA-01/02/03 should be unchecked from Plan 04)
  </read_first>
  <action>
Using the measured data provided by the human in Task 1's resume signal:

**1. Update load-tests/CAPACITY.md:**
- Change the STATUS line from "PROJECTED VALUES" to "MEASURED VALUES — Tests executed [date]"
- Replace all "(projected)" P50/P95/P99 values with actual k6 measurements
- Replace "(projected)" connection usage with actual dashboard reading
- Replace "(projected)" rate_limit_log count with actual query result
- Replace projected pg_stat_statements top-10 with actual values (if provided)
- Update the Pass/Fail column based on actual thresholds:
  - P95 < 1000ms = Pass (per D-11)
  - Connection usage < 70% of max_connections = Pass (per D-12)
  - Error rate < 1% = Pass

**2. Update .planning/PROJECT.md:**
- Find the Phase 24 Key Decisions entry
- Replace projected P95 and connection values with actual measured values
- Remove the "Pending actual staging test execution to confirm" caveat
- If actual P95 > 1s or connections > 70%: change STAY to UPGRADE with rationale

**3. Update .planning/REQUIREMENTS.md:**
- Based on actual test results, re-check requirements that are now evidence-backed:
  - If k6 ran successfully with read mix + write spike: check INFRA-01
  - If CAPACITY.md has real measured values: check INFRA-02
  - If compute decision confirmed with real data: check INFRA-03
- Update traceability table status from "Pending" to "Complete"
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('load-tests/CAPACITY.md','utf8'); const noProjected = !s.includes('(projected)'); const hasMeasured = s.includes('MEASURED'); if (!noProjected || !hasMeasured) { console.error('FAIL:', {noProjected, hasMeasured}); process.exit(1); } console.log('PASS: CAPACITY.md has measured values, no projected values remain');"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "(projected)" in load-tests/CAPACITY.md returns NO matches
    - grep for "MEASURED" in load-tests/CAPACITY.md returns a match
    - grep for "Pending actual staging" in .planning/PROJECT.md returns NO matches
    - INFRA-01/02/03 checkboxes in REQUIREMENTS.md match actual test outcomes
    - Traceability table shows "Complete" for all INFRA requirements (if tests passed)
  </acceptance_criteria>
  <done>CAPACITY.md contains only measured values from actual k6 test runs; PROJECT.md compute decision is evidence-based; REQUIREMENTS.md accurately reflects validation status</done>
</task>

</tasks>

<verification>
1. `node -e "const s=require('fs').readFileSync('load-tests/CAPACITY.md','utf8'); console.log(s.includes('(projected)') ? 'FAIL: projected values remain' : 'PASS')"` returns PASS
2. `node -e "const s=require('fs').readFileSync('.planning/PROJECT.md','utf8'); console.log(s.includes('Pending actual staging') ? 'FAIL' : 'PASS')"` returns PASS
3. All three k6 scenario results are documented in CAPACITY.md (write-spike, read-mix, combined)
4. Compute sizing decision in PROJECT.md reflects measured data
</verification>

<success_criteria>
- No "(projected)" values remain in CAPACITY.md
- PROJECT.md Phase 24 entry has real P95 and connection numbers
- INFRA-01/02/03 re-checked only if actual evidence supports it
- Phase 24 goal — "validated under realistic 5,000-student load" — is met with real data
</success_criteria>

<output>
After completion, create `.planning/phases/24-infrastructure-validation/24-05-SUMMARY.md`
</output>
