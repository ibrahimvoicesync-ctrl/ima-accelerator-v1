---
phase: 12-claude-md-hard-rule-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/student/WorkTrackerClient.tsx
  - src/components/student/CycleCard.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/[code]/RegisterCard.tsx
  - src/app/(auth)/register/MagicLinkCard.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/(auth)/register/[code]/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "WorkTrackerClient.tsx contains zero raw green/amber/red Tailwind color tokens"
    - "WorkTrackerClient.tsx stale-session abandon fetch checks response.ok"
    - "CycleCard.tsx contains zero raw green/amber/red Tailwind color tokens"
    - "All auth pages contain zero raw red Tailwind color tokens"
    - "All replaced tokens use ima-success, ima-warning, ima-error with opacity modifiers where needed"
  artifacts:
    - path: "src/components/student/WorkTrackerClient.tsx"
      provides: "Work tracker with ima-* tokens only and response.ok check"
      contains: "bg-ima-success/10"
    - path: "src/components/student/CycleCard.tsx"
      provides: "Cycle card with ima-* tokens only"
      contains: "text-ima-success"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page error alert with ima-* tokens"
      contains: "bg-ima-error/10"
    - path: "src/app/(auth)/register/[code]/RegisterCard.tsx"
      provides: "Register card error alert with ima-* tokens"
      contains: "bg-ima-error/10"
    - path: "src/app/(auth)/register/MagicLinkCard.tsx"
      provides: "Magic link card error alert with ima-* tokens"
      contains: "bg-ima-error/10"
    - path: "src/app/(auth)/register/page.tsx"
      provides: "Register magic page ErrorCard with ima-* tokens"
      contains: "text-ima-error"
    - path: "src/app/(auth)/register/[code]/page.tsx"
      provides: "Register invite page ErrorCard with ima-* tokens"
      contains: "text-ima-error"
  key_links:
    - from: "tailwind.config.ts"
      to: "all 7 modified files"
      via: "ima-success, ima-warning, ima-error token definitions"
      pattern: "ima-(success|warning|error)"
    - from: "src/components/student/WorkTrackerClient.tsx"
      to: "/api/work-sessions/[id]"
      via: "fetch PATCH with response.ok check in abandonStale"
      pattern: "results\\.every"
---

<objective>
Replace all raw Tailwind color tokens with semantic ima-* design tokens across work tracker components and auth error states, and fix the missing response.ok check on WorkTrackerClient.tsx stale-session abandon.

Purpose: Enforce CLAUDE.md hard rules #8 ("ima-* tokens only") and #6 ("Check response.ok") which were violated in code written during Phases 2 and 3.
Output: 7 files with zero raw color tokens remaining and response.ok properly checked.
</objective>

<execution_context>
@C:/Users/ibrah/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ibrah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-claude-md-hard-rule-compliance/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw color tokens and fix response.ok in WorkTrackerClient.tsx and CycleCard.tsx</name>
  <files>src/components/student/WorkTrackerClient.tsx, src/components/student/CycleCard.tsx</files>
  <read_first>src/components/student/WorkTrackerClient.tsx, src/components/student/CycleCard.tsx, CLAUDE.md</read_first>
  <action>
Make TWO categories of changes to WorkTrackerClient.tsx, plus color token changes to CycleCard.tsx.

**CATEGORY A: Color token replacements in WorkTrackerClient.tsx**

Replace every raw Tailwind color token with the corresponding ima-* token. Complete mapping (apply to ALL occurrences):

Celebration banner (around line 202):
- `bg-green-50` -> `bg-ima-success/10`
- `border-green-200` -> `border-ima-success/30`
- `text-green-800` -> `text-ima-success`
- `text-green-700` -> `text-ima-success`
- `text-green-600` -> `text-ima-success`

Pause button (around line 235):
- `bg-amber-100` -> `bg-ima-warning/15`
- `text-amber-700` -> `text-ima-warning`
- `hover:bg-amber-200` -> `hover:bg-ima-warning/25`

Complete button (around line 242):
- `bg-green-600` -> `bg-ima-success`
- `hover:bg-green-700` -> `hover:bg-ima-success/90`

Abandon button in ACTIVE state (around line 249) AND Abandon button in PAUSED state (around line 303):
- `text-red-600` -> `text-ima-error`
- `hover:bg-red-50` -> `hover:bg-ima-error/10`

Abandon confirm box in ACTIVE state (around line 257) AND abandon confirm box in PAUSED state (around line 311):
- `bg-red-50` -> `bg-ima-error/10`
- `text-red-700` -> `text-ima-error`

Confirm Abandon button in ACTIVE state (around line 262) AND Confirm Abandon button in PAUSED state (around line 316):
- `bg-red-600` -> `bg-ima-error`
- `hover:bg-red-700` -> `hover:bg-ima-error/90`

CRITICAL: There are TWO identical abandon-confirm blocks (one for active state ~lines 256-274, one for paused state ~lines 310-328). BOTH must be updated.

**CATEGORY B: Fix response.ok check in WorkTrackerClient.tsx abandonStale function (lines 42-53)**

Replace the current abandonStale function inside the stale-session useEffect:

Current code:
```typescript
const abandonStale = async () => {
  await Promise.all(
    staleSessions.map((s) =>
      fetch(`/api/work-sessions/${s.id}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ status: "abandoned" }),
      })
    )
  );
  router.refresh();
};
```

Replace with:
```typescript
const abandonStale = async () => {
  const results = await Promise.all(
    staleSessions.map((s) =>
      fetch(`/api/work-sessions/${s.id}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ status: "abandoned" }),
      })
    )
  );
  const allOk = results.every((r) => r.ok);
  if (!allOk) {
    console.error("[WorkTrackerClient] Some stale sessions failed to abandon");
  }
  router.refresh();
};
```

Key: Store results, check `results.every((r) => r.ok)`, log error if any failed, call `router.refresh()` unconditionally (best-effort cleanup).

**CATEGORY C: Color token replacements in CycleCard.tsx**

Status icons (lines 17, 19, 20):
- `text-green-600` -> `text-ima-success` (completed icon, line 17)
- `text-amber-500` -> `text-ima-warning` (paused icon, line 19)
- `text-red-500` -> `text-ima-error` (abandoned icon, line 20)

Do NOT change any non-color classes. Do NOT change `text-white` on colored backgrounds. Do NOT change already-correct ima-* tokens like `text-ima-primary`.
  </action>
  <verify>
    <automated>cd C:/Users/ibrah/ima-accelerator-v1 && echo "=== RAW TOKENS (expect 0) ===" && grep -cn "bg-green-\|text-green-\|bg-red-\|text-red-\|bg-amber-\|text-amber-" src/components/student/WorkTrackerClient.tsx src/components/student/CycleCard.tsx && echo "=== RESPONSE.OK CHECK ===" && grep -n "results.every" src/components/student/WorkTrackerClient.tsx && echo "=== IMA TOKENS ===" && grep -c "ima-success\|ima-error\|ima-warning" src/components/student/WorkTrackerClient.tsx</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "bg-green-\|text-green-\|bg-red-\|text-red-\|bg-amber-\|text-amber-" src/components/student/WorkTrackerClient.tsx` returns zero matches
- `grep -n "text-green-\|text-amber-\|text-red-" src/components/student/CycleCard.tsx` returns zero matches
- `grep -c "ima-success" src/components/student/WorkTrackerClient.tsx` returns at least 8
- `grep -c "ima-error" src/components/student/WorkTrackerClient.tsx` returns at least 10
- `grep -c "ima-warning" src/components/student/WorkTrackerClient.tsx` returns at least 2
- WorkTrackerClient.tsx contains `const results = await Promise.all(`
- WorkTrackerClient.tsx contains `results.every((r) => r.ok)`
- WorkTrackerClient.tsx contains `console.error("[WorkTrackerClient] Some stale sessions failed to abandon")`
- CycleCard.tsx line with Check icon contains `text-ima-success`
- CycleCard.tsx line with Pause icon contains `text-ima-warning`
- CycleCard.tsx line with X icon contains `text-ima-error`
- `npx tsc --noEmit` passes with zero errors
- `npm run lint` passes with zero errors
  </acceptance_criteria>
  <done>WorkTrackerClient.tsx has zero raw color tokens and checks response.ok on stale abandon; CycleCard.tsx has zero raw color tokens</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw color tokens in auth pages</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/register/[code]/RegisterCard.tsx, src/app/(auth)/register/MagicLinkCard.tsx, src/app/(auth)/register/page.tsx, src/app/(auth)/register/[code]/page.tsx</files>
  <read_first>src/app/(auth)/login/page.tsx, src/app/(auth)/register/[code]/RegisterCard.tsx, src/app/(auth)/register/MagicLinkCard.tsx, src/app/(auth)/register/page.tsx, src/app/(auth)/register/[code]/page.tsx, CLAUDE.md</read_first>
  <action>
Replace raw red color tokens in auth error states with ima-error tokens:

**login/page.tsx error alert div (around line 86):**
Change:
`className="mt-4 rounded-lg bg-red-50 border border-red-200 p-3 text-sm text-red-700"`
To:
`className="mt-4 rounded-lg bg-ima-error/10 border border-ima-error/30 p-3 text-sm text-ima-error"`

**RegisterCard.tsx error alert div (around line 101):**
Change:
`className="mt-4 rounded-lg bg-red-50 border border-red-200 p-3 text-sm text-red-700"`
To:
`className="mt-4 rounded-lg bg-ima-error/10 border border-ima-error/30 p-3 text-sm text-ima-error"`

**MagicLinkCard.tsx error alert div (around line 94):**
Change:
`className="mt-4 rounded-lg bg-red-50 border border-red-200 p-3 text-sm text-red-700"`
To:
`className="mt-4 rounded-lg bg-ima-error/10 border border-ima-error/30 p-3 text-sm text-ima-error"`

**register/page.tsx ErrorCard XCircle icon (around line 16):**
Change: `className="mx-auto h-12 w-12 text-red-500"`
To: `className="mx-auto h-12 w-12 text-ima-error"`

**register/[code]/page.tsx ErrorCard XCircle icon (around line 16):**
Change: `className="mx-auto h-12 w-12 text-red-500"`
To: `className="mx-auto h-12 w-12 text-ima-error"`

Do NOT change any non-color classes. Do NOT change any already-correct ima-* tokens.
  </action>
  <verify>
    <automated>cd C:/Users/ibrah/ima-accelerator-v1 && echo "=== RAW RED TOKENS IN AUTH (expect 0) ===" && grep -rn "bg-red-\|text-red-\|border-red-" "src/app/(auth)/" ; echo "EXIT:$?" && echo "=== IMA ERROR TOKENS ===" && grep -rn "ima-error" "src/app/(auth)/"</automated>
  </verify>
  <acceptance_criteria>
- `grep -rn "bg-red-\|text-red-\|border-red-" "src/app/(auth)/"` returns zero matches
- login/page.tsx contains `bg-ima-error/10 border border-ima-error/30 p-3 text-sm text-ima-error`
- RegisterCard.tsx contains `bg-ima-error/10 border border-ima-error/30 p-3 text-sm text-ima-error`
- MagicLinkCard.tsx contains `bg-ima-error/10 border border-ima-error/30 p-3 text-sm text-ima-error`
- register/page.tsx contains `text-ima-error` on XCircle className
- register/[code]/page.tsx contains `text-ima-error` on XCircle className
- `npx tsc --noEmit` passes with zero errors
- `npm run lint` passes with zero errors
  </acceptance_criteria>
  <done>All 5 auth page files contain zero raw Tailwind red color tokens; all error states use ima-error with opacity modifiers</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "bg-green-\|text-green-\|bg-red-\|text-red-\|bg-amber-\|text-amber-" src/components/student/WorkTrackerClient.tsx src/components/student/CycleCard.tsx "src/app/(auth)/"` returns zero matches
2. `grep -n "results.every" src/components/student/WorkTrackerClient.tsx` returns a match
3. `npm run lint` passes
4. `npx tsc --noEmit` passes
5. `npm run build` passes
</verification>

<success_criteria>
All 7 files use exclusively ima-* design tokens for color. Zero raw Tailwind green/amber/red tokens remain. WorkTrackerClient.tsx checks response.ok on stale-session abandon. Build passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/12-claude-md-hard-rule-compliance/12-01-SUMMARY.md`
</output>
