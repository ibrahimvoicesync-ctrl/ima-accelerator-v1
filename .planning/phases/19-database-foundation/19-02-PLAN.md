---
phase: 19-database-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - .planning/phases/19-database-foundation/BASELINE.md
autonomous: false
requirements:
  - DB-03
  - DB-04

must_haves:
  truths:
    - "All RLS policies use (select get_user_role()) and (select get_user_id()) initplan wrappers — verified by reading migration source and confirmed by EXPLAIN output"
    - "Helper functions get_user_id() and get_user_role() are STABLE and SECURITY DEFINER — confirmed by reading migration source"
    - "pg_stat_statements is enabled and returning data on Supabase"
    - "Top 10 slowest queries are captured before and after index migration in BASELINE.md"
    - "EXPLAIN ANALYZE confirms index scans on hot query paths (or documents why seq scan is correct at current data volume)"
  artifacts:
    - path: ".planning/phases/19-database-foundation/BASELINE.md"
      provides: "pg_stat_statements before/after baseline and EXPLAIN ANALYZE results"
      contains: "## Before Migration"
  key_links:
    - from: ".planning/phases/19-database-foundation/BASELINE.md"
      to: ".planning/phases/20-query-consolidation-caching/"
      via: "Phase 20 references baseline for query optimization targets"
      pattern: "top 10 slowest"

user_setup:
  - service: supabase-dashboard
    why: "pg_stat_statements must be enabled via Supabase Dashboard before SQL queries will work"
    dashboard_config:
      - task: "Enable pg_stat_statements extension"
        location: "Supabase Dashboard -> Database -> Extensions -> search 'pg_stat_statements' -> Enable"
---

<objective>
Apply the migration to Supabase, verify RLS initplan optimization via EXPLAIN, capture pg_stat_statements query performance baseline before and after index changes, and verify composite indexes are used by hot query paths.

Purpose: Produce the documented evidence that database optimizations are working. This baseline is the reference point for Phase 20 query consolidation work.

Output: `BASELINE.md` with before/after query stats, EXPLAIN ANALYZE results, and RLS initplan verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-database-foundation/19-CONTEXT.md
@.planning/phases/19-database-foundation/19-RESEARCH.md
@.planning/phases/19-database-foundation/19-01-SUMMARY.md

@supabase/migrations/00009_database_foundation.sql
@supabase/migrations/00001_create_tables.sql

<interfaces>
<!-- Key SQL queries the executor must run in Supabase SQL Editor -->

pg_stat_statements baseline capture query:
```sql
SELECT
  LEFT(query, 120)                              AS query_snippet,
  calls,
  ROUND(mean_exec_time::numeric, 2)             AS mean_ms,
  ROUND(total_exec_time::numeric, 2)            AS total_ms,
  rows,
  ROUND(100.0 * shared_blks_hit /
    NULLIF(shared_blks_hit + shared_blks_read, 0), 1) AS cache_hit_pct
FROM extensions.pg_stat_statements
WHERE calls > 5
  AND query NOT LIKE '%pg_stat%'
ORDER BY total_exec_time DESC
LIMIT 10;
```

EXPLAIN ANALYZE for work_sessions hot path:
```sql
EXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)
SELECT *
FROM public.work_sessions
WHERE student_id = '<test-uuid>'
  AND date = CURRENT_DATE
ORDER BY cycle_number;
```

EXPLAIN ANALYZE for daily_reports hot path:
```sql
EXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)
SELECT submitted_at, brands_contacted, influencers_contacted
FROM public.daily_reports
WHERE student_id = '<test-uuid>'
  AND date = CURRENT_DATE;
```

RLS initplan verification:
```sql
SET LOCAL role = authenticated;
SET LOCAL "request.jwt.claims" = '{"sub": "<auth-uuid>"}';
EXPLAIN (ANALYZE, FORMAT TEXT)
SELECT * FROM public.work_sessions LIMIT 5;
-- Look for "InitPlan" nodes, not "SubPlan"
```

RLS policy audit (verify all use initplan wrappers):
```sql
SELECT schemaname, tablename, policyname, qual, with_check
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit RLS policies and create BASELINE.md scaffold</name>
  <files>.planning/phases/19-database-foundation/BASELINE.md</files>
  <read_first>
    - supabase/migrations/00009_database_foundation.sql (the migration to be applied)
    - supabase/migrations/00001_create_tables.sql (existing RLS policies to audit)
    - .planning/phases/19-database-foundation/19-RESEARCH.md (verification SQL queries)
  </read_first>
  <action>
**Step 1 — Audit RLS policies from source (DB-03):**

Read all RLS policies in `00001_create_tables.sql` and verify every policy uses `(select get_user_role())` or `(select get_user_id())` — never bare `auth.uid()` or `get_user_role()` without the scalar subquery wrapper. The research confirmed all 26 policies are correct; document this verification.

Also verify the helper functions `get_user_id()` and `get_user_role()` are both marked `STABLE` and `SECURITY DEFINER` (lines 143-161 of 00001).

**Step 2 — Create BASELINE.md scaffold:**

Create `.planning/phases/19-database-foundation/BASELINE.md` with the following structure. This is the exact content to write:

```markdown
# Phase 19: Database Foundation — Performance Baseline

**Captured:** [DATE]
**Database:** Supabase Postgres 17
**Migration applied:** 00009_database_foundation.sql

## RLS Initplan Verification (DB-03)

### Source Audit

All 26 RLS policies in `00001_create_tables.sql` verified to use `(select get_user_role())` and `(select get_user_id())` initplan wrappers. No bare `auth.uid()` calls found in any policy definition.

Helper functions confirmed:
- `get_user_id()`: LANGUAGE sql, STABLE, SECURITY DEFINER, SET search_path = public
- `get_user_role()`: LANGUAGE sql, STABLE, SECURITY DEFINER, SET search_path = public

### EXPLAIN Verification

[TO BE FILLED — Run the following in Supabase SQL Editor after migration:]

SET LOCAL role = authenticated;
SET LOCAL "request.jwt.claims" = '{"sub": "<auth-uuid>"}';
EXPLAIN (ANALYZE, FORMAT TEXT)
SELECT * FROM public.work_sessions LIMIT 5;

Expected: Output contains "InitPlan" nodes for get_user_role/get_user_id calls.

Result: [PASTE EXPLAIN OUTPUT HERE]

## Before Migration (DB-04)

### pg_stat_statements — Top 10 Slowest Queries

[TO BE FILLED — Run before applying migration 00009:]

| # | Query Snippet | Calls | Mean (ms) | Total (ms) | Rows | Cache Hit % |
|---|---------------|-------|-----------|------------|------|-------------|
| 1 | | | | | | |
| 2 | | | | | | |

## After Migration (DB-04)

### pg_stat_statements — Top 10 Slowest Queries

[TO BE FILLED — Run after applying migration 00009 and letting the app run:]

| # | Query Snippet | Calls | Mean (ms) | Total (ms) | Rows | Cache Hit % |
|---|---------------|-------|-----------|------------|------|-------------|
| 1 | | | | | | |
| 2 | | | | | | |

## Index Verification (DB-01)

### EXPLAIN ANALYZE — work_sessions Hot Path

Result: [PASTE EXPLAIN OUTPUT HERE]
Expected: Index Scan using idx_work_sessions_student_date (or idx_work_sessions_student_date_status)

### EXPLAIN ANALYZE — daily_reports Hot Path

Result: [PASTE EXPLAIN OUTPUT HERE]
Expected: Index Scan using idx_daily_reports_student_date

### EXPLAIN ANALYZE — work_sessions Status Filter

Result: [PASTE EXPLAIN OUTPUT HERE]
Expected: Index Scan using idx_work_sessions_student_date_status

### Notes

- If EXPLAIN shows Seq Scan instead of Index Scan, this may be correct behavior for small tables. Document this finding — the index exists and will be used at production scale.
- Phase 20 will reference this baseline for query optimization targets.
```

**Step 3 — Verify the RLS source audit programmatically:**

Run grep on 00001_create_tables.sql to confirm:
- Count of `(select get_user_role())` occurrences in policy definitions
- Count of `(select get_user_id())` occurrences in policy definitions
- Zero occurrences of bare `auth.uid()` in CREATE POLICY statements (the only `auth.uid()` should be inside the helper function body definitions on lines 150 and 158)
  </action>
  <verify>
    <automated>test -f .planning/phases/19-database-foundation/BASELINE.md && grep -c "RLS Initplan Verification" .planning/phases/19-database-foundation/BASELINE.md && grep -c "Before Migration" .planning/phases/19-database-foundation/BASELINE.md && grep -c "After Migration" .planning/phases/19-database-foundation/BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/19-database-foundation/BASELINE.md exists
    - BASELINE.md contains `## RLS Initplan Verification (DB-03)` section
    - BASELINE.md contains `## Before Migration (DB-04)` section with pg_stat_statements query template
    - BASELINE.md contains `## After Migration (DB-04)` section with pg_stat_statements query template
    - BASELINE.md contains `## Index Verification (DB-01)` section with 3 EXPLAIN ANALYZE query templates
    - BASELINE.md source audit confirms all 26 RLS policies use `(select get_user_role())` or `(select get_user_id())`
    - BASELINE.md confirms get_user_id() and get_user_role() are STABLE and SECURITY DEFINER
  </acceptance_criteria>
  <done>BASELINE.md scaffold created with RLS source audit complete, pg_stat_statements query templates ready, and EXPLAIN ANALYZE templates ready for human to fill in via Supabase SQL Editor</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human applies migration, enables pg_stat_statements, fills BASELINE.md</name>
  <files>.planning/phases/19-database-foundation/BASELINE.md</files>
  <action>
This is a human verification checkpoint. Claude has prepared migration 00009_database_foundation.sql and the BASELINE.md scaffold with all SQL queries pre-written. The human must now execute the database operations in Supabase Dashboard and SQL Editor, then fill in the results.

Steps for the human:
1. Enable pg_stat_statements via Supabase Dashboard (Database -> Extensions -> search "pg_stat_statements" -> Enable)
2. Run `SELECT pg_stat_statements_reset();` in SQL Editor to reset counters
3. Wait for some application activity (browse the app for 5 minutes)
4. Run the "Before Migration" pg_stat_statements query from BASELINE.md, paste results into the table
5. Copy the contents of `supabase/migrations/00009_database_foundation.sql` and run in SQL Editor
6. Verify output shows CREATE INDEX and CREATE EXTENSION success
7. Run `SELECT pg_stat_statements_reset();` again
8. Wait for some application activity
9. Run the "After Migration" pg_stat_statements query, paste results into BASELINE.md table
10. Run each EXPLAIN ANALYZE query (3 total) with a real student UUID from the users table, paste output into BASELINE.md
11. Run the RLS EXPLAIN query with a real auth UUID, confirm "InitPlan" appears in output, paste into BASELINE.md
12. Commit updated BASELINE.md
  </action>
  <verify>
    <automated>grep -c "PASTE EXPLAIN OUTPUT HERE" .planning/phases/19-database-foundation/BASELINE.md | grep -q "^0$" && echo "All sections filled" || echo "Some sections still have placeholder text"</automated>
  </verify>
  <acceptance_criteria>
    - BASELINE.md no longer contains "[PASTE EXPLAIN OUTPUT HERE]" placeholder text
    - BASELINE.md Before Migration table has at least 1 row of real data
    - BASELINE.md After Migration table has at least 1 row of real data
    - BASELINE.md Index Verification sections contain actual EXPLAIN output
    - BASELINE.md RLS EXPLAIN Verification section contains actual output with InitPlan (or documents why InitPlan is not visible)
  </acceptance_criteria>
  <done>BASELINE.md is fully populated with real Supabase query data: before/after pg_stat_statements snapshots, EXPLAIN ANALYZE results confirming index usage, and RLS initplan EXPLAIN verification</done>
</task>

</tasks>

<verification>
1. BASELINE.md exists with all sections filled in (Before, After, Index Verification, RLS Verification)
2. RLS source audit documents all 26 policies using initplan wrappers
3. pg_stat_statements is capturing query statistics on Supabase
4. EXPLAIN ANALYZE results document index usage on hot paths
5. Migration 00009 applied successfully to Supabase
</verification>

<success_criteria>
- BASELINE.md captures before/after top 10 slowest queries from pg_stat_statements (DB-04)
- RLS initplan verification completed via both source audit and EXPLAIN output (DB-03)
- EXPLAIN ANALYZE results confirm composite indexes are used by hot query paths (DB-01 verification)
- All results are version-controlled in BASELINE.md for Phase 20 reference
</success_criteria>

<output>
After completion, create `.planning/phases/19-database-foundation/19-02-SUMMARY.md`
</output>
