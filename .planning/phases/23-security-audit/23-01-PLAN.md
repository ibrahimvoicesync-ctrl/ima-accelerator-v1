---
phase: 23-security-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/23-security-audit/23-AUDIT-REPORT.md
autonomous: true
requirements: [SEC-02, SEC-04, DB-03]

must_haves:
  truths:
    - "Every API route's auth check, role verification, and ownership isolation is documented in the audit report"
    - "Cross-student data isolation gaps are identified with severity levels"
    - "RLS policies are confirmed to use initplan wrappers (DB-03 closure)"
    - "Proxy route guard defense-in-depth layer is documented"
    - "Each finding has a severity level: Critical / High / Medium / Info"
  artifacts:
    - path: ".planning/phases/23-security-audit/23-AUDIT-REPORT.md"
      provides: "Complete security audit report covering all 12 routes, proxy, and RLS"
      contains: "## Executive Summary"
    - path: ".planning/phases/23-security-audit/23-AUDIT-REPORT.md"
      provides: "DB-03 formal closure"
      contains: "## DB-03 Status"
  key_links:
    - from: "23-AUDIT-REPORT.md"
      to: "23-02-PLAN.md"
      via: "Human reviews report, approves remediation plan"
      pattern: "## Approval Instructions"
---

<objective>
Produce a comprehensive security audit report covering all three defense-in-depth layers (route handlers, proxy guard, RLS policies) across all 12 API routes. No code changes — read-only analysis.

Purpose: Per D-01/D-02/D-03, the report must be produced and reviewed by a human BEFORE any remediation code is written. This plan generates the report; Plan 02 applies fixes after human approval.

Output: `.planning/phases/23-security-audit/23-AUDIT-REPORT.md` — structured findings with severity levels per D-07/D-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-security-audit/23-CONTEXT.md
@.planning/phases/23-security-audit/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 12 API routes and proxy guard (Layer 1 + Layer 2)</name>
  <files>.planning/phases/23-security-audit/23-AUDIT-REPORT.md</files>
  <read_first>
    src/app/api/auth/signout/route.ts
    src/app/api/auth/callback/route.ts
    src/app/api/calendar/route.ts
    src/app/api/reports/route.ts
    src/app/api/reports/[id]/review/route.ts
    src/app/api/work-sessions/route.ts
    src/app/api/work-sessions/[id]/route.ts
    src/app/api/roadmap/route.ts
    src/app/api/invites/route.ts
    src/app/api/magic-links/route.ts
    src/app/api/assignments/route.ts
    src/app/api/alerts/dismiss/route.ts
    src/proxy.ts
    src/lib/supabase/admin.ts
    src/lib/supabase/server.ts
    src/lib/session.ts
    src/lib/rate-limit.ts
    .planning/phases/23-security-audit/23-RESEARCH.md
  </read_first>
  <action>
Read every API route file and src/proxy.ts. For EACH of the 12 routes, document in a table:

**Route-by-route table columns:**
| Route | Method | Auth Check | Role Check | Ownership/Isolation | CSRF Check | Rate Limited | Gaps Found |

**Per D-04 (three-layer audit), document:**

**Layer 1 — Route Handler Analysis:**
For each of the 12 routes (per D-05), record:
1. Whether `createClient() -> auth.getUser()` is called (auth check)
2. Whether profile role is verified after admin client lookup (role check)
3. Whether queries filter by `profile.id` or equivalent ownership check (isolation)
4. Whether CSRF Origin header is checked (will be "None" for all — this is the universal gap per research)
5. Whether `checkRateLimit()` is called (already done in Phase 22 for mutations)
6. Any route-specific gaps (e.g., reports/[id]/review report-ID probing — Gap 5 from research)

**Layer 2 — Proxy Route Guard:**
Document the checks in `src/proxy.ts`:
- Auth check on protected routes (getUser -> redirect /login)
- Role-based route access (ROLE_ROUTE_ACCESS map)
- API route exclusion from proxy (matcher excludes `api/`)
- Note the Info-level finding: proxy.ts uses direct `createClient()` instead of `createAdminClient()` singleton

**Also check:** Run `grep -rn "api/auth/signout" src/` to confirm whether the signout route is dead code (no client references). Document result.

**Per D-07/D-08 severity classification:**
- Missing auth check on mutation = Critical
- Missing CSRF on mutation = High
- Missing CSRF on read-only GET = Info
- Missing ownership filter with RLS backup = Medium
- Dead code route with no auth = Info

**Create findings with this format:**

```
### [FIND-NN] -- {Severity} -- {Route} -- {Short title}
- **Layer:** Route handler / Proxy / RLS
- **File:** src/app/api/.../route.ts (line N)
- **Description:** [what is missing/wrong]
- **Attack vector:** [what a malicious actor could do]
- **Proposed fix:** [one-line description]
```

Expected findings based on research (verify each one independently):
- FIND-01: High — ALL mutation routes — No CSRF Origin header verification
- FIND-02: Info — auth/signout — Dead code with no auth guard
- FIND-03: Medium — auth/callback GET — No rate limiting on unauthenticated user-creating endpoint
- FIND-04: Info — calendar GET — No rate limiting (read-only, low risk)
- FIND-05: Medium — reports/[id]/review — Report ID probing via 404 vs 403 distinction
- FIND-06: Info — proxy.ts — Uses direct createClient() not admin singleton

Write the first sections of 23-AUDIT-REPORT.md:
```markdown
# Security Audit Report -- Phase 23

**Audited:** {date}
**Routes:** 12 / 12 covered
**Layers:** Route handlers, proxy guard, RLS policies

## Executive Summary
[Number of findings by severity, overall posture assessment]

## Route-by-Route Audit Table
[Full table with all 12 routes]

## Findings
[All FIND-NN entries]

## Layer 2: Proxy Route Guard
[Proxy analysis]
```

Do NOT include Layer 3 (RLS) yet — that is Task 2.
  </action>
  <verify>
    <automated>grep -c "FIND-" .planning/phases/23-security-audit/23-AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - 23-AUDIT-REPORT.md contains "## Executive Summary"
    - 23-AUDIT-REPORT.md contains "## Route-by-Route Audit Table"
    - 23-AUDIT-REPORT.md contains "## Findings"
    - 23-AUDIT-REPORT.md contains "## Layer 2: Proxy Route Guard"
    - 23-AUDIT-REPORT.md contains at least 5 "### [FIND-" entries (one per confirmed gap)
    - Every finding has "Severity", "Layer", "File", "Description", "Attack vector", "Proposed fix"
    - All 12 routes appear in the audit table (auth/signout, auth/callback, calendar, reports, reports/[id]/review, work-sessions, work-sessions/[id], roadmap, invites, magic-links, assignments, alerts/dismiss)
    - grep "12 / 12" 23-AUDIT-REPORT.md returns a match (all routes covered)
  </acceptance_criteria>
  <done>Layer 1 (route handlers) and Layer 2 (proxy) are fully audited with severity-classified findings. All 12 routes documented in table. Findings include attack vectors and proposed fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Audit RLS policies (Layer 3) and close DB-03</name>
  <files>.planning/phases/23-security-audit/23-AUDIT-REPORT.md</files>
  <read_first>
    supabase/migrations/00001_create_tables.sql
    supabase/migrations/00004_alert_dismissals.sql
    supabase/migrations/00006_v1_1_schema.sql
    .planning/phases/23-security-audit/23-AUDIT-REPORT.md
  </read_first>
  <action>
Read all three migration files that contain RLS policies. For each table's RLS policies, verify:

1. The policy uses `(select get_user_role())` NOT bare `get_user_role()` or `auth.uid()` — this is the initplan optimization pattern (DB-03)
2. Student-scoped policies use `(select get_user_id())` for row-level filtering
3. No policy has bare `auth.uid()` calls (which would cause per-row volatile function scans)

**Tables to audit (from 00001_create_tables.sql):**
- users: SELECT (owner/coach/student), UPDATE (owner/coach/student)
- invites: SELECT (owner/coach), INSERT (owner/coach)
- magic_links: SELECT/INSERT/UPDATE (owner/coach)
- work_sessions: SELECT (owner/coach/student), INSERT/UPDATE/DELETE (student)
- roadmap_progress: SELECT/UPDATE (owner/coach/student)
- daily_reports: SELECT (owner/coach/student), INSERT/UPDATE (student), UPDATE (coach)

**From 00004_alert_dismissals.sql:**
- alert_dismissals: SELECT/INSERT (owner only)

**From 00006_v1_1_schema.sql:**
- Check for any new RLS policies added in v1.1

Append to 23-AUDIT-REPORT.md the following sections:

```markdown
## Layer 3: RLS Policy Audit

| Table | Policy Name | Operation | initplan Wrapper | Status |
|-------|-------------|-----------|-----------------|--------|
[One row per policy, Status = PASS if uses (select get_user_role()) / (select get_user_id()), FAIL otherwise]

### Admin Client Bypass Note
All 10 mutation route handlers use `createAdminClient()` (service_role key) which bypasses RLS entirely. RLS is the third defense line — it protects against direct DB access but NOT against application-level bugs. The application-level checks (auth + role + ownership filter via `eq("student_id", profile.id)`) documented in Layer 1 are the PRIMARY security gates.

## DB-03 Status

**Requirement:** All RLS policies use `(SELECT auth.uid())` instead of bare `auth.uid()` for initplan optimization.

**Finding:** All policies in 00001_create_tables.sql and 00004_alert_dismissals.sql use `(select get_user_role())` and `(select get_user_id())` wrappers — which internally call `auth.uid()` inside a subselect. No bare `auth.uid()` exists in any policy.

**Status:** SATISFIED. DB-03 is formally closed.

## Approval Instructions

This audit report is the output of Phase 23 Plan 1. To proceed with remediation:

1. Review all findings above
2. For each finding, confirm whether the proposed fix should be applied
3. Type "approved" or list which findings to fix/skip
4. Plan 2 will then apply CSRF protection and any approved ownership fixes
```
  </action>
  <verify>
    <automated>grep -c "PASS\|FAIL" .planning/phases/23-security-audit/23-AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - 23-AUDIT-REPORT.md contains "## Layer 3: RLS Policy Audit"
    - 23-AUDIT-REPORT.md contains "## DB-03 Status"
    - 23-AUDIT-REPORT.md contains "SATISFIED" in DB-03 section
    - 23-AUDIT-REPORT.md contains "## Approval Instructions"
    - The RLS audit table covers all 7 tables: users, invites, magic_links, work_sessions, roadmap_progress, daily_reports, alert_dismissals
    - Every policy row has a "PASS" or "FAIL" status
    - 23-AUDIT-REPORT.md contains "Admin Client Bypass Note"
    - grep "select get_user_role" 23-AUDIT-REPORT.md returns matches (initplan pattern documented)
  </acceptance_criteria>
  <done>All RLS policies audited table-by-table with initplan verification. DB-03 formally closed. Approval instructions present for human reviewer. Complete audit report ready for human sign-off.</done>
</task>

</tasks>

<verification>
1. `grep "## Executive Summary" .planning/phases/23-security-audit/23-AUDIT-REPORT.md` — report has executive summary
2. `grep "12 / 12" .planning/phases/23-security-audit/23-AUDIT-REPORT.md` — all 12 routes covered
3. `grep -c "FIND-" .planning/phases/23-security-audit/23-AUDIT-REPORT.md` — at least 5 findings documented
4. `grep "DB-03" .planning/phases/23-security-audit/23-AUDIT-REPORT.md` — DB-03 closure documented
5. `grep "## Approval Instructions" .planning/phases/23-security-audit/23-AUDIT-REPORT.md` — human review gate present
</verification>

<success_criteria>
- Complete audit report exists at `.planning/phases/23-security-audit/23-AUDIT-REPORT.md`
- All 12 API routes documented in route-by-route table
- All three defense-in-depth layers audited (route handlers, proxy, RLS)
- Every finding has severity level per D-07/D-08
- DB-03 (RLS initplan) formally closed with evidence
- Approval instructions present for human reviewer before Plan 2 executes
- No code files modified (read-only audit per D-01)
</success_criteria>

<output>
After completion, create `.planning/phases/23-security-audit/23-01-SUMMARY.md`
</output>
