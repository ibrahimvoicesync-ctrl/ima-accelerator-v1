---
phase: 25-roadmap-config-stage-headers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/config.ts
  - src/components/student/RoadmapClient.tsx
autonomous: true
requirements:
  - ROAD-01
  - ROAD-02
  - ROAD-03
  - ROAD-04
  - ROAD-05

must_haves:
  truths:
    - "Steps 1-8 descriptions each contain parenthetical time guidance text"
    - "Step 5 shows the skool CRM link as its unlock URL"
    - "Step 6 has no unlock URL (null)"
    - "Step 6 description reads exactly 'Build 100 Influencer Lead List, and Watch 3 Influencer Roast My Email'"
    - "Step 7 title and description focus on drafting emails only — no 'Roast My Email' reference"
    - "Step 8 has target_days of 14 and the deadline chip renders correctly"
    - "Student roadmap page groups steps under three visible stage headers: Setup & Preparation, Influencer Outreach, Brand Outreach"
    - "isLast prop is true for the last step within each stage, not just the global last step"
  artifacts:
    - path: "src/lib/config.ts"
      provides: "Updated ROADMAP_STEPS with corrected descriptions, URLs, and target_days"
      contains: "(time asap)"
    - path: "src/components/student/RoadmapClient.tsx"
      provides: "Stage-grouped roadmap rendering with headers"
      contains: "stageName"
  key_links:
    - from: "src/components/student/RoadmapClient.tsx"
      to: "src/lib/config.ts"
      via: "ROADMAP_STEPS import — stage/stageName fields drive grouping"
      pattern: "ROADMAP_STEPS\\.filter.*stage"
    - from: "src/components/student/RoadmapClient.tsx"
      to: "src/components/student/RoadmapStep.tsx"
      via: "isLast prop — per-stage, not global"
      pattern: "isLast.*stageSteps\\.length"
---

<objective>
Update ROADMAP_STEPS config with corrected descriptions, unlock URLs, and target_days per decisions D-01 through D-06, then add stage header grouping to the student roadmap view.

Purpose: Students see accurate step content with time guidance and their roadmap is visually grouped into three stages for better navigation.
Output: Updated config.ts and stage-grouped RoadmapClient.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-roadmap-config-stage-headers/25-CONTEXT.md
@.planning/phases/25-roadmap-config-stage-headers/25-RESEARCH.md

@src/lib/config.ts
@src/components/student/RoadmapClient.tsx
@src/components/student/RoadmapStep.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/student/RoadmapStep.tsx:
```typescript
interface RoadmapStepProps {
  step: {
    step_number: number;
    title: string;
    description: string;
    target_days: number | null;
    unlock_url: string | null;
  };
  progress: RoadmapProgress | null;
  isLast: boolean;  // Controls whether connecting line is drawn below this step
  joinedAt: string;
  onComplete: (stepNumber: number) => void;
}
```

From src/lib/config.ts (current ROADMAP_STEPS shape):
```typescript
export const ROADMAP_STEPS = [
  { step: number, stage: number, stageName: string, title: string, description: string,
    autoComplete?: boolean, target_days: number | null, unlock_url: string | null },
  // ...
] as const;
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP_STEPS config with corrected content</name>
  <files>src/lib/config.ts</files>
  <read_first>
    - src/lib/config.ts (lines 128-147 — current ROADMAP_STEPS array to see all current values)
    - .planning/phases/25-roadmap-config-stage-headers/25-CONTEXT.md (decisions D-01 through D-06)
  </read_first>
  <action>
Edit the ROADMAP_STEPS array in src/lib/config.ts (lines 128-147). Make these exact changes:

**Per D-01 (ROAD-01) — Append parenthetical time guidance to descriptions for steps 1-8:**
- Step 1: description becomes `"Complete your onboarding and set up your profile (time asap)"`
- Step 2: description becomes `"Watch the welcome videos and complete the introductory chapter (2 hrs)"`
- Step 3: description becomes `"Choose the influencer niche you will focus on and complete the niche chapter (1hr - don't overthink it)"`
- Step 4: description becomes `"Set up your agency infrastructure — email, domain, and professional presence (begin on day 1 - finish by day 2)"`
- Step 5: description becomes `"Start your 14-day email warmup to build sender reputation (Day 3)"`
- Step 6: description becomes `"Build 100 Influencer Lead List, and Watch 3 Influencer Roast My Email (Day 6)"` (per D-04, this is the new description; parenthetical per D-01)
- Step 7: description becomes `"Draft your first outreach emails using the templates and frameworks from the course (volume is key here)"` (per D-05, new drafting-only description; parenthetical per D-01)
- Step 8: description becomes `"Send your first outreach email to an influencer (Day 14)"` (parenthetical per D-01)

**Per D-02 (ROAD-02) — Step 5 unlock_url:**
- Change step 5 `unlock_url` from `null as string | null` to `"https://www.skool.com/the-ima-accelerator-9388/ultimate-influencer-brand-crm-organize-your-contacts" as string | null`

**Per D-03 (ROAD-02) — Step 6 unlock_url removal:**
- Change step 6 `unlock_url` from `"https://www.skool.com/ima-accelerator/classroom" as string | null` to `null as string | null`

**Per D-04 (ROAD-03) — Step 6 title and description:**
- Step 6 title stays: `"Build 100 Influencer Lead List"` — WAIT, the current title is already `"Build 100 Influencer Lead List"`. The description update is covered above. But the step 6 description per ROAD-03 exact text is "Build 100 Influencer Lead List, and Watch 3 Influencer Roast My Email" — this is the DESCRIPTION not title. Title stays as-is: `"Build 100 Influencer Lead List"`. Description becomes `"Build 100 Influencer Lead List, and Watch 3 Influencer Roast My Email (Day 6)"`.

Actually, re-reading CONTEXT.md D-04 more carefully: "Step 6 description: 'Build 100 Influencer Lead List, and Watch 3 Influencer Roast My Email'". The current step 6 title is "Build 100 Influencer Lead List" and current description is "Research and compile a list of 100 influencers in your niche using the CRM". Update the description to the D-04 text plus the D-01 parenthetical. Title remains unchanged.

**Per D-05 (ROAD-03) — Step 7 title and description:**
- Change step 7 title from `"Watch 3 Roast My Email Calls + Draft First Outreach Emails"` to `"Draft Your First Outreach Emails"`
- Change step 7 description from `"Watch 3 roast-my-email call recordings and draft your first outreach emails"` to `"Draft your first outreach emails using the templates and frameworks from the course (volume is key here)"` (description already includes D-01 parenthetical)

**Per D-06 (ROAD-04) — Step 8 target_days:**
- Change step 8 `target_days` from `null as number | null` to `14 as number | null`

Do NOT change any steps 9-15. Do NOT change titles for steps 1-5 or step 8. Do NOT add any new fields to the array.
  </action>
  <verify>
    <automated>cd C:/Users/ibrah/ima-accelerator-v1 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - config.ts step 1 description contains "(time asap)"
    - config.ts step 2 description contains "(2 hrs)"
    - config.ts step 3 description contains "(1hr - don't overthink it)"
    - config.ts step 4 description contains "(begin on day 1 - finish by day 2)"
    - config.ts step 5 description contains "(Day 3)"
    - config.ts step 5 unlock_url contains "skool.com/the-ima-accelerator-9388/ultimate-influencer-brand-crm"
    - config.ts step 6 description contains "Build 100 Influencer Lead List, and Watch 3 Influencer Roast My Email"
    - config.ts step 6 description contains "(Day 6)"
    - config.ts step 6 unlock_url is `null`
    - config.ts step 7 title is "Draft Your First Outreach Emails" (no "Roast My Email")
    - config.ts step 7 description contains "(volume is key here)"
    - config.ts step 7 title does NOT contain "Roast"
    - config.ts step 8 description contains "(Day 14)"
    - config.ts step 8 target_days is `14` (not null)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 8 step descriptions have parenthetical time guidance. Step 5 has the skool CRM unlock URL. Step 6 unlock_url is null. Step 6 description matches D-04 text. Step 7 title and description reflect drafting only. Step 8 target_days is 14. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add stage header grouping to student RoadmapClient</name>
  <files>src/components/student/RoadmapClient.tsx</files>
  <read_first>
    - src/components/student/RoadmapClient.tsx (full file — see current flat ROADMAP_STEPS.map iteration)
    - src/components/student/RoadmapStep.tsx (isLast prop controls connecting line — must be per-stage)
    - src/lib/config.ts (stage and stageName fields on each step)
  </read_first>
  <action>
Modify the JSX in RoadmapClient.tsx to group steps by stage with visible stage headers. Replace the flat `ROADMAP_STEPS.map(...)` block (lines 87-106) with stage-grouped rendering.

**Step 1 — Derive stages from config (add before the return statement, inside the component):**
```typescript
const stages = [...new Map(
  ROADMAP_STEPS.map(s => [s.stage, s.stageName])
).entries()].map(([stage, stageName]) => ({ stage, stageName }));
```

This produces: `[{stage:1, stageName:"Setup & Preparation"}, {stage:2, stageName:"Influencer Outreach"}, {stage:3, stageName:"Brand Outreach"}]`

**Step 2 — Replace the flat map with grouped rendering:**

Replace the content inside the `<div className="motion-safe:animate-slideUp" ...>` (the outer animated div stays unchanged — keep its className and style attributes exactly as-is):

```tsx
{stages.map(({ stage, stageName }, stageIdx) => {
  const stageSteps = ROADMAP_STEPS.filter(s => s.stage === stage);
  return (
    <div key={stage}>
      {/* Stage header */}
      <div className={cn(
        "flex items-center gap-3 pb-2",
        stageIdx > 0 && "pt-8"
      )}>
        <span className="text-xs font-semibold uppercase tracking-wider text-ima-text-muted">
          {stageName}
        </span>
        <div className="flex-1 h-px bg-ima-border" aria-hidden="true" />
      </div>
      {/* Stage steps */}
      {stageSteps.map((step, i) => {
        const stepProgress = progress.find((p) => p.step_number === step.step) ?? null;
        return (
          <RoadmapStep
            key={step.step}
            step={{
              step_number: step.step,
              title: step.title,
              description: step.description,
              target_days: step.target_days,
              unlock_url: step.unlock_url,
            }}
            progress={stepProgress}
            isLast={i === stageSteps.length - 1}
            joinedAt={joinedAt}
            onComplete={(stepNumber) => setConfirmStep(stepNumber)}
          />
        );
      })}
    </div>
  );
})}
```

**Step 3 — Add cn import if not already present:**
The file does not currently import `cn`. Add `import { cn } from "@/lib/utils";` to the imports.

**Critical details:**
- `isLast={i === stageSteps.length - 1}` — this is per-STAGE last, not global last. This prevents the connecting line from extending past the last step of each stage into the next stage header.
- `stageIdx > 0 && "pt-8"` — first stage header has no top padding; subsequent stages get pt-8 spacing for visual separation.
- `aria-hidden="true"` on the decorative horizontal rule div.
- stageName comes from config (not hardcoded) per CLAUDE.md rule "Config is truth".
- The outer `<div className="motion-safe:animate-slideUp" style={{...}}>` wrapper remains unchanged.
- No new animations are added (no additional animate-* classes needed).
  </action>
  <verify>
    <automated>cd C:/Users/ibrah/ima-accelerator-v1 && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - RoadmapClient.tsx contains `import { cn } from "@/lib/utils"`
    - RoadmapClient.tsx contains `stages.map` (stage grouping iteration)
    - RoadmapClient.tsx contains `ROADMAP_STEPS.filter(s => s.stage === stage)` (stage filtering)
    - RoadmapClient.tsx contains `text-ima-text-muted` (stage header text color using ima-* token)
    - RoadmapClient.tsx contains `bg-ima-border` (stage header rule using ima-* token)
    - RoadmapClient.tsx contains `aria-hidden="true"` on the decorative rule div
    - RoadmapClient.tsx contains `isLast={i === stageSteps.length - 1}` (per-stage isLast, NOT global)
    - RoadmapClient.tsx does NOT contain `isLast={i === ROADMAP_STEPS.length - 1}` (old global pattern removed)
    - RoadmapClient.tsx still contains `motion-safe:animate-slideUp` (outer animation preserved)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Student roadmap page renders three stage headers (Setup & Preparation, Influencer Outreach, Brand Outreach) with steps grouped under each. Connecting lines stop at stage boundaries. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles with no errors
2. `npm run build` — Next.js production build succeeds
3. `npm run lint` — ESLint passes on all changed files
4. Grep config.ts for "(time asap)" confirms ROAD-01 step 1
5. Grep config.ts for "target_days: 14" confirms ROAD-04
6. Grep config.ts for "skool.com/the-ima-accelerator-9388" confirms ROAD-02 step 5
7. Grep config.ts step 6 for `unlock_url: null` confirms ROAD-02 step 6
8. Grep RoadmapClient.tsx for "stageName" confirms stage headers exist
9. Grep RoadmapClient.tsx for "stageSteps.length - 1" confirms per-stage isLast
</verification>

<success_criteria>
- All 8 active step descriptions contain parenthetical time guidance (ROAD-01)
- Step 5 unlock_url is the skool CRM link; step 6 unlock_url is null (ROAD-02)
- Step 6 description matches D-04 text; step 7 reflects drafting only (ROAD-03)
- Step 8 target_days is 14 (ROAD-04)
- Student roadmap renders three stage headers from config stageName values (ROAD-05)
- Build, type check, and lint all pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-roadmap-config-stage-headers/25-01-SUMMARY.md`
</output>
