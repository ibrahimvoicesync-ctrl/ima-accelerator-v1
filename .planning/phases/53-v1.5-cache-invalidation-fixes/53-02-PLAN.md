---
phase: 53-v1.5-cache-invalidation-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/coach/analytics/export.csv/route.ts
autonomous: true
requirements:
  - PERF-02
gap_closure: true
must_haves:
  truths:
    - "A coach making > 30 CSV export requests per minute receives a 429 on the 31st request."
    - "The 429 response matches the standard JSON envelope used by every other coach/mutation API route in the codebase (`{ error: ... }` body + `Retry-After` header)."
    - "Existing auth + role gate + Zod param validation + EXPORT_HARD_CAP enforcement are preserved — no behavior regression for legitimate export use."
    - "Rate-limit log entry is keyed on the coach's user_id and `/api/coach/analytics/export.csv` endpoint string."
  artifacts:
    - path: "src/app/api/coach/analytics/export.csv/route.ts"
      provides: "Rate-limited CSV export for coach analytics"
      contains: "checkRateLimit"
  key_links:
    - from: "src/app/api/coach/analytics/export.csv/route.ts GET"
      to: "src/lib/rate-limit.ts checkRateLimit"
      via: "awaited call using `user.id` + endpoint string, AFTER role gate, BEFORE param validation"
      pattern: "await checkRateLimit\\(user\\.id, \"/api/coach/analytics/export\\.csv\"\\)"
---

<objective>
Close the MINOR integration gap from v1.5-MILESTONE-AUDIT.md:
`src/app/api/coach/analytics/export.csv/route.ts` currently has auth + role
check but no `checkRateLimit` call. A coach could loop the 5000-row RPC
without bound, wasting DB cycles and bandwidth.

Purpose: Apply the canonical 30 req/min/user rate-limit pattern (same as
every other coach/mutation route) to the CSV export GET handler. Satisfies
PERF-02 "All new API endpoints enforce auth + role verification + rate
limiting (30 req/min/user via existing checkRateLimit)".

Output: Patched GET handler that calls `checkRateLimit(user.id,
"/api/coach/analytics/export.csv")` after role gate and returns the standard
429 JSON envelope on limit breach.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/v1.5-MILESTONE-AUDIT.md

# Reference files (read, do not modify)
@src/lib/rate-limit.ts
@src/lib/session.ts
@src/app/api/alerts/dismiss/route.ts

<interfaces>
<!-- Rate limit function signature. -->

From src/lib/rate-limit.ts:
```typescript
export async function checkRateLimit(
  userId: string,
  endpoint: string,
  maxRequests?: number,        // default 30
  windowMinutes?: number       // default 1
): Promise<{
  allowed: boolean;
  remaining: number;
  retryAfterSeconds: number;
}>;
```

Canonical 429 envelope pattern used by every other coach/mutation route
(copy exactly — see src/app/api/alerts/dismiss/route.ts lines 41-48):
```typescript
const { allowed, retryAfterSeconds } = await checkRateLimit(
  user.id,
  "/api/coach/analytics/export.csv"
);
if (!allowed) {
  return NextResponse.json(
    { error: `Too many requests, try again in ${retryAfterSeconds} seconds.` },
    { status: 429, headers: { "Retry-After": String(retryAfterSeconds) } }
  );
}
```

From src/lib/session.ts: `getSessionUser()` already returns `{ id: string, role: Role, ... }`
— use `user.id` (the profile UUID, NOT authId) as the rate-limit key so
rate-limit logs match every other route.

Note: The existing export route uses `NextResponse` for 400/403/500 paths
with plain-text bodies. The 429 path MUST use JSON envelope per PERF-02 and
match every other route; do NOT match the plain-text style for 429.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate limit to coach analytics CSV export GET handler</name>
  <files>src/app/api/coach/analytics/export.csv/route.ts</files>
  <read_first>
    - src/app/api/coach/analytics/export.csv/route.ts (entire file — understand current GET flow and error-response style)
    - src/lib/rate-limit.ts (entire file — 63 lines; verify signature and return shape)
    - src/app/api/alerts/dismiss/route.ts lines 41-48 (canonical 429 envelope pattern to copy)
  </read_first>
  <action>
    Edit `src/app/api/coach/analytics/export.csv/route.ts`:

    1. **Add import** alongside existing imports (after line 16, near
       `fetchCoachAnalytics`):
       ```typescript
       import { checkRateLimit } from "@/lib/rate-limit";
       ```

    2. **Insert the rate-limit check** in the GET handler, immediately AFTER
       the role gate (current line 63 — `if (user.role !== "coach") ... 403`)
       and BEFORE the param parsing (current lines 65-72). The numbered
       comments in the file use `// 1. Auth + role gate.` / `// 2. Validate
       params.` — insert this as a new step `// 2. Rate limit.` and renumber
       the subsequent comments from 2→3, 3→4, 4→5, 5→6.

       Exact insert (place after the role-gate `return NextResponse.json(...,
       { status: 403 })` block closes, before the `// 2. Validate params.`
       comment):
       ```typescript
       // 2. Rate limit (per D-01, D-04 — 30 req/min/user matches all coach API routes).
       const { allowed, retryAfterSeconds } = await checkRateLimit(
         user.id,
         "/api/coach/analytics/export.csv"
       );
       if (!allowed) {
         return NextResponse.json(
           { error: `Too many requests, try again in ${retryAfterSeconds} seconds.` },
           { status: 429, headers: { "Retry-After": String(retryAfterSeconds) } }
         );
       }
       ```

    3. **Renumber** the existing inline step comments (the file currently has
       `// 1.` through `// 5.` — after insertion they should read
       `// 1.` auth, `// 2.` rate limit, `// 3.` validate params, `// 4.`
       fetch, `// 5.` build CSV body, `// 6.` return attachment).
    4. **Do not** change the 400 / 403 / 500 paths (they correctly use plain
       `NextResponse("text", { status })` for CSV-oriented failures).
    5. **Do not** change the default `checkRateLimit` args — the default is
       30 req/min, which is the mandated policy per PERF-02. Do NOT pass
       explicit `maxRequests` / `windowMinutes`.
    6. **Do not** add CSRF `verifyOrigin` — GET is a safe method and the
       existing route correctly omits it (matches `GET /api/deals` pattern).
  </action>
  <verify>
    <automated>
      cd "C:/Users/ibrah/ima-accelerator-v1" &amp;&amp; \
      grep -q 'import { checkRateLimit }' "src/app/api/coach/analytics/export.csv/route.ts" &amp;&amp; \
      grep -q 'await checkRateLimit(' "src/app/api/coach/analytics/export.csv/route.ts" &amp;&amp; \
      grep -q '"/api/coach/analytics/export.csv"' "src/app/api/coach/analytics/export.csv/route.ts" &amp;&amp; \
      grep -q 'status: 429' "src/app/api/coach/analytics/export.csv/route.ts" &amp;&amp; \
      grep -q '"Retry-After": String(retryAfterSeconds)' "src/app/api/coach/analytics/export.csv/route.ts" &amp;&amp; \
      npx tsc --noEmit &amp;&amp; \
      npm run lint
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import { checkRateLimit } from "@/lib/rate-limit"' src/app/api/coach/analytics/export.csv/route.ts` exits 0
    - `grep -q 'await checkRateLimit(' src/app/api/coach/analytics/export.csv/route.ts` exits 0
    - `grep -q '"/api/coach/analytics/export.csv"' src/app/api/coach/analytics/export.csv/route.ts` exits 0 (endpoint key string present)
    - `grep -q 'status: 429' src/app/api/coach/analytics/export.csv/route.ts` exits 0 (429 branch present)
    - `grep -q '"Retry-After": String(retryAfterSeconds)' src/app/api/coach/analytics/export.csv/route.ts` exits 0 (Retry-After header present)
    - `grep -c 'checkRateLimit' src/app/api/coach/analytics/export.csv/route.ts` returns exactly 2 (import + call, no double-call)
    - `grep -q 'maxRequests' src/app/api/coach/analytics/export.csv/route.ts` exits NON-ZERO (no override of defaults; 30/min policy preserved)
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    CSV export GET handler rejects the 31st request/minute with a 429 JSON envelope + `Retry-After` header matching every other coach/mutation route; 5000-row RPC call is not reached on limit breach; type-check and lint pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach client → /api/coach/analytics/export.csv GET | Authenticated coach read that triggers an expensive 5000-row RPC + CSV stream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53.02-01 | Denial of Service | CSV export loop consuming 5000-row RPC repeatedly | mitigate | This plan — `checkRateLimit(user.id, ...)` caps at 30 req/min/coach. On breach, 429 returned before `fetchCoachAnalytics` is invoked. |
| T-53.02-02 | Information Disclosure | coach exporting another coach's students' data | mitigate | Already mitigated upstream: `fetchCoachAnalytics(user.id, ...)` scopes the RPC by coach_id; role gate ensures only `role === "coach"` reaches the call. Unchanged by this plan. |
| T-53.02-03 | Tampering | client spoofing `user.id` in rate-limit key | mitigate | `user.id` is derived from `getSessionUser()` which reads `auth.getUser()` via the server-side Supabase client; not controllable via request body or query params. |
| T-53.02-04 | Repudiation | coach denying an export happened | accept | `rate_limit_log` table records (user_id, endpoint, called_at) triples; sufficient audit trail for this v1.5 surface. No additional logging required. |
</threat_model>

<verification>
- `checkRateLimit(user.id, "/api/coach/analytics/export.csv")` fires on every GET after role gate.
- 429 response uses the exact same JSON envelope + `Retry-After` header as every other coach/mutation route.
- `fetchCoachAnalytics` is NOT called when `allowed === false` (early-return on 429).
- Legitimate export use (within 30 req/min) is unaffected — existing auth, param validation, 5000-row cap, CSV body, and attachment headers are preserved.
- `npx tsc --noEmit` and `npm run lint` pass with zero errors.
</verification>

<success_criteria>
- Coach hitting `/api/coach/analytics/export.csv` 30 times in under a minute succeeds; the 31st call returns 429 with `{ error: "Too many requests, try again in 60 seconds." }` body and `Retry-After: 60` header.
- No behavior change for normal export flow (single click → CSV downloads).
- CSV body content, filename pattern, and `Cache-Control: no-store` header are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/53-v1.5-cache-invalidation-fixes/53-02-SUMMARY.md`
</output>
