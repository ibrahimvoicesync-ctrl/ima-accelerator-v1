---
phase: 53-v1.5-cache-invalidation-fixes
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - COACH-DASH-04
  - COACH-DASH-06
  - COACH-ANALYTICS-07
  - PERF-02
  - PERF-05
  - DEALS-09
gap_closure: true
must_haves:
  truths:
    - "REQUIREMENTS.md traceability reflects shipped reality: all 43 shipped v1.5 requirement sections (ANALYTICS-01..10, COACH-DASH-01..07, COACH-ANALYTICS-01..07, DEALS-01..11, PERF-01..08) are `[x]`."
    - "NOTIF-01 is `[ ]` (deferred — D-06 Tech/Email Setup step pending Monday stakeholder meeting per STATE.md). This requires flipping it FROM `[x]` → `[ ]` because REQUIREMENTS.md line 71 currently shows `[x]` and that is a pre-existing bookkeeping error per the v1.5-MILESTONE-AUDIT.md (which places NOTIF-01 in the `deferred` block)."
    - "NOTIF-06 is `[x]` — it was `[ ]` pre-phase and must be flipped to `[x]` because the audit confirms 54/54 requirements are satisfied with NOTIF-01 as the sole deferred entry. NOTIF-06 shipped in Phase 52 (coach alerts grouping) and its message-content + click-navigation behavior was verified."
    - "NOTIF-02, 03, 04, 05, 07, 08, 09, 10, 11 (already `[x]` pre-phase) remain `[x]` — no regression on already-correct checkboxes. Post-change the NOTIF section has exactly 9 checked + 1 unchecked (NOTIF-01) + 1 flipped-to-checked (NOTIF-06) = 10 `[x]` and 1 `[ ]` total."
    - "The Coverage line at the bottom of REQUIREMENTS.md is unchanged: `**Coverage:** 47/47 primary REQ-IDs mapped ... Zero orphans.` (this text maps REQ-IDs to owning phases, structural bookkeeping — it does not change when checkbox states change)."
    - "No REQ-ID text, phase mapping, or traceability table row is modified — only checkbox state on the bullet lines changes. Traceability table (REQ-ID → phase name mapping) is immutable bookkeeping."
    - "Total line changes to REQUIREMENTS.md: 45 (44 `[ ]`→`[x]` flips + 1 `[x]`→`[ ]` flip). Breakdown: 10 ANALYTICS + 7 COACH-DASH + 7 COACH-ANALYTICS + 11 DEALS + 8 PERF + 1 NOTIF-06 = 44 `[ ]`→`[x]`; plus 1 NOTIF-01 `[x]`→`[ ]`."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability checkboxes matching v1.5-MILESTONE-AUDIT.md reality (53 shipped `[x]` + 1 deferred `[ ]` = 54 in scope)"
      contains: "- [x] **ANALYTICS-01**"
      contains_any:
        - "- [x] **PERF-08**"
        - "- [ ] **NOTIF-01**"
        - "- [x] **NOTIF-06**"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/v1.5-MILESTONE-AUDIT.md scores.requirements 54/54"
      via: "checkbox state matches audit evidence (53 satisfied [x] + 1 deferred [ ] = 54)"
      pattern: "\\*\\*Coverage:\\*\\*"
---

<objective>
Close the bookkeeping gap from v1.5-MILESTONE-AUDIT.md: REQUIREMENTS.md
currently shows `[ ]` for all ANALYTICS, COACH-DASH, COACH-ANALYTICS, DEALS,
and PERF requirements plus NOTIF-06 even though `scores.requirements: 54/54`
in the audit confirms every one shipped. It also currently shows NOTIF-01 as
`[x]` even though the audit and STATE.md D-06 blocker both classify it as
deferred. This is traceability lag — execute-phase does not auto-tick these
boxes and did not catch the NOTIF-01 pre-existing error.

Also: execute the post-phase build gate (`npm run lint && npx tsc --noEmit
&& npm run build`) that v1.5 D-12 mandates as phase completion criteria, and
which MUST pass cleanly after Plans 01 + 02 + 03 apply their code edits.
This plan runs in Wave 2 (after Plans 01-03 complete in Wave 1) so the
build gate validates those edits on disk.

Purpose: REQUIREMENTS.md traceability matches audit reality (44 items become
`[x]`, NOTIF-01 becomes `[ ]` to match its deferred status), and the
post-phase D-12 build gate is verified green across the whole repository
after the Plan 01-03 code changes.

Output: Patched REQUIREMENTS.md with 45 line changes (44 `[ ]`→`[x]` +
1 `[x]`→`[ ]`) + unchanged traceability table + unchanged Coverage footnote.
Passing build gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/v1.5-MILESTONE-AUDIT.md

# File to modify
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Checkbox flip scope — verified against current file state on 2026-04-15.

[ ] → [x] flips (44 total):
  ANALYTICS-01..10   (10)   — currently all 10 show `- [ ]` (lines 16-25)
  COACH-DASH-01..07  (7)    — currently all 7 show `- [ ]` (lines 31-37)
  COACH-ANALYTICS-01..07 (7) — currently all 7 show `- [ ]` (lines 43-49)
  DEALS-01..11       (11)   — currently all 11 show `- [ ]` (lines 55-65)
  PERF-01..08        (8)    — currently all 8 show `- [ ]` (lines 87-94)
  NOTIF-06           (1)    — currently `- [ ]` on line 76 (shipped in Phase 52)
  = 44 flips from [ ] to [x].

[x] → [ ] flips (1 total):
  NOTIF-01           (1)    — currently `- [x]` on line 71 (incorrect per audit
                              which classifies NOTIF-01 as deferred pending D-06
                              Monday stakeholder meeting). Flip to `- [ ]`.

Total line changes: 45.

Stays [x] (do NOT touch — already correct):
  NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05, NOTIF-07, NOTIF-08, NOTIF-09,
  NOTIF-10, NOTIF-11  (9 lines)

Post-edit NOTIF section state (expected):
  NOTIF-01 = [ ]   (deferred)
  NOTIF-02..05, 06, 07..11 = [x]   (10 of 11 checked)

Coverage footnote (line ~191):
  Current text: "Coverage: 47/47 primary REQ-IDs mapped ... Zero orphans."
  This text maps REQ-IDs to phases (structural ownership). It is CORRECT
  as-is and is not affected by shipped-vs-deferred status. DO NOT edit this
  line — the traceability table reflects ownership, not shipped status.
-->

<!-- The traceability table (lines ~134-189) is IMMUTABLE — REQ-ID to phase
mapping does not change when we flip checkboxes on the bullet list above
(lines ~16-94). Only the bullet-list checkboxes in the five shipped requirement
sections + NOTIF-01 + NOTIF-06 change. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile REQUIREMENTS.md checkboxes to reflect v1.5 shipped reality (44 flips `[ ]`→`[x]` + 1 flip `[x]`→`[ ]`)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — 192 lines; locate each bullet and verify current state before edit)
    - .planning/v1.5-MILESTONE-AUDIT.md frontmatter lines 1-58 (confirms scores.requirements 54/54 + NOTIF-01 as the sole deferred entry)
    - .planning/STATE.md lines 46-65 (confirms D-06 still pending → NOTIF-01 remains deferred)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` in place. The edits are **unconditional** —
    do not probe current state and skip; run the exact replacements below.
    The current file state was confirmed on 2026-04-15 and matches the
    expected "before" state for every line listed.

    **44 flips from `- [ ]` → `- [x]`:**

    **ANALYTICS section (lines ~16-25):**
    - ANALYTICS-01, ANALYTICS-02, ANALYTICS-03, ANALYTICS-04, ANALYTICS-05,
      ANALYTICS-06, ANALYTICS-07, ANALYTICS-08, ANALYTICS-09, ANALYTICS-10
      → all 10 become `- [x]`

    **COACH-DASH section (lines ~31-37):**
    - COACH-DASH-01 through COACH-DASH-07 → all 7 become `- [x]`

    **COACH-ANALYTICS section (lines ~43-49):**
    - COACH-ANALYTICS-01 through COACH-ANALYTICS-07 → all 7 become `- [x]`

    **DEALS section (lines ~55-65):**
    - DEALS-01 through DEALS-11 → all 11 become `- [x]`

    **PERF section (lines ~87-94):**
    - PERF-01 through PERF-08 → all 8 become `- [x]`
    - Note: PERF-05 is satisfied by Plan 01 (work-sessions coach-tag bust);
      PERF-02 is satisfied by Plan 02 (CSV export rate limit). Both ship
      BEFORE this plan runs because Plan 04 is now in Wave 2 with
      `depends_on: ["01", "02", "03"]`.

    **NOTIF section (line 76):**
    - NOTIF-06 → flip `- [ ]` → `- [x]` (unconditional; current state
      confirmed `- [ ]`). NOTIF-06 shipped in Phase 52 coach-alerts work;
      audit classifies it as satisfied.

    **1 flip from `- [x]` → `- [ ]`:**

    **NOTIF section (line 71):**
    - NOTIF-01 → flip `- [x]` → `- [ ]` (unconditional; current state
      confirmed `- [x]`). This is an inverse flip — REQUIREMENTS.md
      currently overstates NOTIF-01 as shipped, but v1.5-MILESTONE-AUDIT.md
      places NOTIF-01 in its `deferred` block and STATE.md D-06 documents
      the pending Monday stakeholder meeting. Flipping to `- [ ]` makes the
      file truthfully reflect the deferred status.

    **DO NOT TOUCH:**
    - NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05 (already `[x]`, correct).
    - NOTIF-07, NOTIF-08, NOTIF-09, NOTIF-10, NOTIF-11 (already `[x]`, correct).
    - The Traceability table (lines ~134-189 — REQ-ID → phase name mapping).
    - The Coverage footnote (line ~191 — "47/47 primary REQ-IDs mapped").
    - Any prose outside checkbox bullets (section headers, HTML comments,
      Future Requirements, Out of Scope).
    - Any bullet text after the bolded REQ-ID.

    Method: Use targeted find-and-replace.
    - For each of the 44 REQ-IDs flipping `[ ]`→`[x]`: locate the unique
      string `- [ ] **{REQ_ID}**:` and replace with `- [x] **{REQ_ID}**:`.
      Keep the colon, bolding, and description untouched.
    - For NOTIF-01 flipping `[x]`→`[ ]`: locate the unique string
      `- [x] **NOTIF-01**:` and replace with `- [ ] **NOTIF-01**:`.
      Keep the colon, bolding, and description untouched.

    After editing, verify arithmetic with these six greps (each must return
    the expected count):
    ```bash
    grep -c '^- \[x\] \*\*ANALYTICS-' .planning/REQUIREMENTS.md       # 10
    grep -c '^- \[x\] \*\*COACH-DASH-' .planning/REQUIREMENTS.md      # 7
    grep -c '^- \[x\] \*\*COACH-ANALYTICS-' .planning/REQUIREMENTS.md # 7
    grep -c '^- \[x\] \*\*DEALS-' .planning/REQUIREMENTS.md           # 11
    grep -c '^- \[x\] \*\*PERF-' .planning/REQUIREMENTS.md            # 8
    grep -c '^- \[x\] \*\*NOTIF-' .planning/REQUIREMENTS.md           # 10 (NOTIF-01 out, NOTIF-06 in)
    ```
    Total `[x]` across these six prefixes = 10+7+7+11+8+10 = 53.
    The one remaining `[ ]` is NOTIF-01.
  </action>
  <verify>
    <automated>
      cd "C:/Users/ibrah/ima-accelerator-v1" &amp;&amp; \
      test "$(grep -c '^- \[x\] \*\*ANALYTICS-' .planning/REQUIREMENTS.md)" = "10" &amp;&amp; \
      test "$(grep -c '^- \[x\] \*\*COACH-DASH-' .planning/REQUIREMENTS.md)" = "7" &amp;&amp; \
      test "$(grep -c '^- \[x\] \*\*COACH-ANALYTICS-' .planning/REQUIREMENTS.md)" = "7" &amp;&amp; \
      test "$(grep -c '^- \[x\] \*\*DEALS-' .planning/REQUIREMENTS.md)" = "11" &amp;&amp; \
      test "$(grep -c '^- \[x\] \*\*PERF-' .planning/REQUIREMENTS.md)" = "8" &amp;&amp; \
      test "$(grep -c '^- \[x\] \*\*NOTIF-' .planning/REQUIREMENTS.md)" = "10" &amp;&amp; \
      grep -q '^- \[ \] \*\*NOTIF-01\*\*' .planning/REQUIREMENTS.md &amp;&amp; \
      grep -q '^- \[x\] \*\*NOTIF-06\*\*' .planning/REQUIREMENTS.md &amp;&amp; \
      test "$(grep -c '^- \[ \] \*\*ANALYTICS-' .planning/REQUIREMENTS.md)" = "0" &amp;&amp; \
      test "$(grep -c '^- \[ \] \*\*COACH-DASH-' .planning/REQUIREMENTS.md)" = "0" &amp;&amp; \
      test "$(grep -c '^- \[ \] \*\*COACH-ANALYTICS-' .planning/REQUIREMENTS.md)" = "0" &amp;&amp; \
      test "$(grep -c '^- \[ \] \*\*DEALS-' .planning/REQUIREMENTS.md)" = "0" &amp;&amp; \
      test "$(grep -c '^- \[ \] \*\*PERF-' .planning/REQUIREMENTS.md)" = "0" &amp;&amp; \
      test "$(grep -c '^- \[ \] \*\*NOTIF-' .planning/REQUIREMENTS.md)" = "1" &amp;&amp; \
      grep -q '\*\*Coverage:\*\* 47/47 primary REQ-IDs mapped' .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <acceptance_criteria>
    Line-change arithmetic (total = 45: 44 `[ ]`→`[x]` + 1 `[x]`→`[ ]`):
    - 10 ANALYTICS flips `[ ]`→`[x]`
    - 7  COACH-DASH flips `[ ]`→`[x]`
    - 7  COACH-ANALYTICS flips `[ ]`→`[x]`
    - 11 DEALS flips `[ ]`→`[x]`
    - 8  PERF flips `[ ]`→`[x]`
    - 1  NOTIF-06 flip `[ ]`→`[x]`
    - 1  NOTIF-01 flip `[x]`→`[ ]`
    Sum: 10+7+7+11+8+1+1 = 45 line changes.

    Verified by grep:
    - `grep -c '^- \[x\] \*\*ANALYTICS-' .planning/REQUIREMENTS.md` returns 10
    - `grep -c '^- \[x\] \*\*COACH-DASH-' .planning/REQUIREMENTS.md` returns 7
    - `grep -c '^- \[x\] \*\*COACH-ANALYTICS-' .planning/REQUIREMENTS.md` returns 7
    - `grep -c '^- \[x\] \*\*DEALS-' .planning/REQUIREMENTS.md` returns 11
    - `grep -c '^- \[x\] \*\*PERF-' .planning/REQUIREMENTS.md` returns 8
    - `grep -c '^- \[x\] \*\*NOTIF-' .planning/REQUIREMENTS.md` returns 10
    - `grep -c '^- \[ \] \*\*ANALYTICS-' .planning/REQUIREMENTS.md` returns 0
    - `grep -c '^- \[ \] \*\*COACH-DASH-' .planning/REQUIREMENTS.md` returns 0
    - `grep -c '^- \[ \] \*\*COACH-ANALYTICS-' .planning/REQUIREMENTS.md` returns 0
    - `grep -c '^- \[ \] \*\*DEALS-' .planning/REQUIREMENTS.md` returns 0
    - `grep -c '^- \[ \] \*\*PERF-' .planning/REQUIREMENTS.md` returns 0
    - `grep -c '^- \[ \] \*\*NOTIF-' .planning/REQUIREMENTS.md` returns 1
    - `grep -q '^- \[ \] \*\*NOTIF-01\*\*' .planning/REQUIREMENTS.md` exits 0 (NOTIF-01 is deferred)
    - `grep -q '^- \[x\] \*\*NOTIF-06\*\*' .planning/REQUIREMENTS.md` exits 0 (NOTIF-06 is shipped)
    - `grep -q '\*\*Coverage:\*\* 47/47 primary REQ-IDs mapped' .planning/REQUIREMENTS.md` exits 0 (footnote unchanged)
    - `grep -c '| ANALYTICS-' .planning/REQUIREMENTS.md` returns 10 (traceability table rows unchanged)
    - `grep -c '| DEALS-' .planning/REQUIREMENTS.md` returns 11 (traceability table rows unchanged)
  </acceptance_criteria>
  <done>
    45 line changes applied to REQUIREMENTS.md: 44 `[ ]`→`[x]` flips across
    the five shipped v1.5 sections plus NOTIF-06, and 1 `[x]`→`[ ]` flip on
    NOTIF-01 to reflect its deferred status. Traceability table and Coverage
    footnote are byte-identical to pre-edit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Post-phase D-12 build gate — lint + typecheck + build</name>
  <files></files>
  <read_first>
    - .planning/STATE.md lines 58-60 (D-12 mandate: `npm run lint && npx tsc --noEmit && npm run build`)
    - CLAUDE.md Commands section (confirm the three command strings)
  </read_first>
  <action>
    Execute the post-phase build gate that v1.5 D-12 mandates. This task
    runs in Wave 2 AFTER Plans 01 + 02 + 03 have completed and committed
    their code edits in Wave 1 (Plan 04's frontmatter
    `depends_on: ["01", "02", "03"]` enforces this ordering at the execute
    level — Plan 04 cannot start until all three Wave 1 plans are green).

    Run these three commands in sequence, from the repo root. Each must
    exit 0. Do NOT use `--fix`, do NOT use `--no-verify`. If any command
    fails, fix the underlying issue (usually a lint or type error
    introduced by Plan 01/02/03) and re-run from the top.

    1. `npm run lint`  — must exit 0, zero ESLint errors across all modified files
    2. `npx tsc --noEmit`  — must exit 0, zero TypeScript errors
    3. `npm run build`  — must exit 0, full Next.js production build green

    Scope of validation: the build must cover the three modified files from
    Plans 01-03 (work-sessions/[id]/route.ts, coach/analytics/export.csv/route.ts,
    deals/route.ts) plus every other source file in the repository — a
    full-repo build, not a partial one. This is the phase's completion gate.

    If all three commands exit 0, this task is done. No file modifications
    expected (pure verification step).
  </action>
  <verify>
    <automated>
      cd "C:/Users/ibrah/ima-accelerator-v1" &amp;&amp; \
      npm run lint &amp;&amp; \
      npx tsc --noEmit &amp;&amp; \
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 with zero errors
    - `npx tsc --noEmit` exits 0 with zero type errors
    - `npm run build` exits 0 with a successful Next.js production build
    - No new files created (verification-only task)
    - No files modified during this task (the three commands read the working tree; `npm run build` creates .next/ which is gitignored and not a tracked file change)
  </acceptance_criteria>
  <done>
    All three commands of the D-12 post-phase gate exit 0. The full v1.5 surface (including Plans 01/02/03 edits from this phase) compiles, lints, and builds cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan edits an internal planning document and runs local build
tooling. No runtime trust boundaries touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53.04-01 | — | — | N/A | REQUIREMENTS.md is a bookkeeping document with no runtime consumers; flipping checkboxes has no security-relevant effect. |
| T-53.04-02 | Tampering | build gate masking regressions | mitigate | The build gate (`npm run lint && npx tsc --noEmit && npm run build`) enforces that Plans 01-03's code edits compile and lint cleanly against the whole repo — catches regressions from those plans before the phase commits. Runs the FULL Next.js build (not a partial check). Plan 04 is now in Wave 2 with `depends_on: ["01", "02", "03"]` so the gate validates Wave 1's committed output rather than relying on implicit sequential execution within a single wave. |
| T-53.04-03 | Repudiation | incorrect requirement status claim | mitigate | The checkbox flips are evidenced by `.planning/v1.5-MILESTONE-AUDIT.md` (scores.requirements 54/54) + each v1.5 phase's VERIFICATION.md — audit trail is complete. NOTIF-01 deferral is documented in STATE.md D-06 blocker and in the audit's `deferred` frontmatter block. The NOTIF-01 `[x]`→`[ ]` inverse flip is explicitly to correct a pre-existing bookkeeping error surfaced by the audit reconciliation. |
</threat_model>

<verification>
- All 44 intended `[ ]`→`[x]` flips applied; NOTIF-01 is `[ ]` (flipped from `[x]`).
- Traceability table and Coverage footnote are unchanged.
- `npm run lint` / `npx tsc --noEmit` / `npm run build` all exit 0 — D-12 gate green.
</verification>

<success_criteria>
- REQUIREMENTS.md traceability matches v1.5-MILESTONE-AUDIT.md's `scores.requirements: 54/54` (53 satisfied `[x]` + 1 deferred `[ ]` = 54 in scope).
- No REQ-ID row in the traceability table is altered.
- The full-repo build succeeds after Plans 01-03 have applied their code edits (enforced by Wave 2 ordering).
</success_criteria>

<output>
After completion, create `.planning/phases/53-v1.5-cache-invalidation-fixes/53-04-SUMMARY.md`
</output>
