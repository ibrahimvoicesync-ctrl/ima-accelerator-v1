---
phase: 53-v1.5-cache-invalidation-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/deals/route.ts
autonomous: true
requirements:
  - DEALS-09
gap_closure: true
must_haves:
  truths:
    - "`revalidateTag(\"deals-${studentId}\")` no longer appears in `src/app/api/deals/route.ts` (both lines 183 and 213 call sites removed)."
    - "All other revalidateTag calls in the same file (`badges`, `studentAnalyticsTag`, `coachDashboardTag`, `coachAnalyticsTag`, `coachMilestonesTag`) are preserved — they back real cache consumers."
    - "Creating a deal as student / coach / owner continues to work end-to-end with no behavior regression in the three deals pages (`/student/deals`, `/student_diy/deals`, coach+owner deals tabs are direct-fetch server components — unaffected by removal of the orphan tag)."
    - "POST /api/deals retry-on-23505 path still invalidates all live tags (badges, studentAnalyticsTag, coach* tags)."
  artifacts:
    - path: "src/app/api/deals/route.ts"
      provides: "POST handler with orphan deals-${studentId} tag removed from both retry path and happy path"
      forbids_contains: 'revalidateTag(`deals-'
  key_links:
    - from: "src/app/api/deals/route.ts POST"
      to: "student/coach/owner deals pages"
      via: "direct admin.from('deals').select(...) in server components; no unstable_cache with 'deals-*' tag registration"
      pattern: "revalidateTag\\(studentAnalyticsTag"
---

<objective>
Close the MINOR integration gap from v1.5-MILESTONE-AUDIT.md:
`src/app/api/deals/route.ts` fires `revalidateTag("deals-${studentId}")` at
lines 183 and 213 but NO cache consumer in the codebase registers that tag.
The student/coach/owner deals pages are direct-fetch server components with
no `unstable_cache` wrapping. This is dead invalidation work — no functional
break, but wasted syscall + noise during debugging.

Purpose: Remove both orphan `revalidateTag("deals-${studentId}")` calls
without touching any sibling live-tag invalidation. Satisfies DEALS-09
hygiene audit finding.

Output: Patched POST handler with exactly 2 lines removed. All other cache
busts preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/v1.5-MILESTONE-AUDIT.md

# Reference file (read, do not modify)
@src/lib/rpc/student-analytics-types.ts
@src/lib/rpc/coach-dashboard-types.ts

<interfaces>
<!-- Audit evidence confirming orphan status -->

The `deals-${studentId}` tag is NOT registered by any `unstable_cache` call
anywhere in `src/lib/` or `src/app/`. Verification command (must return 0
matches after this plan ships — and already returns 0 matches as a consumer
today, which is exactly why the invalidation is orphaned):

```bash
grep -rE 'tags:\\s*\\[.*deals-\\$\\{' src/ || true
grep -rE 'unstable_cache.*deals-' src/ || true
```

The actual student deals page is a direct-fetch server component — no tag
registration exists. See .planning/v1.5-MILESTONE-AUDIT.md line 151-153 for
the finding.

Lines to remove (from current `src/app/api/deals/route.ts`):

- Line 183: `revalidateTag(\`deals-${effectiveStudentId}\`, "default");` (inside the 23505 retry block)
- Line 213: `revalidateTag(\`deals-${effectiveStudentId}\`, "default");` (inside the happy-path block)

Sibling calls that MUST stay (all have real consumers — do NOT delete):
- `revalidateTag("badges", "default")` — sidebar coach_milestone_alerts badge
- `revalidateTag(studentAnalyticsTag(effectiveStudentId), "default")` — /student/analytics + /student_diy/analytics
- `revalidateTag(coachDashboardTag(studentRow.coach_id), "default")` — /coach homepage
- `revalidateTag(coachAnalyticsTag(studentRow.coach_id), "default")` — /coach/analytics
- `revalidateTag(coachMilestonesTag(studentRow.coach_id), "default")` — /coach/alerts feed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphan deals-${studentId} revalidateTag calls from POST /api/deals</name>
  <files>src/app/api/deals/route.ts</files>
  <read_first>
    - src/app/api/deals/route.ts (entire file — locate both call sites: 23505 retry at line 183 and happy-path at line 213; confirm no OTHER `deals-` tag references exist)
    - .planning/v1.5-MILESTONE-AUDIT.md lines 150-155 (gap rationale)
  </read_first>
  <action>
    Edit `src/app/api/deals/route.ts`:

    1. **Delete line 183** — the statement
       `revalidateTag(\`deals-${effectiveStudentId}\`, "default");`
       inside the 23505 retry block (currently immediately after the
       `if (retryError) {...return 500}` block closes, before the
       `revalidateTag("badges", "default")` line).

    2. **Delete line 213** — the statement
       `revalidateTag(\`deals-${effectiveStudentId}\`, "default");`
       in the happy-path cache-invalidation block (immediately after the
       comment `// 10. Cache invalidation (per-student)` and before
       `revalidateTag("badges", "default")`).

    3. **Do not** remove or modify the adjacent `revalidateTag("badges", ...)`
       calls — both have active sidebar-badge consumers in Phase 51/52.
    4. **Do not** touch the `studentAnalyticsTag` try/catch blocks in either
       location — both serve live `/student/analytics` + `/student_diy/analytics`
       cache entries.
    5. **Do not** modify the coach-tag invalidation blocks (the inner
       `admin.from("users").select("coach_id")` lookups and their sibling
       `revalidateTag(coachDashboardTag/coachAnalyticsTag/coachMilestonesTag)`
       calls) — all three back real cache consumers.
    6. Remove any now-dead comment artifacts ONLY if a comment literally
       mentions the `deals-` tag. Do NOT edit the comment
       `// 10. Cache invalidation (per-student)` — the remaining busts are
       still per-student-scoped (studentAnalyticsTag is per-student).
    7. Verify zero `deals-` template-literal tag references remain after
       edit (see acceptance criteria grep below).
  </action>
  <verify>
    <automated>
      cd "C:/Users/ibrah/ima-accelerator-v1" &amp;&amp; \
      test "$(grep -c 'revalidateTag(`deals-' src/app/api/deals/route.ts)" = "0" &amp;&amp; \
      grep -q 'revalidateTag("badges"' src/app/api/deals/route.ts &amp;&amp; \
      grep -q 'revalidateTag(studentAnalyticsTag' src/app/api/deals/route.ts &amp;&amp; \
      grep -q 'revalidateTag(coachDashboardTag' src/app/api/deals/route.ts &amp;&amp; \
      grep -q 'revalidateTag(coachAnalyticsTag' src/app/api/deals/route.ts &amp;&amp; \
      grep -q 'revalidateTag(coachMilestonesTag' src/app/api/deals/route.ts &amp;&amp; \
      npx tsc --noEmit &amp;&amp; \
      npm run lint
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'revalidateTag(\`deals-' src/app/api/deals/route.ts` returns 0 (orphan tag fully removed; both call sites gone)
    - `grep -c 'revalidateTag("badges"' src/app/api/deals/route.ts` returns 2 (retry path + happy path preserved)
    - `grep -c 'revalidateTag(studentAnalyticsTag' src/app/api/deals/route.ts` returns 2 (both paths preserved)
    - `grep -c 'revalidateTag(coachDashboardTag' src/app/api/deals/route.ts` returns 2 (both paths preserved)
    - `grep -c 'revalidateTag(coachAnalyticsTag' src/app/api/deals/route.ts` returns 2 (both paths preserved)
    - `grep -c 'revalidateTag(coachMilestonesTag' src/app/api/deals/route.ts` returns 2 (both paths preserved)
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Zero `revalidateTag(\`deals-\${...}\`)` calls remain in `src/app/api/deals/route.ts`; all five live sibling tag invalidations (badges, studentAnalyticsTag, coachDashboardTag, coachAnalyticsTag, coachMilestonesTag) still fire on both retry and happy path; type-check and lint pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced or crossed by this plan — it is pure deletion of dead cache-invalidation syscalls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53.03-01 | — | — | N/A | This plan removes only orphaned revalidation calls that had no consumer. No security property changes. Existing auth + role + rate-limit + dual-layer RLS + Zod validation on POST /api/deals are untouched. |
| T-53.03-02 | Denial of Service | accidentally removing live tag | mitigate | Acceptance-criteria grep counts assert all 5 live sibling tags (badges, studentAnalyticsTag, coachDashboardTag, coachAnalyticsTag, coachMilestonesTag) still appear exactly 2× after edit — one per code path. Any accidental deletion of a live tag is blocked by the automated verification. |
</threat_model>

<verification>
- Two `revalidateTag("deals-${...}")` call sites deleted; grep count for the orphan tag is 0.
- All five live sibling tags still fire twice each (retry + happy path).
- No other code in `src/app/api/deals/route.ts` is modified.
- End-to-end: student self-logs a deal → `/student/deals` still shows it on next request (direct-fetch server component reads DB fresh); `/student/analytics`, `/coach`, `/coach/analytics`, `/coach/alerts` continue to invalidate correctly.
- `npx tsc --noEmit` and `npm run lint` pass with zero errors.
</verification>

<success_criteria>
- `grep -c 'revalidateTag(\`deals-' src/app/api/deals/route.ts` returns 0.
- Creating a deal as student, coach, and owner continues to work and the respective deals pages show the new deal on next navigation.
- No regression in sidebar badge count, student analytics page, coach dashboard, coach analytics, or coach alerts feed.
</success_criteria>

<output>
After completion, create `.planning/phases/53-v1.5-cache-invalidation-fixes/53-03-SUMMARY.md`
</output>
