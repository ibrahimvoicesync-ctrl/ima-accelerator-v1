---
phase: 22-spike-protection-rate-limiting
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/app/api/reports/route.ts
  - src/app/api/reports/[id]/review/route.ts
  - src/app/api/work-sessions/route.ts
  - src/app/api/work-sessions/[id]/route.ts
  - src/app/api/roadmap/route.ts
  - src/app/api/invites/route.ts
  - src/app/api/magic-links/route.ts
  - src/app/api/assignments/route.ts
  - src/app/api/alerts/dismiss/route.ts
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "A user who exceeds 30 requests/minute to any single mutation endpoint receives a 429 response"
    - "The 429 response includes a Retry-After header with the value 60"
    - "The 429 response body contains a human-readable error message for client-side toast display"
    - "Rate limiting is enforced after auth/role check and before Zod body parsing"
    - "All 10 mutation routes (POST /api/reports, PATCH /api/reports/[id]/review, POST /api/work-sessions, PATCH /api/work-sessions/[id], PATCH /api/roadmap, POST /api/invites, POST /api/magic-links, PATCH /api/magic-links, PATCH /api/assignments, POST /api/alerts/dismiss) call checkRateLimit()"
    - "Each route uses a unique endpoint string so rate limits are per-endpoint per-user (per D-04)"
  artifacts:
    - path: "src/app/api/reports/route.ts"
      provides: "Rate-limited POST handler"
      contains: "checkRateLimit"
    - path: "src/app/api/reports/[id]/review/route.ts"
      provides: "Rate-limited PATCH handler"
      contains: "checkRateLimit"
    - path: "src/app/api/work-sessions/route.ts"
      provides: "Rate-limited POST handler"
      contains: "checkRateLimit"
    - path: "src/app/api/work-sessions/[id]/route.ts"
      provides: "Rate-limited PATCH handler"
      contains: "checkRateLimit"
    - path: "src/app/api/roadmap/route.ts"
      provides: "Rate-limited PATCH handler"
      contains: "checkRateLimit"
    - path: "src/app/api/invites/route.ts"
      provides: "Rate-limited POST handler"
      contains: "checkRateLimit"
    - path: "src/app/api/magic-links/route.ts"
      provides: "Rate-limited POST and PATCH handlers with distinct endpoint strings"
      contains: "checkRateLimit"
    - path: "src/app/api/assignments/route.ts"
      provides: "Rate-limited PATCH handler"
      contains: "checkRateLimit"
    - path: "src/app/api/alerts/dismiss/route.ts"
      provides: "Rate-limited POST handler"
      contains: "checkRateLimit"
  key_links:
    - from: "src/app/api/reports/route.ts"
      to: "src/lib/rate-limit.ts"
      via: "import checkRateLimit"
      pattern: "import.*checkRateLimit.*from.*@/lib/rate-limit"
    - from: "src/app/api/magic-links/route.ts"
      to: "src/lib/rate-limit.ts"
      via: "import checkRateLimit — called in both POST and PATCH with different endpoint strings"
      pattern: "checkRateLimit\\(profile\\.id"
---

<objective>
Integrate checkRateLimit() into all 10 mutation API routes so every POST/PATCH/DELETE mutation is rate-limited at 30 requests/minute per user per endpoint.

Purpose: Without integration, the rate_limit_log table and helper from Plan 01 are unused. This plan connects the infrastructure to every mutation route, completing the SEC-01 requirement.

Output: All 9 route handler files modified with rate limiting after auth check, before body parsing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-spike-protection-rate-limiting/22-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: the rate-limit helper that all routes will call -->

From src/lib/rate-limit.ts (created in Plan 01):
```typescript
export type RateLimitResult = {
  allowed: boolean;
  remaining: number;
  retryAfterSeconds: number;
};

export async function checkRateLimit(
  userId: string,
  endpoint: string,
  maxRequests?: number,    // default: 30
  windowMinutes?: number   // default: 1
): Promise<RateLimitResult>;
```

Endpoint string mapping (per D-04 — per-endpoint per-user):
| Route File                                | Method | Endpoint String              |
|-------------------------------------------|--------|------------------------------|
| src/app/api/reports/route.ts              | POST   | "/api/reports"               |
| src/app/api/reports/[id]/review/route.ts  | PATCH  | "/api/reports/review"        |
| src/app/api/work-sessions/route.ts        | POST   | "/api/work-sessions"         |
| src/app/api/work-sessions/[id]/route.ts   | PATCH  | "/api/work-sessions/update"  |
| src/app/api/roadmap/route.ts              | PATCH  | "/api/roadmap"               |
| src/app/api/invites/route.ts              | POST   | "/api/invites"               |
| src/app/api/magic-links/route.ts          | POST   | "/api/magic-links/create"    |
| src/app/api/magic-links/route.ts          | PATCH  | "/api/magic-links/update"    |
| src/app/api/assignments/route.ts          | PATCH  | "/api/assignments"           |
| src/app/api/alerts/dismiss/route.ts       | POST   | "/api/alerts/dismiss"        |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add checkRateLimit() to student and coach mutation routes</name>
  <files>
    src/app/api/reports/route.ts
    src/app/api/reports/[id]/review/route.ts
    src/app/api/work-sessions/route.ts
    src/app/api/work-sessions/[id]/route.ts
    src/app/api/roadmap/route.ts
  </files>
  <read_first>
    - src/lib/rate-limit.ts (confirm export name and signature from Plan 01)
    - src/app/api/reports/route.ts (see current auth pattern — add rate limit after profile.role check on line 42)
    - src/app/api/reports/[id]/review/route.ts (add after role check for coach on line 38)
    - src/app/api/work-sessions/route.ts (add after profile.role check on line 31)
    - src/app/api/work-sessions/[id]/route.ts (add after profile.role check on line 32)
    - src/app/api/roadmap/route.ts (add after profile.role check on line 33 — note this route has an outer try-catch)
  </read_first>
  <action>
For each of the 5 route files below, make two changes:

**Change 1 — Add import at top of file:**
```typescript
import { checkRateLimit } from "@/lib/rate-limit";
```

**Change 2 — Insert rate limit check AFTER the role/profile check and BEFORE `request.json()` parsing:**

The insertion pattern is identical for all routes (only the endpoint string changes):
```typescript
  // Rate limit check (per D-01, D-04)
  const { allowed, retryAfterSeconds } = await checkRateLimit(profile.id, ENDPOINT_STRING);
  if (!allowed) {
    return NextResponse.json(
      { error: `Too many requests, try again in ${retryAfterSeconds} seconds.` },
      { status: 429, headers: { "Retry-After": String(retryAfterSeconds) } }
    );
  }
```

**Route-specific details:**

1. **src/app/api/reports/route.ts** — `POST` handler
   - Endpoint string: `"/api/reports"`
   - Insert AFTER `if (profile.role !== "student")` check (after line 42)
   - Insert BEFORE `let body: unknown;` (before line 44)

2. **src/app/api/reports/[id]/review/route.ts** — `PATCH` handler
   - Endpoint string: `"/api/reports/review"`
   - Insert AFTER `if (profile.role !== "coach")` check (after line 39)
   - Insert BEFORE `let body: unknown;` (before line 42)

3. **src/app/api/work-sessions/route.ts** — `POST` handler
   - Endpoint string: `"/api/work-sessions"`
   - Insert AFTER `if (!profile || profile.role !== "student")` check (after line 33)
   - Insert BEFORE `let body: unknown;` (before line 36)

4. **src/app/api/work-sessions/[id]/route.ts** — `PATCH` handler
   - Endpoint string: `"/api/work-sessions/update"`
   - Insert AFTER `if (!profile || profile.role !== "student")` check (after line 33)
   - Insert BEFORE `let body: unknown;` (before line 36)

5. **src/app/api/roadmap/route.ts** — `PATCH` handler
   - Endpoint string: `"/api/roadmap"`
   - Insert AFTER `if (profile.role !== "student")` check (after line 34)
   - Insert BEFORE `let body: unknown;` (before line 37)
   - Note: This route already has an outer try-catch wrapping the entire handler. The rate limit check sits inside this try-catch, which is correct — any DB error from checkRateLimit() will be caught and return 500.

Do NOT modify any other logic in these files. The existing auth, Zod validation, mutation, and response logic remains exactly as-is.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && echo "---" && grep -l "checkRateLimit" src/app/api/reports/route.ts src/app/api/reports/*/review/route.ts src/app/api/work-sessions/route.ts src/app/api/work-sessions/*/route.ts src/app/api/roadmap/route.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/reports/route.ts contains `import { checkRateLimit } from "@/lib/rate-limit"`
    - src/app/api/reports/route.ts contains `checkRateLimit(profile.id, "/api/reports")`
    - src/app/api/reports/route.ts contains `status: 429`
    - src/app/api/reports/route.ts contains `"Retry-After"`
    - src/app/api/reports/[id]/review/route.ts contains `checkRateLimit(profile.id, "/api/reports/review")`
    - src/app/api/reports/[id]/review/route.ts contains `status: 429`
    - src/app/api/work-sessions/route.ts contains `checkRateLimit(profile.id, "/api/work-sessions")`
    - src/app/api/work-sessions/route.ts contains `status: 429`
    - src/app/api/work-sessions/[id]/route.ts contains `checkRateLimit(profile.id, "/api/work-sessions/update")`
    - src/app/api/work-sessions/[id]/route.ts contains `status: 429`
    - src/app/api/roadmap/route.ts contains `checkRateLimit(profile.id, "/api/roadmap")`
    - src/app/api/roadmap/route.ts contains `status: 429`
    - All 5 files contain `Too many requests, try again in`
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>All 5 student/coach mutation routes call checkRateLimit() after auth and before body parsing; each uses a unique endpoint string; 429 responses include Retry-After header and human-readable error message</done>
</task>

<task type="auto">
  <name>Task 2: Add checkRateLimit() to admin mutation routes</name>
  <files>
    src/app/api/invites/route.ts
    src/app/api/magic-links/route.ts
    src/app/api/assignments/route.ts
    src/app/api/alerts/dismiss/route.ts
  </files>
  <read_first>
    - src/lib/rate-limit.ts (confirm export name and signature)
    - src/app/api/invites/route.ts (add after role check on line 31)
    - src/app/api/magic-links/route.ts (IMPORTANT: has BOTH POST and PATCH — add to EACH with different endpoint strings)
    - src/app/api/assignments/route.ts (add after role check on line 29)
    - src/app/api/alerts/dismiss/route.ts (add after role check on line 31)
  </read_first>
  <action>
For each of the 4 route files below, make two changes:

**Change 1 — Add import at top of file:**
```typescript
import { checkRateLimit } from "@/lib/rate-limit";
```

**Change 2 — Insert rate limit check (same pattern as Task 1):**
```typescript
  // Rate limit check (per D-01, D-04)
  const { allowed, retryAfterSeconds } = await checkRateLimit(profile.id, ENDPOINT_STRING);
  if (!allowed) {
    return NextResponse.json(
      { error: `Too many requests, try again in ${retryAfterSeconds} seconds.` },
      { status: 429, headers: { "Retry-After": String(retryAfterSeconds) } }
    );
  }
```

**Route-specific details:**

1. **src/app/api/invites/route.ts** — `POST` handler
   - Endpoint string: `"/api/invites"`
   - Insert AFTER `if (profile.role !== "coach" && profile.role !== "owner")` check (after line 31)
   - Insert BEFORE `let body: unknown;` (before line 34)

2. **src/app/api/magic-links/route.ts** — **TWO handlers in this file (per Pitfall 3)**
   - **POST handler:**
     - Endpoint string: `"/api/magic-links/create"`
     - Insert AFTER `if (profile.role !== "coach" && profile.role !== "owner")` check (after line 38)
     - Insert BEFORE `let magicRole` (before line 40)
   - **PATCH handler:**
     - Endpoint string: `"/api/magic-links/update"`
     - Insert AFTER `if (profile.role !== "coach" && profile.role !== "owner")` check (after line 104)
     - Insert BEFORE `const id = request.nextUrl.searchParams.get("id");` (before line 106)
   - CRITICAL: Both POST and PATCH in this file MUST get checkRateLimit() with DIFFERENT endpoint strings. This ensures each method has its own 30/min budget (per D-04/D-05).

3. **src/app/api/assignments/route.ts** — `PATCH` handler
   - Endpoint string: `"/api/assignments"`
   - Insert AFTER `if (profile.role !== "owner")` check (after line 30)
   - Insert BEFORE `const studentId = request.nextUrl.searchParams.get("studentId");` (before line 34)

4. **src/app/api/alerts/dismiss/route.ts** — `POST` handler
   - Endpoint string: `"/api/alerts/dismiss"`
   - Insert AFTER `if (profile.role !== "owner")` check (after line 31)
   - Insert BEFORE `let body: unknown;` (before line 34)

Do NOT modify any other logic in these files. The existing auth, Zod validation, mutation, and response logic remains exactly as-is.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/invites/route.ts contains `checkRateLimit(profile.id, "/api/invites")`
    - src/app/api/invites/route.ts contains `status: 429`
    - src/app/api/magic-links/route.ts contains `checkRateLimit(profile.id, "/api/magic-links/create")`
    - src/app/api/magic-links/route.ts contains `checkRateLimit(profile.id, "/api/magic-links/update")`
    - src/app/api/magic-links/route.ts contains TWO occurrences of `status: 429` (one per handler)
    - src/app/api/assignments/route.ts contains `checkRateLimit(profile.id, "/api/assignments")`
    - src/app/api/assignments/route.ts contains `status: 429`
    - src/app/api/alerts/dismiss/route.ts contains `checkRateLimit(profile.id, "/api/alerts/dismiss")`
    - src/app/api/alerts/dismiss/route.ts contains `status: 429`
    - All 4 files contain `import { checkRateLimit } from "@/lib/rate-limit"`
    - All 4 files contain `Too many requests, try again in`
    - All 4 files contain `"Retry-After"`
    - `npx tsc --noEmit` passes with no errors
    - `npm run build` succeeds
    - Total checkRateLimit() calls across ALL 9 route files = 10 (magic-links has 2)
  </acceptance_criteria>
  <done>All 5 admin mutation routes (across 4 files) call checkRateLimit() after auth and before body parsing; magic-links/route.ts has separate rate limit checks for POST and PATCH with distinct endpoint strings; full build passes</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (all route files compile with checkRateLimit import)
- `npm run build` succeeds (production build validates all routes)
- `npm run lint` passes
- All 9 route handler files contain `import { checkRateLimit } from "@/lib/rate-limit"`
- All 9 route handler files contain `status: 429`
- All 9 route handler files contain `"Retry-After"`
- magic-links/route.ts contains TWO separate checkRateLimit() calls with different endpoint strings
- Total checkRateLimit() calls = 10 across all files (9 files, magic-links has 2)
- No route calls checkRateLimit() before the profile lookup (always uses `profile.id`, never `authUser.id`)
</verification>

<success_criteria>
1. All 10 mutation routes (per D-02) call checkRateLimit() after auth verification and before Zod validation
2. Each route uses a unique endpoint string for per-endpoint rate limiting (per D-04)
3. 429 responses include `Retry-After` header and human-readable error message (per D-06)
4. magic-links/route.ts has separate rate limit checks for POST and PATCH (per Pitfall 3)
5. `npm run build` succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-spike-protection-rate-limiting/22-02-SUMMARY.md`
</output>
